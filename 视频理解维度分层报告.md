# ViralForge 当前已实现的视频理解维度分层报告

## 1. 报告范围

本文只统计当前运行时代码中已经实际分析、归一化、落盘或被下游规则消费的视频理解维度。

本文不展开设计文档中的规划项，也不把未实际生成或消费的字段纳入统计。

本文的分析对象有两类：

1. **用户素材视频**：用户上传、用于复用的原始视频。
2. **参考/爆款视频**：当前任务中的 `reference_video`。系统把它作为参考片拆解，并不依据平台数据重新判断它是否真的“爆款”。

代码证据：

- 用户素材分析入口：`analyze_materials.py:1`
- 参考视频分析入口：`analyze_script.py:1`
- 主流水线步骤顺序：`pipeline.py:2340`

## 2. 当前实际分层

当前已经形成的理解层级如下。层级从细到粗排列。

```text
L0 媒体事实与时间轴
 ↓
L1 帧 / 关键帧 / 音频事实
 ↓
L2 对象 / 主体 / 产品 / 场景
 ↓
L3 动作 / 事件 / 情绪 / 台词语义
 ↓
L4 镜头 / 分镜 / 运镜 / 转场
 ↓
L5 语义片段 / 可召回素材资产
 ↓
L6 分镜与素材之间的匹配关系
 ↓
L7 整视频叙事 / 结构 / 节奏 / 产品植入
 ↓
L8 任务内多视频素材索引与来源追踪
```

> L8 是当前已经实现的“任务内聚合”，不是跨任务用户画像，也不是多条爆款模式学习。

## 3. 字段数量总览

以下统计按当前 JSON schema 中的字段计算。同一个概念在不同产物中分别计数，不进行跨产物去重。

| 分析产物 | 直接分析字段 | 运行时补充字段 | 当前结果 |
|---|---:|---:|---|
| 用户素材单个片段 | 17 个描述字段 + 5 个布尔判定字段 = 22 个 | 片段 ID、源视频、召回文本 3 个；任务索引另加源文件 | 分析产物 25 个字段；任务索引 26 个字段 |
| 用户素材整条视频 | 7 个整体字段 | 素材 ID、视频名、来源 URL、分析链路、总时长、召回版本 6 个 | 7 个整体理解字段 + 6 个元数据字段 |
| 参考视频单个分镜 | 15 个字段 | 无额外分镜语义字段 | 每个分镜 15 个字段 |
| 参考视频整条视频 | 12 个整体语义字段 + 2 个音轨布尔字段 = 14 个 | 视频名、来源 URL、分析链路、总时长 4 个 | 14 个整体理解字段 + 4 个元数据字段 |
| 商品候选帧与帧判定 | 候选选择 3 个字段；帧判定 6 个字段 | 源文件、帧文件等来源字段 | 商品帧理解与可用性判断 |
| 商品事实卡 | 8 个核心字段 | 提供字段、图片、图片 URL、图片来源、补齐过程等 | 商品信息归纳与来源记录 |
| 分镜-素材匹配 | 匹配度、画面匹配、口播内容匹配、理由 4 个结果字段 | 处理动作、规则命中信息 | 跨视频素材适配判断 |

## 4. L0：媒体事实与时间轴层

这一层是后续视频理解的技术底座，当前实际使用的维度如下。

### 4.1 用户素材视频

- 视频文件是否存在；
- 视频真实总时长；
- 是否存在音轨；
- 音频是否可以被抽取；
- 视频是否可以被抽帧；
- 片段开始时间；
- 片段结束时间；
- 片段时长；
- 片段时间轴是否连续；
- 片段时间轴是否覆盖整条用户视频；
- 素材来源文件；
- 素材 ID；
- 片段 ID。

用户素材分析会根据真实视频时长校准模型返回的片段时间轴，并让片段首尾相接，见 `analyze_materials.py:201`。

### 4.2 参考/爆款视频

- 视频真实总时长；
- 视频来源 URL；
- 分镜开始时间；
- 分镜结束时间；
- 分镜时长；
- 分镜时间轴是否连续；
- 分镜时间轴是否覆盖整条视频；
- 视频分析链路。

参考视频的分镜时长会根据真实视频总时长重新校准，见 `analyze_script.py:165`。

## 5. L1：帧、关键帧与音频事实层

当前实际使用了抽帧、音频抽取和若干帧/音频事实判断，具体维度如下。

### 5.1 用户素材中的帧与音频维度

#### 帧相关

- 从片段中选择商品候选帧；
- 商品在候选帧中的可见情况；
- 候选帧是否清晰；
- 候选帧存在的问题；
- 候选帧中的商品外观；
- 候选帧中可见的商品卖点；
- 候选帧适合生成哪些商品白底图；
- 候选帧的绝对时间点；
- 候选帧对应的源片段。

#### 音频相关

- 真人出镜；
- 真人出镜口播；
- 是否有 BGM；
- 是否有人声；
- 是否有可用的人脸；
- 片段中的声音台词；
- 片段中的音效；
- 片段中的音乐；
- 片段中的环境音。

用户素材的 5 个布尔判定字段定义在 `analyze_materials.py:59`：

| 字段 | 实际判断口径 |
|---|---|
| 真人出镜 | 画面里是否有真实人物 |
| 真人出镜口播 | 画面中的人是否张嘴说话，声音是否由该人物发出 |
| 有 BGM | 是否有持续旋律或节奏配乐，环境音和音效不算 |
| 有人声 | 是否有可以听出说话内容的人声，哼唱和歌曲人声不算 |
| 模特人脸出镜 | 是否有清晰、可用作人物参考的正面或侧面人脸 |

#### 用户素材声音的后续可用性判断

系统还会对候选人声做实际音量检测，用于决定是否能作为配音或音色基准：

- 候选片段是否有足够长的可用音频；
- 平均响度；
- 是否低于人声门槛；
- 是否需要分离 BGM；
- 分离后人声是否仍然存在；
- 是否通过克隆基准音门禁；
- 音色基准来自哪个用户片段；
- 被选中的音色基准片段是否与最终画面片段相同。

相关逻辑见 `pipeline.py:1435` 和 `pipeline.py:1525`。

### 5.2 参考/爆款视频中的帧与音频维度

参考视频当前直接分析并使用：

- 是否有背景音乐；
- 是否有人声口播；
- 分镜中的台词、旁白和口播；
- 分镜中的音效；
- 分镜中的音乐；
- 分镜中的环境音；
- 音乐风格；
- 音乐情绪；
- 音乐入点和切点；
- 鼓点与画面的对应关系；
- 声音设计；
- 参考音轨是否需要整轨复用；
- 参考音轨是否需要分离伴奏；
- 参考音轨是否不应复用。

参考视频的两个整体音轨布尔字段定义在 `analyze_script.py:62`。

参考音轨在流水线中还会进行“是否是真正音乐”的判断，而不是把零星环境音或音效直接当作 BGM，见 `pipeline.py:506`。

## 6. L2：对象、主体、产品与场景层

### 6.1 用户素材片段中的对象维度

用户素材的每个片段实际分析以下对象内容：

- 人物；
- 物品；
- 商品；
- 动物；
- 其他可见主体；
- 主体的可识别外观；
- 产品是否出现在画面中；
- 产品是否足够清晰；
- 人物是否出镜；
- 人脸是否可作为模特参考；
- 不可替换的主体；
- 不可替换的品牌字样；
- 不可替换的场景元素。

对应字段：

- `主体`
- `画面`
- `场景`
- `限制条件`
- `真人出镜`
- `模特人脸出镜`

字段定义见 `analyze_materials.py:32`。

### 6.2 用户素材片段中的场景维度

- 地点；
- 环境；
- 道具；
- 时间；
- 天气；
- 场景氛围；
- 场景标签；
- 适合使用的场景；
- 不适合使用的场景。

### 6.3 参考/爆款视频中的对象维度

每个参考分镜实际分析：

- 人物；
- 人物外形和服装；
- 人物表情和情绪；
- 人物视线方向；
- 画面中的主体；
- 场景地点；
- 时代或风格；
- 关键道具；
- 陈设；
- 时间和天气；
- 商品/产品植入；
- 特效对象；
- 字幕花字；
- 贴纸；
- 遮罩；
- 粒子、光效和滤镜。

这些内容分别落在：

- `人物`
- `画面`
- `场景`
- `特效`
- `产品植入`

参考分镜字段定义见 `analyze_script.py:32`。

## 7. L3：动作、事件、情绪与台词语义层

### 7.1 用户素材片段中的动作维度

每个用户片段要求分析：

- 是否有动作；
- 动作起点；
- 动作过程；
- 动作结果；
- 动作是否完整；
- 产品使用动作；
- 人物表演动作；
- 人物状态；
- 动作对应的内容标签；
- 动作适合放入什么剧本位置；
- 动作对应的叙事功能。

用户素材片段的动作字段定义在 `analyze_materials.py:34`，Prompt 对动作起点、过程和结果的要求在 `analyze_materials.py:88`。

### 7.2 用户素材片段中的情绪维度

- 主体情绪；
- 整体情绪氛围；
- 情绪标签；
- 适合承载的情绪场景；
- 情绪是否适合作为钩子、高潮、结果或收尾。

### 7.3 用户素材片段中的声音语义

- 可听见的逐字人声；
- 人物口播；
- 旁白；
- 音效；
- 音乐；
- 环境音；
- 是否有人声；
- 是否是真人口播；
- 原口播是否需要保留或替换。

### 7.4 参考/爆款分镜中的动作与事件维度

每个参考分镜实际分析：

- 人物或主体正在做什么；
- 动作起点；
- 动作终点；
- 动作是否推动剧情；
- 是否存在冲突；
- 是否存在冲突升级；
- 是否存在反转；
- 是否存在产品展示或产品露出；
- 该镜头在整体故事中的叙事功能。

### 7.5 参考/爆款分镜中的台词语义

- 人物台词；
- 旁白；
- 口播；
- 说话人；
- 台词原文；
- 台词对应的动作；
- 台词对应的产品信息；
- 台词与画面的关系。

参考视频的动作、台词和叙事功能字段见 `analyze_script.py:33` 和 `analyze_script.py:39`。

## 8. L4：镜头、分镜、运镜与转场层

### 8.1 用户素材片段中的镜头维度

用户素材不是单独输出物理镜头检测表，而是在片段中记录：

- 景别；
- 机位；
- 运镜；
- 运镜方向；
- 运镜速度；
- 片段时间范围；
- 画面构图；
- 片段是否适合作为某个目标镜头。

这些内容统一落在 `景别运镜`、`画面`、`开始时间`、`结束时间` 和 `时长秒` 中。

### 8.2 参考/爆款分镜的完整镜头字段

当前参考分镜共有 15 个字段：

| 序号 | 分析维度 | 细粒度内容 |
|---:|---|---|
| 1 | 序号 | 分镜在视频中的顺序 |
| 2 | 开始时间 | 分镜起始时间 |
| 3 | 结束时间 | 分镜结束时间 |
| 4 | 时长秒 | 分镜持续时长 |
| 5 | 景别 | 大远景、远景、全景、中景、近景、特写、大特写 |
| 6 | 运镜 | 固定、推、拉、摇、移、跟、升降、手持、环绕、变焦及方向速度 |
| 7 | 画面 | 构图、光线、色调、画质风格 |
| 8 | 人物 | 人物、外观、服装、表情、情绪、视线 |
| 9 | 动作 | 主体动作及起止状态 |
| 10 | 场景 | 地点、时代/风格、道具、陈设、天气、时间 |
| 11 | 特效 | 滤镜、调色、慢动作、加速、抖动、粒子、光效、字幕花字、贴纸、遮罩 |
| 12 | 转场 | 硬切、叠化、闪白、闪黑、划像、遮罩、运镜衔接、匹配剪辑、无 |
| 13 | 台词 | 人物台词、旁白、口播的逐字内容 |
| 14 | 音效音乐 | 音效、音乐起落变化、环境音 |
| 15 | 叙事功能 | 钩子、铺垫、冲突升级、反转、情绪高点、落点、产品露出等 |

字段定义见 `analyze_script.py:25`，镜头分析要求见 `analyze_script.py:72`。

参考视频缺失转场时，系统会单独补问转场字段，见 `analyze_script.py:221`。

## 9. L5：语义片段与可召回素材资产层

### 9.1 用户素材片段类型

当前实际使用的片段类型包括：

- 人物表演；
- 人物状态；
- 产品展示；
- 产品使用；
- 环境空镜；
- 细节特写；
- 动作过程；
- 结果展示；
- 转场素材；
- 其他。

### 9.2 用户素材片段的 17 个描述维度

| 序号 | 字段 | 实际分析内容 |
|---:|---|---|
| 1 | 片段序号 | 片段顺序 |
| 2 | 开始时间 | 原视频中的起始时间 |
| 3 | 结束时间 | 原视频中的结束时间 |
| 4 | 时长秒 | 片段持续时间 |
| 5 | 片段类型 | 人物、产品、动作、结果、空镜等片段类别 |
| 6 | 画面 | 构图、主体位置、光线、色调、画面风格 |
| 7 | 主体 | 人物、物品、动物等主体及外观 |
| 8 | 动作 | 动作起点、过程、结果 |
| 9 | 场景 | 地点、环境、道具、时间、天气 |
| 10 | 景别运镜 | 景别、机位、运镜、方向、速度 |
| 11 | 情绪氛围 | 主体情绪和整体氛围 |
| 12 | 声音台词 | 逐字人声、音效、音乐、环境音 |
| 13 | 视觉标签 | 适合按画面检索的标签 |
| 14 | 内容标签 | 适合按语义检索的标签 |
| 15 | 可用场景 | 未来剧本中可使用的位置和用途 |
| 16 | 限制条件 | 人物露脸、品牌字样等不可替换或需注意内容 |
| 17 | 叙事功能 | 钩子、铺垫、冲突、过程、高潮、结果、收尾、氛围等 |

### 9.3 用户素材片段的 5 个布尔判定维度

- 真人出镜；
- 真人出镜口播；
- 有 BGM；
- 有人声；
- 模特人脸出镜。

### 9.4 用户素材片段的运行时资产字段

这些字段不是再次理解画面，而是将理解结果变成可追踪的素材资产：

- 片段 ID；
- 源视频；
- 源文件；
- 召回文本；
- 素材 ID；
- 召回版本；
- 原始来源 URL；
- 分析链路。

召回文本由片段类型、画面、主体、动作、场景、情绪、视觉标签、内容标签、可用场景和叙事功能拼成，见 `analyze_materials.py:222`。

### 9.5 用户素材整条视频的 7 个整体维度

| 字段 | 实际分析内容 |
|---|---|
| 素材概要 | 这条用户素材整体包含什么内容 |
| 主体标签 | 整条视频出现的主体类别 |
| 场景标签 | 整条视频出现的场景类别 |
| 动作标签 | 整条视频包含的动作类别 |
| 情绪标签 | 整条视频的情绪和氛围类别 |
| 适合内容 | 适合复用到哪些内容或剧本 |
| 不适合内容 | 不适合复用到哪些内容或剧本 |

整体字段定义见 `analyze_materials.py:46`。

### 9.6 参考视频的语义单元

参考视频没有单独的用户素材式 `片段` schema。

当前以“分镜”同时承担以下语义：

- 时间单元；
- 画面单元；
- 主体单元；
- 动作单元；
- 场景单元；
- 音频单元；
- 叙事功能单元；
- 后续脚本复刻单元。

## 10. L6：分镜与用户素材之间的匹配层

这一层不是单条视频内部的理解，而是“参考/目标分镜”和“用户素材片段”之间的关系分析。

### 10.1 匹配时实际分析的维度

当前匹配 Prompt 实际比较：

1. 动作是否匹配；
2. 叙事功能是否匹配；
3. 商品是否清晰可见；
4. 景别是否接近；
5. 机位是否接近；
6. 运镜是否接近；
7. 场景氛围是否接近；
8. 片段时长是否够用；
9. 画面是否清晰稳定；
10. 画面是否与目标台词冲突；
11. 素材原口播与新台词是否表达同一件事。

匹配规则见 `pipeline.py:813`。

### 10.2 匹配结果字段

每个目标分镜输出：

- 匹配到的片段 ID；
- 匹配度，范围为 0 到 1；
- 画面匹配；
- 口播内容匹配；
- 匹配理由；
- 后续处理策略。

匹配结果 schema 见 `pipeline.py:833`。

### 10.3 匹配度对应的处理结果

- 匹配度大于等于 0.80：直接裁剪用户素材；
- 匹配度大于等于 0.55 且小于 0.80：素材编辑；
- 匹配度低于 0.55：重新生成。

阈值和处理策略见 `rules.py:83`。

### 10.4 画面和声音的独立判断

当前实际采用以下口径：

- 画面能表达目标镜头，就可以使用；
- 原素材口播内容不一致，不会自动否决画面；
- 口播不匹配时，静音并重新配音；
- 口播匹配且没有 BGM 时，可以保留原声；
- 口播匹配且有 BGM 时，可以分离 BGM、保留人声；
- 画面匹配是素材是否可用的主要门槛。

相关规则见 `rules.py:25`。

## 11. L7：整条参考视频的叙事、结构与传播维度

参考视频当前实际分析 12 个整体语义维度和 2 个音轨事实维度。

### 11.1 12 个整体语义维度

| 序号 | 字段 | 实际分析内容 |
|---:|---|---|
| 1 | 一句话概要 | 一句话说明整条视频讲了什么 |
| 2 | 核心创意 | 核心点子、矛盾、反差、情绪杠杆 |
| 3 | 开场钩子 | 开头 3 秒如何抓住注意力 |
| 4 | 结构 | 分段、时间区间和每段作用 |
| 5 | 叙事技巧 | 悬念、误导、身份反差、重复递进、Call back 等 |
| 6 | 音乐 | 音乐风格、配器、情绪、入点、切点、卡点 |
| 7 | 声音设计 | 音效与人声的处理特点 |
| 8 | 情绪曲线 | 按时间采样的情绪和强度 |
| 9 | 节奏 | 总时长、镜头数、平均时长、快慢分布 |
| 10 | 镜头骨架 | 可复用的镜头序列模板 |
| 11 | 可复用模板 | 可保留的骨架和可替换的元素 |
| 12 | 产品植入 | 产品或卖点如何融入视频 |

整体字段定义见 `analyze_script.py:44`，整体分析要求见 `analyze_script.py:90`。

### 11.2 2 个整体音轨事实维度

- 有背景音乐；
- 有人声口播。

这两个字段用于决定参考视频音频采用：

- 不使用参考片音频；
- 整轨复用；
- 分离伴奏；
- 使用用户上传音乐。

规则见 `rules.py:96`。

## 12. 商品理解层：由用户视频派生的产品维度

商品理解不是独立的视频分镜分析，但当前会从用户素材片段和抽取帧中实际得到商品相关结果，因此单独列出。

### 12.1 商品候选片段选择

系统从用户素材片段中分析并选择：

- 候选片段 ID；
- 抽帧绝对时间；
- 选择理由；
- 源视频；
- 抽取后的帧文件。

候选片段选择逻辑见 `pipeline.py:545`。

选择依据包括：

- 商品露出是否完整；
- 商品是否清晰；
- 是否为特写或近景；
- 商品是否居中；
- 是否被手或其他物体遮挡；
- 商品在画面中的占比；
- 光线是否适合看清外观。

### 12.2 商品抽取帧判定

对抽取帧实际分析：

- 商品是什么；
- 是否清晰；
- 存在什么问题；
- 商品外观；
- 需要生成哪些白底图角度或状态；
- 从画面能看出的卖点；
- 抽取帧文件。

判定字段见 `pipeline.py:561`。

### 12.3 商品事实卡

当前商品事实卡实际包含：

- 商品名称；
- 商品品类；
- 卖点；
- 一句话广告语；
- 目标人群；
- 商品外观；
- 典型使用场景；
- 哪些字段是推断得到的；
- 哪些字段由用户提供；
- 商品参考图；
- 商品图 URL；
- 商品图来源；
- 商品图补齐过程；
- 抽帧判定结果；
- 候选片段及选择理由。

事实卡 Prompt 定义见 `pipeline.py:601`，实际产物示例为 `output/tasks/20260814-113436-4972/product/fact_card.json`。

## 13. L8：任务内多视频素材索引与来源追踪

当前已经实现的是一个任务内的多视频聚合，不是跨任务画像。

### 13.1 任务内聚合维度

- 用户视频数量；
- 每条视频的素材 ID；
- 每条视频的文件名；
- 每条视频的总时长；
- 每条视频的整体分析；
- 所有片段的集合；
- 每个片段的片段 ID；
- 每个片段的源文件；
- 每个片段的原始时间范围；
- 每个片段的召回文本；
- 每个片段最终被哪个分镜使用；
- 每个分镜最终采用的处理方式。

主流程会把多个用户视频的片段合并到 `material_index.json`，见 `pipeline.py:737`。

### 13.2 任务内音色来源追踪

当前还会记录：

- 音色基准来自哪个片段；
- 音色基准来自用户原声、静音用户片段还是 AI 片段；
- 是否做过 BGM 分离；
- 分离方法；
- 平均响度；
- 是否通过音色门禁；
- 候选片段为什么被弃用。

这些结果写入 `voice_plan.json`，核心逻辑见 `pipeline.py:1525`。

## 14. 当前视频理解结果的最终归类

把当前已经做的内容归并后，实际视频理解维度可以归纳成以下 8 类：

### 第一层：媒体事实

- 时长；
- 音轨存在性；
- 音频抽取；
- 抽帧；
- 时间轴；
- 来源和 ID。

### 第二层：画面与声音事实

- 画面；
- 构图；
- 光线；
- 色调；
- 画质描述；
- 人声；
- 口播；
- BGM；
- 音效；
- 环境音；
- 人脸可见性；
- 商品帧清晰度。

### 第三层：对象与场景

- 人物；
- 商品；
- 道具；
- 动物；
- 主体外观；
- 产品外观；
- 场景地点；
- 环境；
- 时间和天气；
- 文字、品牌和不可替换内容。

### 第四层：动作与事件

- 动作；
- 动作起点；
- 动作过程；
- 动作结果；
- 产品使用；
- 人物状态；
- 人物情绪；
- 事件叙事作用；
- 台词和口播语义。

### 第五层：镜头与视听编辑

- 时间范围；
- 时长；
- 景别；
- 机位；
- 运镜；
- 构图；
- 转场；
- 特效；
- 台词落点；
- 音乐和音效变化；
- 镜头叙事功能。

### 第六层：语义片段与素材可用性

- 片段类型；
- 视觉标签；
- 内容标签；
- 可用场景；
- 限制条件；
- 召回文本；
- 片段来源；
- 产品可见性；
- 画面匹配；
- 口播匹配；
- 匹配度；
- 直接裁剪、素材编辑或重新生成。

### 第七层：叙事结构与传播表达

- 一句话概要；
- 核心创意；
- 开场钩子；
- 结构；
- 叙事技巧；
- 情绪曲线；
- 节奏；
- 镜头骨架；
- 可复用模板；
- 产品植入；
- 音乐；
- 声音设计。

### 第八层：任务级资产关系

- 多视频素材索引；
- 素材 ID；
- 片段 ID；
- 来源时间窗口；
- 分镜到片段的映射；
- 音色基准来源；
- 商品参考图来源；
- 商品事实的用户提供/模型推断来源。

## 15. 用户视频与参考视频的实际侧重点

| 对象 | 当前实际理解重点 |
|---|---|
| 用户素材视频 | 这段素材是什么、包含什么动作、是否有人/产品/声音、适合放在哪里、有什么限制、能否直接复用 |
| 参考/爆款视频 | 每个分镜怎么拍、怎么剪、如何配音配乐、在叙事中承担什么作用、整条视频的结构和传播骨架是什么 |
| 两者关系 | 目标分镜能否由用户片段表达，画面是否匹配，口播是否匹配，最终应该裁剪、编辑还是重新生成 |

## 16. 报告层级终点

本报告的最高层级是当前已经落地的“任务内多视频素材索引与来源追踪”。
