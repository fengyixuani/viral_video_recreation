
# 通用爆款复刻 Agent 设计文档

版本：v0.1
适用范围：ViralForge 短视频爆款复刻与商品营销视频生成
目标形态：由一个具备状态管理、工具调用、人工确认点和失败恢复能力的多模态 Agent，完成“参考视频 + 商品信息 + 用户素材 → 可发布成片”。

## 1. 背景与目标

用户希望将一条已经验证过的爆款视频，复刻成适合自己商品的新视频。复刻并不是简单地让模型“照着写一段提示词”，而是要同时保持参考片的传播结构、节奏和镜头语言，并把人物、商品、场景、素材和声音替换到新内容中。

Agent 需要支持：

1. 用户澄清复刻参数：
   - 是否复刻 BGM，默认是；
   - 是否复刻参考视频原声，默认否；
   - 是否复刻字幕，默认否；
   - 复刻强度：`严格复刻` 或 `创意仿写`。
2. 用户上传：
   - 爆款参考视频，必选；
   - 商品图、商品名称、卖点，至少提供其中足以识别商品的信息；
   - 用户自己的素材视频，可选；
   - 可扩展为商品音频、品牌 Logo、字体、字幕文案等。
3. 在创意仿写模式下，先生成剧本，再为每个分镜优先挑选用户已有素材片段；素材不足时才使用图像/视频生成能力补齐。
4. 在严格复刻模式下，遵守参考视频的镜头数量、镜头时长比例、景别、运镜、转场和叙事功能，只替换人物、商品和场景，不擅自重写故事结构。
5. 输出可追溯的中间产物：
   - 参考视频分析；
   - 商品与素材理解结果；
   - 复刻参数；
   - 剧本和分镜；
   - 素材匹配结果；
   - 生成任务与失败原因；
   - 成片质量报告。

非目标：

- 不承诺复制原视频中的演员肖像、品牌标识、受版权保护的音乐或台词；
- 不把“爆款”理解为保证播放量；
- 不在缺少商品真实性依据时自动编造功效、检测数据或合规背书。

## 2. 核心概念

### 2.1 两种复刻模式

`strict`（严格复刻）：

- 保留参考视频的镜头骨架；
- 每个新分镜必须关联一个参考分镜；
- 默认保持镜头数量、时长比例、景别、运镜、转场和叙事作用；
- 只替换人物、商品、场景及与商品相关的台词；
- 不进行创意扩写，不新增核心剧情。

`creative`（创意仿写）：

- 保留参考片的传播机制，而不是逐帧复制；
- 保留开场钩子类型、节奏曲线、情绪曲线、冲突/反转结构、产品植入位置等；
- 允许大模型重写人物关系、剧情和台词；
- 仍要求每个新分镜记录 `对应参考镜` 和 `复刻说明`，保证结果可解释。

### 2.2 资产类型

统一把输入和中间产物称为 Asset：

- `reference_video`：爆款参考视频；
- `product_image`：商品图或包装图；
- `user_video`：用户上传的素材视频；
- `user_audio`：用户上传的原声、配音或音乐；
- `generated_image`：人物/场景/商品补充图；
- `generated_video`：模型生成的视频片段；
- `subtitle_track`：字幕或花字轨道；
- `logo`：品牌资产。

每个 Asset 都必须有稳定的 `asset_id`、本地路径或对象存储地址、媒体元数据、来源、授权状态和理解结果。模型提示词中使用 `@图片N`、`@视频N`、`@音频N` 等语义引用，不直接暴露底层 URL 或无语义 ID。

## 3. 总体架构

```text
用户
  │
  ▼
任务创建 / 参数澄清
  │
  ▼
输入校验与资产入库
  │
  ├── 参考视频理解 ──► 参考片结构模板
  ├── 商品理解 ──────► 商品事实卡
  └── 用户素材理解 ──► 素材索引与可用片段库
                              │
                              ▼
                    复刻策略规划器
                    ┌─────────┴─────────┐
                    ▼                   ▼
              严格复刻规划          创意仿写剧本
                    └─────────┬─────────┘
                              ▼
                        分镜资产编排
             优先用户片段 → 用户图片/音频 → 生成资产
                              │
                              ▼
                    片段生成 / 剪辑合成
                              │
                              ▼
                       自动质检与预览
                              │
                       人工确认/返工
                              ▼
                            成片
```

建议将 Agent 本身做成“规划与调度层”，把耗时或确定性操作放进工具中。Agent 不直接承担视频转码、上传下载和长任务轮询。

## 4. Agent 工作流与状态机

### 4.1 状态

```text
DRAFT
  → WAITING_FOR_CLARIFICATION
  → WAITING_FOR_UPLOAD
  → INGESTING
  → UNDERSTANDING
  → WAITING_FOR_PLAN_CONFIRMATION
  → WRITING_SCRIPT
  → MATCHING_ASSETS
  → WAITING_FOR_SCRIPT_CONFIRMATION
  → GENERATING
  → COMPOSITING
  → QUALITY_CHECKING
  → WAITING_FOR_REVIEW
  → COMPLETED
```

任意可重试状态都可以进入 `RETRYING`；不可恢复错误进入 `FAILED`，并保存具体的 `error_code`、失败工具、输入和建议动作。每个步骤使用幂等的 `step_id` 和缓存，避免重试时重复产生费用。

### 4.2 参数澄清

首次创建任务时，Agent 只询问缺失或未确认的参数。默认值如下：

```json
{
  "copy_bgm": true,
  "copy_reference_audio": false,
  "copy_subtitles": false,
  "rewrite_mode": "strict",
  "use_user_materials": true,
  "output": {
    "aspect_ratio": "9:16",
    "resolution": "720x1280",
    "language": "zh-CN"
  }
}
```

参数语义必须区分：

- `copy_bgm=true`：复刻参考片的背景音乐，具体行为取决于参考片音轨的实际内容：
  - 只有 BGM、没有口播 → 整条音轨直接复用；
  - 既有 BGM 又有口播 → 先分离出伴奏，只把 BGM 贴回来；
  - 没有 BGM（有无口播都一样）→ 完全不使用参考片音频，保留生成音；
    用户上传了独立 BGM 时优先用用户的；
- `copy_reference_audio=true`：复刻参考片原声中的人声、环境声和音效，是用户的显式覆盖，
  不做上面的分离判断。默认关闭；
- `copy_subtitles=true`：复刻参考片字幕的出现时机、位置和样式。默认关闭，字幕文字应重新生成或由用户确认；
- `rewrite_mode=strict`：只做元素替换；
- `rewrite_mode=creative`：允许大模型改写剧本；
- `use_user_materials=true`：用户有素材时尽可能优先使用，而不是全部重新生成。

涉及原声、字幕、音乐的选项应在界面上提示授权和潜在版权风险，但不阻断用户使用自有或已获授权素材。

### 4.3 人工确认点

建议保留三个确认点：

1. 参数确认：展示默认值和影响；
2. 剧本确认：展示参考片抽象结构、商品事实卡、剧本和分镜；
3. 成片确认：展示成片、质量问题和可重新生成的局部片段。

用户只修改一个分镜时，应从该分镜对应的生成步骤开始局部重跑，不重新分析整条参考视频。

## 5. 输入与输出 Schema

### 5.1 任务输入

```json
{
  "task_id": "task_xxx",
  "reference_video": {"asset_id": "asset_ref_1"},
  "product": {
    "name": "商品名称",
    "selling_points": ["真实卖点1", "真实卖点2"],
    "category": "品类",
    "description": "可选的补充说明"
  },
  "assets": ["asset_product_img_1", "asset_user_video_1"],
  "options": {
    "copy_bgm": true,
    "copy_reference_audio": false,
    "copy_subtitles": false,
    "rewrite_mode": "creative",
    "use_user_materials": true
  }
}
```

商品名称、商品图、卖点都可缺省，但在剧本确认前必须得到一张“商品事实卡”。模型推断的字段标记为 `inferred`，用户提供的字段标记为 `provided`，不可验证的功效标记为 `unverified`。

### 5.2 用户素材片段

```json
{
  "asset_id": "asset_user_video_1",
  "kind": "user_video",
  "duration_sec": 12.4,
  "width": 1080,
  "height": 1920,
  "fps": 30,
  "segments": [
    {
      "segment_id": "asset_user_video_1#seg_03",
      "start_sec": 4.2,
      "end_sec": 7.6,
      "caption": "手持锅铲翻炒",
      "objects": ["锅铲", "不粘锅"],
      "actions": ["翻炒", "手部操作"],
      "scene": "家庭厨房",
      "shot_size": "近景",
      "camera_motion": "固定",
      "quality": {"sharpness": 0.86, "product_visibility": 0.93},
      "embedding": "vector://..."
    }
  ]
}
```

### 5.3 分镜任务

```json
{
  "shot_id": "shot_07",
  "reference_shot_id": "ref_shot_07",
  "duration_sec": 2.8,
  "narrative_function": "产品卖点露出",
  "visual": "商品特写与操作动作",
  "dialogue": "",
  "audio_plan": {"bgm": "keep", "reference_audio": "off", "sfx": "generate"},
  "subtitle_plan": {"mode": "off"},
  "material_plan": {
    "strategy": "user_first",
    "candidates": [
      {"segment_id": "asset_user_video_1#seg_03", "score": 0.91}
    ],
    "selected": "asset_user_video_1#seg_03",
    "fallback": "generate_i2v"
  },
  "generation_plan": {
    "mode": "edit_user_video",
    "prompt": "...",
    "reference_assets": ["asset_product_img_1"]
  }
}
```

### 5.4 最终产物

最终任务记录至少包含：

```json
{
  "status": "completed",
  "script": "script.json",
  "edit_decision": "edit_decision.json",
  "segments": ["segments/seg_01.mp4"],
  "final_video": "final.mp4",
  "quality_report": "quality_report.json",
  "lineage": {
    "reference_video": "asset_ref_1",
    "model_versions": {},
    "prompt_versions": {},
    "user_confirmations": []
  }
}
```

## 6. 各阶段设计

### 6.1 资产入库与预处理

工具完成：

- 文件类型、大小、时长、分辨率、编码校验；
- 病毒扫描、重复文件检测、缩略图生成；
- 上传对象存储并生成短期访问 URL；
- 计算媒体 hash；
- 记录用户授权和来源；
- 对长视频进行低码率代理视频生成。

参考视频必须先生成音视频分离的代理文件、关键帧和波形，便于后续 VLM、音频分析与时间轴校准。

### 6.2 参考视频理解

复用 `analyze_reference.py` 的字段体系，但生产版应拆成多个可重试步骤：

1. 视频元数据：真实时长、画幅、帧率、音轨；
2. 镜头边界检测：优先使用镜头切分模型或 ffmpeg/视觉算法；
3. 关键帧与镜头描述；
4. VLM 逐镜补全人物、动作、场景、台词、音效、转场；
5. 音频分析：BGM、人声、音效、节拍和响度；
6. 整体抽象：钩子、结构、情绪曲线、镜头骨架、产品植入方式；
7. 结构校验：镜头时间轴必须覆盖全片，时长和真实视频误差在阈值内。

现有 `analyze_reference.normalize()` 和真实时长校准逻辑可直接复用。生产链路不能继续使用 `main()` 中硬编码的 `VIDEO`。

### 6.3 商品事实卡

输入可能只有商品图，也可能有名称和卖点。VLM 负责识别外观和使用方式，LLM 负责整理表达，但不得擅自增加无法验证的承诺。

商品事实卡建议包含：

- 基础信息：名称、品类、品牌；
- 可见外观：形状、材质、颜色、结构、包装文字；
- 用户提供的卖点；
- 可从素材验证的卖点；
- 不可验证或需用户确认的卖点；
- 禁止出现的词；
- 适用人群和使用场景；
- 商品一致性约束。

没有商品图时的商品参考图补齐：只要有用户素材，就不该让下游无图可依（无图会导致设定图与
分镜生成全凭文字想象，商品外观完全不可控）。流程是：

1. 挑片段：把素材切片的 `画面`/`主体`/`景别运镜` 给 LLM，让它挑出商品露出最完整、最清晰的
   1-3 个片段，并给出片段内商品最清楚的绝对秒数。不用随机挑 —— 随机极易抽到糊帧或只露一角。
2. 抽帧：ffmpeg 按该秒数抽帧；模型给的时间点不在片段区间内就退回片段中点。
3. 判定：VLM 看这一帧，输出「商品是什么 / 是否清晰 / 什么问题 / 外观 / 需要几张白底图分别表现
   什么 / 画面能看出的卖点」。第一张就清晰就停，不清晰再试下一个候选。
4. 生白底图：以抽帧作参考图走图生图，产出纯白背景商品主图，至少 1 张，最多 3 张。
   需要几张由上一步的判定决定 —— 数码相机给正面/侧面/背面，奶粉给外包装与打开露出粉末。
5. 回填：白底图 + 抽帧一起写进事实卡的 `images`/`image_urls`，白底图排前面，抽帧留一张当
   兜底与人工核对；同时记录 `商品图来源` 与完整的 `商品图补齐` 过程。
6. 名称与卖点：用户没填时，从抽帧判定与素材片段的 `声音台词`（口播里通常直接念商品名和卖点）
   里提取，并在 `inferred_fields` 标明哪些是推断的。

因此步骤顺序必须是「用户素材切片 → 商品事实卡」，事实卡要依赖切片结论才能抽帧。
白底图是生成的，包装文字这类细节可能与真品有偏差，所以原始抽帧必须一起保留、来源必须落盘。

### 6.4 剧本和分镜规划

严格复刻：采用“参考分镜复制 + 元素替换”的确定性模板，LLM 只负责将人物、商品、场景和台词映射到新商品，不能自由增删镜头。

创意仿写：采用“传播结构抽取 + 创意生成 + 约束回填”：

1. 从参考片得到结构模板；
2. 生成 2-3 个创意方向；
3. 选择一个方向；
4. 生成逐镜剧本；
5. 检查钩子、节奏、情绪和卖点覆盖；
6. 关联参考分镜并输出复刻说明；
7. 进入人工确认。

剧本必须将 `audio_plan` 和 `subtitle_plan` 单独建模，不能仅在视频提示词里描述，否则无法稳定控制 BGM、原声和字幕。

设定图的商品一致性：人物/场景设定图会作为参考图进 seedance，所以它们绝不能自己编一个假商品——
编出来的商品会被照抄进成片。流程是「先判定、再图参考」：

1. 判定：一次 LLM 调用批量判断每张设定图的画面里会不会出现这个商品，输入是人物设定、场景设定、
   商品事实卡，以及**分镜的画面与动作**。设定描述常常不提商品（比如人物「外观」只写穿着），
   靠分镜里的同框关系才判得准。
2. 绑定：会出现的，把商品原图放进 `ref_images`，并让同一次调用把提示词改写成在商品出现的位置
   紧跟 `@图1` 锚点，如「手中握着 @图1 的格力锅铲」。提示词开头再声明一句「@图1 是商品原图，
   严格保留其造型、配色与包装文字不变」——参考图生图必须写清「从参考图保留什么」，
   见 `Skills/ImagePromptSkill.md`。
3. 不出现的，走原来的纯文生图，不传参考图。
4. 判定失败时退回关键词兜底（商品名 + 名字尾 2-3 字 + 品类，在设定描述与引用到它的分镜里找），
   再失败就当作不出现商品，退化成改造前的行为，不阻塞出图。

人物设定图即便带了商品参考图，仍必须保留 `FACE_LINE_ART` 线稿脸约束：seedance 拒收含写实人脸的
参考图。商品原图无人脸，不触发这条风控。

### 6.5 用户素材理解与分镜匹配

当 `use_user_materials=true` 且存在用户视频时，先做素材切片和索引：

- 镜头边界；
- 画面描述；
- 商品检测与可见度；
- 人物、动作、场景；
- 景别、机位和运镜；
- 清晰度、曝光、抖动、遮挡；
- 台词/环境音/音乐；
- 片段向量 embedding。

分镜匹配采用“硬约束过滤 + 多目标打分”：

```text
score =
  0.30 * semantic_similarity
+ 0.20 * action_similarity
+ 0.15 * product_visibility
+ 0.12 * scene_similarity
+ 0.08 * shot_size_similarity
+ 0.05 * camera_motion_similarity
+ 0.05 * duration_fit
+ 0.05 * quality_score
```

优先级策略：

1. 能表达该分镜动作和卖点；
2. 商品清晰可见；
3. 画面构图、景别和运镜接近；
4. 时长可裁剪或速度调整；
5. 清晰度和稳定性合格。

若最高分低于阈值，标记为 `uncovered`，进入生成兜底；不要强行使用语义不匹配的素材。一个用户片段默认只能被复用有限次数，除非用户明确允许循环使用。

### 6.6 生成与剪辑决策

每个分镜先由 Edit Planner 决定模式：

- `cut_user_video`：直接裁剪用户视频；
- `retime_user_video`：变速、补帧或调整时长；
- `edit_user_video`：以用户片段为主体，做风格、背景、商品替换或延长；
- `use_user_image`：用户图片配合图生视频；
- `generate_i2v`：使用商品图、人物图或场景图图生视频；
- `generate_t2v`：完全文本生视频；
- `compose_static_product`：产品静物图、字幕、音频和特效由剪辑器合成。

推荐决策顺序：

```text
匹配度 ≥ 0.80       → 直接使用用户片段
0.55 ≤ 匹配度 < .80 → 用户片段编辑/延长/重构
匹配度 < 0.55       → 生成新片段
商品细节镜头         → 商品图优先作为参考
人物连续性镜头       → 角色参考资产优先
```

视频生成提示词必须遵循现有 `Skills/VideoPromptSkill.md` 与 `SeedancePromptSkill.md`：

- 重要素材前置；
- 用 `@图片N`、`@视频N`、`@音频N` 引用；
- 先定义稳定主体；
- 一镜一种运镜；
- 台词用 `{}`、音效用 `<>`、BGM 用 `（）`；
- 多段生成后拼接时，BGM 应使用独立音轨统一混音，不能让每段模型各自生成音乐。

### 6.7 音频与字幕

音频建议独立于视频生成模型处理：

- BGM：先判定参考片音轨里有没有 BGM、有没有口播，再决定整轨复用、只取分离出的伴奏，
  还是完全不用参考片音频；用户上传的 BGM 优先级最高；
- 原声：仅在用户选择且拥有授权时保留/提取；
- 人声：支持原声保留、TTS、用户配音和音色参考；
- 音效：从参考片提取时间点，或使用音效库按事件补齐；
- 混音：统一响度、对白 ducking、起落点和片尾淡出；
- 字幕：关闭时不让生成模型在画面中生成文字；开启时由 ASR/剧本生成字幕轨道，再由剪辑器渲染。
- 字幕不建议依赖视频生成模型直接生成。这样可以避免错字、不可编辑和跨片段样式不一致。
  但生成模型仍可能自带字幕，所以烧制前必须逐段检测画面文字，命中的段跳过叠加。
  走用户素材的段（cut_user_video / edit_user_video）同样要检测：用户素材本身可能带烧死字幕，
  实测 edit_user_video 段就把原片字幕一起继承了下来。
- 逐段检测结果可以缓存，但缓存键必须包含段视频的内容指纹（md5），不能只用段号。
  段视频重新生成后如果复用旧结论，会同时出两种错：该跳过的段被叠字，该叠字的段被跳过。
- 字幕宽度不能交给渲染器兜底：libass 只在空格处自动折行，中文没有空格根本不会折，
  必须自己按画面宽度和字号算每行字数并折行。

### 6.8 合成与质量检查

复用现有 ffmpeg 归一化和拼接逻辑，但增加：

- 片段时间线检查；
- BGM/原声/音效/字幕轨道检查；
- 画幅、安全区和黑边检查；
- 商品 Logo、文字和外观一致性检查；
- 人物身份、服装、发型连续性检查；
- 镜头是否缺失、重复、跳帧、冻结；
- 音画同步和响度检查；
- 敏感内容、品牌侵权风险和未经授权肖像提醒。

质量报告按镜头输出问题，支持：

```json
{
  "overall_score": 0.82,
  "blocking_issues": [],
  "warnings": [
    {"shot_id": "shot_04", "type": "product_visibility", "score": 0.58}
  ],
  "actions": [
    {"shot_id": "shot_04", "action": "regenerate", "reason": "商品被手遮挡"}
  ]
}
```

## 7. Agent 工具清单

### 输入与媒体工具

- `create_task`：创建任务和默认参数；
- `ask_clarification`：返回尚未确认的参数；
- `register_asset`：注册上传素材；
- `inspect_media`：读取媒体元数据；
- `upload_asset`：上传对象存储；
- `extract_audio`：提取音频；
- `extract_keyframes`：提取关键帧；
- `split_shots`：检测镜头边界；
- `crop_media`：按时间范围裁剪素材。

### 理解工具

- `analyze_reference_video`：输出逐镜拆解和整体结构；
- `analyze_audio`：输出 BGM、原声、音效和节奏特征；
- `understand_product`：生成商品事实卡；
- `index_user_video`：切片并建立素材索引；
- `match_assets_to_shots`：为分镜召回并排序素材。

### 创作与生成工具

- `generate_script`：按模式生成剧本；
- `validate_script`：检查镜头、时长、商品事实和模式约束；
- `generate_character_asset`：生成角色一致性资产；
- `generate_scene_asset`：生成场景资产；
- `optimize_video_prompt`：按视频提示词规范优化；
- `cut_user_segment`：裁剪用户片段；
- `edit_video_segment`：编辑/延长用户片段；
- `generate_image`：文生图/图生图；
- `generate_video`：文生视频/图生视频；
- `generate_voice`：TTS 或授权音色生成；
- `generate_music`：生成替代 BGM；
- `generate_sfx`：生成/检索音效。

### 后处理与治理工具

- `mix_audio`：混音；
- `render_subtitles`：渲染字幕；
- `compose_video`：归一化、拼接和导出；
- `quality_check_video`：自动质检；
- `retry_segment`：只重试失败片段；
- `create_preview`：生成低清预览；
- `get_task_status`：查询状态和进度；
- `export_artifacts`：导出成片和中间文件；
- `record_consent`：记录素材授权和用户确认。

## 8. 与当前 ViralForge 的复用和改造

### 可直接复用

- `analyze_reference.py`：VLM 拆解提示词、字段标准化、真实时长校准；
- `write_script.py`：商品图描述、剧本输出结构、人物/场景资产生成、15 秒分段规划；
- `aigc.py`：文本、视觉、Gemini、图像和视频生成统一调用；
- `line_art.py`：人物参考图和视频生成安全降级；
- `storage.py`：本地素材与公网 URL 转换；
- `produce_video.py`：提示词优化、视频片段生成、ffmpeg 归一化和拼接；
- `Skills/`：视频、图像和 Seedance 提示词规范。

### 必须改造

1. 用任务 JSON/API 替代 `analyze_reference.py`、`write_script.py`、`produce_video.py` 的 `main()` 硬编码参数；
2. 新增 Asset Registry，支持商品视频和用户视频，而不只是 `materials/{编号}` 下的商品图；
3. 新增视频切片、VLM 标注和向量索引；
4. 新增严格复刻与创意仿写两个策略分支；
5. 新增分镜-素材匹配和 `edit_user_video` 决策；
6. 将音频、字幕从视频提示词中拆为独立时间轴；
7. 将中间结果写成版本化 manifest，支持断点恢复和局部重跑；
8. 增加自动质检、人工确认和失败重试；
9. 将 `config.env` 中的密钥改为运行时注入并进行密钥轮换；
10. 补齐 `imageio_ffmpeg` 等实际依赖，增加单元测试和端到端测试。

## 9. 推荐的数据目录

```text
output/tasks/{task_id}/
├── task.json
├── assets/
│   ├── registry.json
│   ├── proxy/
│   ├── keyframes/
│   └── user_segments/
├── reference/
│   ├── analysis.json
│   ├── audio_analysis.json
│   └── structure_template.json
├── product/
│   └── fact_card.json
├── script/
│   ├── script.json
│   ├── script.md
│   └── validation.json
├── edit/
│   ├── shot_plan.json
│   └── asset_matches.json
├── generated/
│   ├── images/
│   └── videos/
├── audio/
├── render/
│   ├── normalized/
│   ├── preview.mp4
│   └── final.mp4
└── quality/
    └── quality_report.json
```

## 10. MVP 分阶段实施

### Phase 1：可用闭环

- 任务 JSON 输入；
- 参考视频分析；
- 商品图/名称/卖点理解；
- 严格复刻和创意仿写；
- 复用现有图生视频和拼接；
- BGM、原声、字幕开关；
- 保存 `task.json`、`script.json`、`produce.json`。

### Phase 2：用户素材优先

- 用户视频上传；
- 自动切片和片段描述；
- 分镜-素材召回；
- 直接裁剪、变速、编辑和生成的策略选择；
- 片段级重试和人工替换素材。

### Phase 3：生产级体验

- Web/API 任务队列；
- 实时进度和确认节点；
- 音频独立编排；
- 字幕轨道编辑；
- 自动质量报告；
- 任务缓存、预算控制、模型降级和权限审计。

## 11. 关键验收指标

- 参考视频分析：镜头时间轴覆盖率 ≥ 95%，总时长误差 ≤ 5%；
- 严格复刻：镜头数量和参考镜头映射完整率 100%；
- 创意仿写：钩子、节奏、情绪曲线、产品植入四项均有结构化说明；
- 用户素材优先：可用素材覆盖的分镜中，直接使用或编辑用户素材比例 ≥ 80%；
- 商品一致性：商品外观关键属性识别准确率 ≥ 90%；
- 成片技术质量：无黑帧、无缺失片段、音画同步、画幅正确；
- 可恢复性：单片段失败不影响其他片段，支持从最近成功步骤恢复；
- 成本可控：缓存分析结果，局部重跑不重复调用未变化步骤；
- 合规可追溯：所有外部素材、原声、BGM、人物参考和用户确认均有记录。

## 12. 主要风险与处理

| 风险                  | 处理                                                       |
| --------------------- | ---------------------------------------------------------- |
| 参考片原声/BGM 无授权 | 默认不复制原声；BGM 提示授权，必要时生成相似风格替代       |
| VLM 漏镜头或时长不准  | 先做镜头检测，再用真实媒体时长校准并自动校验               |
| 生成视频商品变形      | 商品图前置、多帧参考、商品关键属性质检，失败则改为静物合成 |
| 用户素材与分镜不匹配  | 低于阈值不强行使用，转编辑或生成兜底                       |
| 多段 BGM 在切点突变   | BGM 独立生成/混音，禁止各段模型分别生成整段音乐            |
| 字幕错字或不可编辑    | ASR/剧本生成字幕轨道，最后由剪辑器统一渲染                 |
| 人物跨镜头不一致      | 角色资产注册、参考图绑定、跨镜一致性检查                   |
| 模型输出 JSON 不稳定  | JSON Schema 校验、修复重试、确定性字段由代码计算           |
| API/模型长任务失败    | 工具幂等、指数退避、断点续跑、模型降级                     |
| 商品卖点虚构          | 商品事实卡标记来源和可信度，未经确认的功效禁止进入成片     |

## 13. 推荐的 Agent 系统提示词原则

Agent 的系统提示词不应直接塞入所有业务细节，而应强调：

1. 先检查输入和参数，再调用工具；
2. 缺少关键输入时只询问必要问题；
3. 严格区分 `strict` 和 `creative`；
4. 用户素材存在时先检索、再决定是否生成；
5. 所有镜头、素材、音频和字幕决策必须可追溯；
6. 不编造商品事实，不擅自复制未授权内容；
7. 长任务通过状态查询工具推进，不重复提交；
8. 生成前给出成本/耗时预估，失败时提供局部重试；
9. 在剧本和最终成片两个节点等待用户确认；
10. 优先输出结构化 JSON，面向用户时再生成自然语言摘要。

## 14. 最终建议

当前代码最适合先抽出一个 `orchestrator` 层，将现有函数包装成工具，再增加 `asset_indexer`、`asset_matcher`、`audio_pipeline` 和 `quality_checker` 四个模块。第一版不需要立刻替换现有模型能力，重点是把硬编码流水线改造成“任务状态 + 资产注册 + 分镜计划 + 可恢复执行”。

最先落地的核心路径应是：

```text
参考视频 + 商品图/名称/卖点 + 可选用户视频
→ 参数确认
→ 参考片分析与商品事实卡
→ 严格复刻/创意仿写剧本
→ 用户素材优先匹配
→ 缺口生成
→ 独立音频/字幕编排
→ 合成与质检
→ 用户确认后导出
```

## 15. 已落地实现（v0.1，Phase 1 + Phase 2 主链路）

### 新增文件

- `pipeline.py`：任务编排层。一个任务 = `output/tasks/{task_id}/`，9 个步骤单独记状态，
  重跑自动跳过已完成步骤（幂等 + 断点续跑）。步骤顺序：
  `ingest → reference → audio → product → materials → script → match → generate → compose`。
- `server.py`：Flask API + 静态前端托管，后台线程跑流水线，前端轮询任务状态。
- `web/index.html`：单页前端（上传素材 / 填商品信息 / 调复刻参数 / 看进度 / 播成片）。
- `write_script.py`：新增 `MODE_RULES`，`write_script()` 与 `build()` 支持
  `mode="strict"|"creative"` 与自定义 `outdir`。
- `Agent_tools/`：外挂工具目录（`tts_clone` 零样本声音克隆、`caption_clone` 字幕风格克隆），
  每个工具自带 README / CLI / `available()` 探活，重依赖隔离在子进程或独立 conda env。
  统一入口 `Agent_tools/registry.py`：挂 sys.path、灌 `MODEL_ROOT` / `VF_GPU`
  （钉推理用哪张卡，子进程继承 `CUDA_VISIBLE_DEVICES`）、`probe()` 探活
  （`step_ingest` 开始时写进任务日志）。模型权重共享根 `MODEL_ROOT=/root/jmzhang/models`
  （VoxCPM2 / Qwen3-ASR-0.6B / Qwen3-ForcedAligner-0.6B），路径都在 `config.env`。

### 步骤与产物

```text
output/tasks/{task_id}/
├── task.json                      参数、步骤状态、日志、成片信息
├── assets/registry.json           入库素材的时长/大小/有无音轨
├── assets/uploads/                前端上传的原始文件
├── assets/material_index.json     用户素材切片与召回文本
├── reference/analysis.{json,md}   爆款拆解
├── audio/reference.m4a            参考片原音轨
├── audio/reference_audio.json     音轨判定结果与 BGM 策略
├── audio/bgm.m4a                  分离出的伴奏（策略=分离伴奏时）
├── audio/voice_plan.json          全片音色基准（来源、策略、判定痕迹）
├── audio/voice_base.wav           基准原声（2~12s pcm_s16le，给 seedance 参考 / 给克隆配音）
├── audio/dub_seg*.wav             静音切片段的克隆配音
├── product/fact_card.json         商品事实卡（区分用户提供/模型推断）
├── script/script.{json,md}        剧本、分镜、≤15s 分段、人物场景设定图
├── edit/asset_matches.json        每个分镜的素材匹配、出片策略与切片处理判定
├── generated/segments/            各段视频
├── generated/clips/               裁剪出的用户素材片段
├── generated/model_ref.json       模特线稿参考图（从已选切片抽帧转线稿，整任务一次）
├── render/stitched.mp4            拼接结果
├── render/with_audio.mp4          音轨处理后
├── render/subtitles.srt           字幕（copy_subtitles 开启时）
├── render/subtitles.ass           实际烧入用的 ASS（含分辨率、字号、边距）
├── render/subtitle_check.json     各段是否自带画面文字的检测结果
└── render/final.mp4               成片
```

### 复刻参数如何生效

- `rewrite_mode=strict`：`MODE_RULES["strict"]` 要求一镜对一镜、不许增删镜头，
  只替换人物/商品/场景与台词中的商品信息；`creative` 允许改写剧本，分镜数可 ±20%。
- `copy_bgm`：按参考片音轨的实际内容分三种情况处理（`step_audio` 判定，`compose` 执行）：
  - **只有 BGM、没有口播** → `策略=整轨复用`，整条参考片音轨作为 BGM 直接混入（原声直取、不压低，与人声 normalize=0 叠加 + 限幅防爆音）；
  - **背景音不是音乐** → `策略=不使用参考片音频`：`gates.check("BGM音乐性")` 实测候选轨（有声占比≥0.5、最长连续有声≥5s、谱平坦度≤0.35，全过才算音乐），只是零星音效/环境声就不复用（判不出按门禁声明的「放行」策略处理，不因判定器挂掉丢 BGM）；
  - **既有 BGM 又有口播** → `策略=分离伴奏`，`separate_bgm()` 抽出伴奏后只贴 BGM；
  - **没有 BGM** → `策略=不使用参考片音频`，无论有没有口播都保留各段生成音；
  - 用户上传了独立 BGM 文件 → `策略=用户上传音乐`，优先级最高。
    判定走音频模型（`aigc.vision_gemini` 直接听音轨），模型不可用时回落到用拆解结果里的
    台词与音乐字段推断。分离用 librosa 的 REPET-SIM 软掩蔽（人声是非重复前景，中值滤波
    估出重复的伴奏后软掩蔽），不需要下载模型；失败回落 ffmpeg 立体声中置抵消。
    贴回时音量由 `VF_BGM_VOLUME` 控制，默认 0.35。
- `copy_reference_audio`：用户的显式覆盖，直接把参考片整条音轨映射成成片音轨
  （`-map 0:v -map 1:a`），不做上面的判定与分离。
- `copy_subtitles`：按各**片**实际时长把剧本台词摊到时间轴，生成 SRT 后用 libass 烧入；
  找不到中文字体时只产出 SRT（可用 `SUBTITLE_FONT` 指定字体）。两条约束：
  - **画面自带字幕的片跳过**：烧字幕前逐片检查有没有叠加字幕
    （`detect_burned_text()`，默认走最快的 qwen VLM，`VF_SUBCHECK_ENGINE=gemini` 可切换；
    结果缓存在 `render/subtitle_check.json`，判定口径变化用 `SUBCHECK_VERSION` 让旧缓存失效），
    命中的片不再叠加我们的字幕，避免双层字幕；这些片的时间轴照常推进。
    检测按片而不是按段：段内混合出片后，一块 AI 补片自带字幕不该让整段都不叠字幕。
    判定口径必须写清楚只算「叠加在画面上的字幕/花字/水印」，商品、包装、吊牌、屏幕里的
    文字不算，否则会把商品手柄上的品牌字样也当成字幕而误跳过整段。
  - **保证不超出画面**：按 `每行字数 = (画面宽 - 左右边距) / 字号` 算预算，
    中日韩字符按 2 个半宽单位、其余按 1 个计宽，`_wrap_cue()` 折行（英文回退到空格断词），
    一屏最多 2 行、超出的按时间切成多条；烧制时自己写 ASS 并把
    `PlayResX/PlayResY` 设成成片实际分辨率，字号与边距才是真实像素。
    字号、边距可用 `VF_SUB_FONT_SIZE` / `VF_SUB_MARGIN_X` / `VF_SUB_MARGIN_V` 调。
- 上述任一音轨开关打开时，段内提示词强制「无bgm」，避免模型自带音乐在切点处突变；
  画面内文字一律禁止，字幕统一由后处理渲染。

### 用户素材优先策略

`analyze_materials.py` 把每条素材切成带「召回文本」的片段，`step_match` 让 LLM 为每个分镜
选片段并打 0-1 匹配度，再由代码按阈值决定：

- ≥0.80 直接裁剪素材（ffmpeg 裁剪 + 不足时最多放慢 1.6 倍补时长）；
- 0.55-0.80 把素材片段作为 `ref_videos` 传给 seedance 做编辑生成；
- <0.55 走原来的图生视频（人物设定图 + 商品图）。

**段内混合出片（用户素材为主、AI 为辅）**：出片单位不是「段」而是段内的「片」
（`_seg_blocks`）——能直接裁的镜头一律裁用户素材，裁不动的**连续**镜头并成一块交给一次
生成，段内再拼回去（`_assemble_segments` 走归一化拼接，两种来源的分辨率/帧率不一样，
不能复制流）。

- 之前是整段 all-or-nothing：一段里只要有一个镜头裁不了，整段都改成 AI 生成，
  实测 8 个能裁的镜头因此全被丢掉、成片 0 帧用户原片；
- 并块是必须的：seedance 最短出 4s，一镜一块会把 0.4s 的镜头也撑成 4s，
  额度和参考片节奏都亏。短块生成后由 `_trim_to` 裁回计划时长；
- 补片的提示词只用这一块自己的镜头（`_sub_seg`），不能照抄段级提示词，
  否则补片会把已经用素材裁掉的画面再演一遍；
- 「片」同时是下游时间轴的单位：字幕时间摊到片（`_pieces` / `_dialog_timeline`），
  自带字幕检测也按片跳过（`detect_burned_text`），一块 AI 补片自带字幕不会让整段
  （含大量用户素材）都不叠字幕。

**拼接的两条硬约束**（混合出片以后才暴露出来，两条都是静默丢内容的坑）：

- **音轨参数必须统一**（`produce_video.aac_args()`：aac / 44100 / 立体声）。concat 是流复制，
  一条流里声道数中途从 mono 变 stereo，后面再重编码这条流时 AAC 编码器会在切换点报
  `Input contains (near) NaN/+-Inf` 而失败——克隆配音出来的片是单声道、seedance 补片是立体声，
  混在一段里就随机踩中。所有会进 concat 的编码点（`_normalize`/`cut_clip`/`_clip_audio`/
  `_paste_dub`/`_trim_to`）统一走 `aac_args()`；
- **归一化失败必须整体报错**：`concat` 以前会把归一化失败的片段悄悄跳过，只在日志里留一句
  「拼接 2/3 段完成」，成片直接少一整段（实测 21.9s vs 计划 36.4s 才发现）。现在有一片失败
  就返回 error，让 compose 直接失败——少一段的成片没有意义。

### 启动方式

```bash
cd /root/jmzhang/baidu/ViralForge
export VF_WEB_TOKEN=<自定义口令>     # 建议设置，见下方安全说明
python3 server.py                    # 默认 0.0.0.0:8420
```

远程开发机访问推荐 SSH 端口转发，不要把端口直接暴露出去：

```bash
ssh -N -L 8420:127.0.0.1:8420 <user>@<开发机>
# 本机浏览器打开 http://127.0.0.1:8420
```

安全说明：`server.py` 默认没有账号体系。设置 `VF_WEB_TOKEN` 后所有 `/api` 请求需要带
`Authorization: Bearer <token>`（前端有输入框，存 localStorage）；不设置就是任何能访问该
端口的人都能建任务、消耗模型额度并下载任务产物。产物下载接口做了任务目录越界校验。

### 已验证

- 三种 BGM 情况都实跑过：纯 BGM 参考片（14_眼睛）→ 整轨复用；纯口播参考片（15_胶原乳液）
  → 不使用参考片音频；人工合成的「BGM+口播」混合轨 → 分离伴奏后再让音频模型复听，
  结果为「有音乐、无口播」。
- 素材入库、素材裁剪与变速对齐、分段拼接、原声替换、SRT 生成与中文字幕烧入（实跑烧入 20 条）、
  素材编辑分支（edit_user_video）、断点续跑、全部 API（含路径穿越拦截与口令校验）。
- 设定图的商品一致性（见 6.4）：格力锅铲这一版实跑，判定为人物 c1 与厨房 s1 出现商品、
  客厅 s2 不出现；出图后逐张核对，c1 手中与 s1 台面上的锅铲在铲头形状、一字镂空、
  不锈钢手柄、手柄「格力」刻字、尾部挂孔上都与商品原图一致，s2 没有凭空出现商品。
  seedream 认 `@图1` 锚点，不会把锚点当文字画进画面。关键词兜底路径单独 mock 测过，
  判定结果与 LLM 一致。
- 字幕检测缓存的内容指纹：重出设定图后重跑 generate，段2（edit_user_video）继承了用户素材的
  烧死字幕，旧缓存却说它没字幕，成片出现叠字；加上 md5 指纹后重跑 compose，段2、3 都被正确
  判为自带字幕并跳过，我们只烧段1 的 4 条，抽帧确认无叠字。

### 后续待补（对应 Phase 3）

- 剧本确认与成片确认目前只有「从某步骤重跑」，还没有分镜级替换素材的交互；
- 自动质检（商品可见度、人物一致性、黑帧检测）尚未实现；
- 生成前的成本与耗时预估、任务队列与预算控制尚未实现。

## 16. 判定策略的分层与接线

策略只写在 `rules.py` 的规则表里，`pipeline.py` 只负责「采集事实 → 调 `decide()` → 按动作名查动作表执行」。
这条分层是硬约束：**改策略只改 `rules.py` 的表行，不碰执行代码**；表里出现的是动作名，
动作名到函数的映射放在 pipeline 的 `*_ACTIONS` 字典里，换实现不动表。

三条不变量：

1. 每张表最后一行是无条件兜底行，`decide()` 找不到兜底行直接抛错，不静默放过；
2. 事实缺失（`None`）只能被通配吃掉，绝不等值命中——漏采集的维度不会静默走进某条规则；
3. 判定痕迹（`规则表`/`命中行`/`事实`）原样写进产物 json，排查时能回答「这一条为什么这么处理」。
   `python3 rules.py` 渲染总表并穷举布尔组合，报出只能落到兜底行的组合，供加字段后人工确认。

### 16.1 六张表的事实来源与执行位置

- **用户切片处理**（`CLIP_GATES` + `CLIP_FIELDS`）
  - 门槛只有 `画面匹配`：`step_match` 让 LLM 逐分镜给出布尔，与 `匹配度` 并列输出。画面匹配从宽判
    —— 主体/商品对得上、能表达外观展示或功能展示、与本镜台词不冲突即算过，允许裁剪与改造。
  - `口播内容匹配`（素材原声与分镜台词语义是否一致）不再是门槛，只参与动作表：说的不是这句就
    静音后重新配音，画面照用。台词是新写的，卡这一条会把全部真素材淘汰成 AIGC。
  - `真人出镜口播`/`有BGM`：`analyze_materials.JUDGE_FIELDS` 的片段级布尔，直接取。
  - 兜底行不是「不使用」而是「静音后使用 + 需要配音」：画面过了门槛就一定要用上这条素材，
    音轨维度判不出来（标注缺失 → None，既不等于 True 也不等于 False）时按最安全的
    「静音 + 重新配音」处理。实测有一轮 12 条 0.75~0.85 匹配度的切片全掉进旧兜底行，
    成片 0 帧用户素材——缺一条标注不该让真素材整条作废；画面对不上仍由门槛判成「不使用」。
  - 动作表 `CLIP_ACTIONS`：`原声直接使用` → 原声保留；`静音后使用` → 音轨换成等长静音
    （用 `anullsrc` 而不是 `-an`，少一条音轨会让整段 concat 拼不上）；
    `分离BGM保留人声` → `separate_bgm(want="vocal")` 抽人声后回贴；`不使用` → 不做直接裁剪。
  - 判定在 `step_match`（每行记 `切片动作`/`需要配音`/`切片判定`，落 `edit/asset_matches.json`），
    执行在 `_cut_segment` → `_clip_audio`。
- **切片缺失补片**（`GAP_FIELDS`）
  - `需要模特出镜`：分镜级事实，`write_script` 的分镜 schema 直接产出布尔，
    `_pack_segments` 按「任一镜要 → 要，全部明确不要 → 不要，否则未判定」汇总到段。
  - `已有模特出镜`：已判为「直接裁剪」的切片的 `模特人脸出镜` 取或（全 false 才是 false）。
  - `多段需要模特`：全片需要模特的段数 > 1。
  - 动作表 `GAP_ACTIONS`（`_gap_refs` 实现）：`AIGC直接生成` → 只给商品图；
    `提取模特改线稿图` → 从已选切片抽帧转线稿（`_model_frame_line_art`，整任务只做一次，
    结果缓存 `generated/model_ref.json`）+ 商品图；`直接生成线稿图` → 复用剧本阶段的
    人物设定图（本身就是线稿脸）+ 商品图；`文字描述生成` → 只给商品图，人物靠
    人物设定的「外观」文字描述。前两种参考图带线稿脸，提示词必须前置
    `line_art.REAL_ACTOR_HINT`，否则线稿脸会被照抄进成片。
- **素材匹配策略**（`MATCH_FIELDS`）：`匹配度` 由 `step_match` 的 LLM 打 0-1 分，
  阈值 `DIRECT_USE_SCORE=0.80` / `EDIT_SCORE=0.55` 决定 直接裁剪 / 素材编辑 / 重新生成。
- **参考片音轨**（`REF_AUDIO_FIELDS`）：`用户上传BGM` 看 `inputs`，
  `参考片有BGM`/`参考片有口播` 来自 `analyze_reference.JUDGE_FIELDS`（模型不可用时回落听音轨）。
  动作表 `REF_AUDIO_ACTIONS` 在 `step_audio` 判定、`_apply_audio` 执行。
- **字幕复刻**（`SUBTITLE_FIELDS`）：`复刻字幕` = `options.copy_subtitles`，
  `有参考片` 看 `inputs.reference_video` 是否存在，`风格克隆可用` =
  `Agent_tools.registry.caption_available()`（指工具可导入；词级 ASR 缺失只是逐字时间退化，
  不算不可用）。动作在 `step_compose` 分支执行：`风格克隆` → `_clone_ref_captions`
  （Agent_tools/caption_clone 复刻参考片字幕风格，items 用 `_dialog_timeline` 的台词时间轴，
  自带字幕的片剔除；失败按表里的「回退」列走基础排版）；`基础排版` → `build_srt` + ASS 烧录。
  两条链路共用 `detect_burned_text` 的逐片检测与同一条台词时间轴，时间口径一致。
  单条字幕流有三条硬约束在 `charstream.build_lines` 出口保证（都是实测踩出来的）：
  ① ASR 命中的句子若绝对时间跑出 plan 窗口（`_overlaps` < 50%）只保留它的**相对节奏**、
  整体压回 plan 窗口——Qwen3-ASR 是生成式的，它会把被裁掉的半句顺回来，整段时间跟着漂
  （实测漂 2.5s，字幕压到下一个镜头上）；② 补漏（gapfill）跳过与 plan 已有句子同源的段
  （`_same_sentence`，含子串与相似度），否则「只对上半句」的句子会被补出第二行；
  ③ 相邻块交叠时裁前一块的尾（`_clip_overlaps`），避免底部同一行位置叠两块字。
- **音色优先级**（`VOICE_PRIORITY`，有序表，不是判定表）：`pick_voice()` 取当前拿得到的
  最高优先级来源作为整片音色基准（铆钉），`voice_strategy()` 给出每个来源该怎么对齐它。

### 16.2 两表之间的优先级

- 切片能不能原样出镜，先过「用户切片处理」的门槛（`画面匹配`）：门槛不过 → 动作 `不使用`，
  这条切片不做直接裁剪；画面够用时（匹配度 ≥0.55）仍可当「素材编辑」的参考视频，
  因为那时它只提供镜头运动，不作为成片画面直接出镜。
- 门槛通过后按匹配度分 直接裁剪 / 素材编辑；`直接裁剪` 的音轨按「用户切片处理」的动作处理。
- 「切片缺失补片」按**块**判：段内裁不动的那几个连续镜头要调生成模型时才判，
  同一段里既有裁剪块也有补片块是正常形态（见「用户素材优先策略」的段内混合）。
- 参考片音轨在成片层（`_apply_audio`）叠加，与段内音色处理互不覆盖：
  `copy_reference_audio` 是用户显式覆盖，一旦开启整轨替换，段内音色策略不再生效。

### 16.3 音色链路

`_voice_plan` 在生成前跑一次，产物 `audio/voice_plan.json`：

1. 可用来源按实际判定结果收集——有「原声直接使用/分离BGM保留人声」的切片 → `用户视频-原声`；
   有要 AI 补片的镜头 → `AI视频片段`；有「静音后使用」的切片 → `用户视频-静音`；
   交给 `rules.pick_voice()` 取优先级最高的一个作为基准；
2. 基准原声（`_voice_source`）先从「能带走原声」的切片里取时长最长的一条；一条都没有时
   退到**素材里任意一条「真人出镜口播」片段**——它的画面可能没被选中、或者被静音后使用，
   但音轨里那把嗓子就是用户本人，克隆音色够用（不这么兜的话，全是「静音后使用」的
   片子只能挂默认 TTS 音色，实测就是这个结果）。连这个标注都缺时再退一层
   `_voice_by_loudness`：用 `声音台词` 标注排掉明确说没人声的片段，按时长从长到短实测
   平均音量（>−40 dB 就算有人声）取第一条——「全片没有配音」是不可接受的结果，
   所以这一层退到可测事实上，而不是直接放弃。抽 2~12s 到 `audio/voice_base.wav`
   （必须是 wav/mp3：seedance 的 r2v 参考音只认这两种，m4a/AAC 会被 41000000 拒掉）
   并上传（上传失败只影响 AI 段的参考声音，本地克隆照常可用），取自哪里记在 `基准来源说明`；
3. AI 补片把它作为 `ref_audios` 传给 seedance（`aigc.gen_video` 支持）；
4. `需要配音` 的片（静音切片拼的）用 `Agent_tools/tts_clone` 零样本克隆基准音色念这一片台词。
   顺序是**先合成、后裁画面**：`_dub_wav` 拿到念白实际时长 → `_dub_scale` 算画面要放长多少倍
   → `_cut_piece(scale=)` 多取素材里没用到的真秒数（素材不够长才由 `cut_clip` 放慢补足）
   → `_paste_dub` 贴回。塞不下时的顺序固定为「先放长画面（≤`DUB_MAX_STRETCH` 1.5 倍）
   再加速念白（≤`DUB_MAX_TEMPO` 1.4 倍）」——多用一秒真素材优于把半句话砍掉；
   短了 `apad` 补静音，长度必须自己 `atrim` 到画面长（`apad` + `-shortest` + `-c:v copy`
   会让 ffmpeg 挂死）。台词取分镜的裸念白，否则 TTS 会把「女主：」也念出来；
   没有基准原声或 TTS 后端未就绪时明确跳过并记原因，不让整段失败；
5. AI 补片逐片验声（`_fill_silent_dub`）：这一片的分镜有台词、但平均音量低于 `SILENT_DB`
   （−45 dB，seedance 时不时返回静音轨甚至没有音轨）→ 同样用克隆音色补配上。
   音色和别的片略有差别可以接受，成片口播凭空断掉一句不行；分镜本来没台词的片保持静音，
   那是快切/空镜的设计；
6. 一条基准原声都拿不到时 `pick_voice()` 兜底 `全部AI配音`。

念白装不下的**根因在剧本层**，不在配音层：LLM 会把长句塞进短镜（实测 25 字 / 1.8s
= 14 字/秒，而克隆音色实测 4.6~5.7 字/秒）。所以 `write_script` 三道口子一起收：

- 提示词按 `时长秒 × SPEECH_RATE`（6.5 字/秒 ≈ TTS 语速 × 1.25 倍加速，也接近爆款主播的
  真实语速）给每镜字数上限；
- `_cut_long_shots` 把超过 `SHOT_MAX`(6s) 的镜切成连续子镜——下游全部按镜工作
  （素材匹配、裁剪、字幕摊时间），参考片里的「一镜到底口播」会被拆成一个 20 多秒的镜，
  拿一整镜去找素材必然一条都对不上（实测这类参考片素材利用率 0，切开后升到 1.0）。
  画面/提示词照抄（本来就是同镜续拍），台词按句分给子镜（句号切不够份数就连逗号一起切），
  时长按分到的字数比例给，**总时长守恒**；没台词的长镜按时长均分；
- LLM 不听话时再由 `_fit_speech` 确定性重分配——把时间从话少的镜挪给话多的镜，
  同样总时长守恒，让出方的下限是「自己也念得完」且不低于 0.4s，仍然装不下的镜记进
  `剧本告警` 交给出片层的加速/放长兜底。

出片层还有两道保证念白不被截断的机制：`_block_needs` 在块内做缺口重分配——某镜的素材短、
`cut_clip` 放慢到 `MAX_SLOWDOWN` 也补不齐它那一份时，把缺口挪给块内还有余料的镜
（块的总长对齐计划才是目的）；裁完再按**实际**长度校正一次，画面还是不够长就带着更大的
放长倍数重裁一遍（`one()` 里的 `cut()`）。

### 16.4 加/删策略的操作口径

- 改判定条件：动 `rules.py` 对应表的行，跑 `python3 rules.py` 看总表和兜底覆盖自检；
- 加一个新动作：表里加行写新动作名 + 在 pipeline 的 `*_ACTIONS` 里补一个函数，两处；
- 加一个新判定维度：先在对应的 `JUDGE_FIELDS`（素材 / 参考片）或分镜 schema 里把事实问出来，
  再加进表的 `字段` 元组。**不允许**在规则层用关键词从自由文本里猜事实——
  `_has_person()` 那种关键词兜底只作为标注缺失时的最坏情况保护，不是事实来源。

### 16.5 实测门禁（`gates.py`）：标注是假设，用前必须实测

规则表解决「事实齐了怎么决策」，但有一整类问题出在更上游——**事实本身不可信**：
VLM 标注「有人声」实测是 -42.6 dB 底噪（4496）、标注「有BGM」实测是零星音效不是音乐（54f7）、
分离出的「伴奏」里残留人声。这类问题的通用范式收敛在 `gates.py`，不允许在管线里散写 if + 魔法数：

1. **标注是假设，不是事实**。标注只配决定「候选顺序」；资产被用进成片前，必须对
   「将要使用的那个文件」跑一次可测指标（响度、有声占比、连续段、谱平坦度……）。
2. **判定进声明表**。每个门禁 = 指标函数 + 阈值 + 判不出策略 + 口径说明，集中在 `gates.GATES`；
   调用方只喊 `gates.check(门禁名, 文件)`，不自带阈值。全项目阈值单一来源
   （`SILENT_DB` / `VOICE_MIN_DB` / `MUSIC_*` 都住这里，pipeline 只留别名）。
3. **报告是标准件**。`{"门禁","通过","指标","判据","原因"}` 原样写进产物 json 留痕；
   谁真用了这份资产就在报告上补 `"使用": true`。
4. **判不出 ≠ 不通过**。探测手段挂了走门禁声明的「判不出」策略——`放行`（宁可维持旧行为
   也不静默丢内容，如 BGM 音乐性）或 `拦下`（来路不明的资产不许上，如克隆基准音）。
   调用方统一用 `gates.ok(report)` 拿最终结论，不自己解释 None。
5. **复核自动对账**。`review_case` 用 `gates.violations()` 扫产物里全部门禁报告，
   「通过=False 且 使用=True」一律记问题——新加的门禁自动被对账覆盖，不用改复核器。

现有门禁三个：`有声内容`（静音线 -45 dB）、`克隆基准音`（≥ -38 dB，判不出拦下）、
`BGM音乐性`（占比/连续段/平坦度三特征，判不出放行）。加一个新门禁 = `GATES` 里加一条声明
（必要时补一个指标函数）+ 调用方一行 `check()`；`python3 gates.py` 生成 `output/gates.md`
门禁清单自检（同 `rules.py` 的通览）。

与 16.0 的分层合起来，一条资产的完整路径是：**标注给出候选 → 门禁实测放行 → 规则表决策 →
动作表执行 → 产物留痕 → 复核对账**，六步各有唯一的家，出问题先问「哪一步没走」。
