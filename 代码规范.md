# 代码规范（本仓库强制项）

> 目的：记录 iCode 扫描工具（机器检查）会报的规范问题，写代码时对照自查，避免提交后被拦下。
> 来源：代码库机器检查实际报出的 PY0xx 规则。遇到新的检查项再往里补。

## PY033（强制）：接口必须写 docstring
- **规则**：`module`、`function`、`class`、`method` 都必须有 docstring，且用**三个双引号** `"""..."""` 括起来。
- **典型遗漏**：
  - 顶层函数 `def main():` 没写 docstring（最常见）
  - 模块（文件）顶部没写 `"""模块说明"""`
  - 私有函数、`class` 内方法漏写
- **正确写法**：
  ```python
  def main():
      """命令行入口：…一句话说明用途和参数。"""
      ...
  ```
- **自查**：每个 `def` / `class` 定义后紧跟着写一行 `"""..."""`。

## PY027（建议）：文件级定义之间隔两个空行
- **规则**：模块里**文件级**的类或函数定义之间要隔**两个空行**；**类内方法**之间隔**一个空行**。
- **典型遗漏**：import 块 / 变量定义后只隔 1 个空行就写 `def`，或两个 `def` 之间只隔 1 个空行。
- **正确写法**：
  ```python
  import os

  def foo():
      ...


  def bar():      # 前面空 2 行
      ...
  ```
- **自查**：每个顶层 `def`/`class` 前保证有 2 个空行（紧贴的注释块不算）。

## 其他易踩点
- **行宽**：单行不超过 100 字符（超了换行）。
- **缩进**：统一 4 空格，不用 Tab。
- **`__pycache__` / `.pyc`**：编译产物不入库，`.gitignore` 忽略，`git add` 时避开。

## 自查命令（本地可先跑，避免提交后返工）
```bash
# 无 pycodestyle 时的简易 ast 检查（模块/函数 docstring + 顶层 def 前两空行 + 行宽）
python3 - <<'PY'
import ast, glob, sys
bad = 0
for f in sorted(glob.glob("**/*.py", recursive=True)):
    if "/__pycache__/" in f:
        continue
    src = open(f, encoding="utf-8").read()
    tree = ast.parse(src)
    lines = src.splitlines()
    if not ast.get_docstring(tree):
        print(f, ": 缺 module docstring"); bad += 1
    for node in ast.walk(tree):
        if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)) \
                and not ast.get_docstring(node):
            print("%s:%d %s 缺 docstring" % (f, node.lineno, node.name)); bad += 1
    for node in tree.body:
        if not isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef, ast.ClassDef)):
            continue
        i = min([d.lineno for d in node.decorator_list] + [node.lineno]) - 2
        while i >= 0 and lines[i].lstrip().startswith("#"):
            i -= 1
        blanks = 0
        while i >= 0 and not lines[i].strip():
            blanks += 1; i -= 1
        if i >= 0 and not blanks == 2:
            print("%s:%d %s 前 %d 空行" % (f, node.lineno, node.name, blanks)); bad += 1
    over = [n for n, l in enumerate(lines, 1) if len(l) > 100]
    if over:
        print("%s 超 100 列：%s" % (f, over)); bad += 1
print("问题数：%d" % bad)
sys.exit(1 if bad else 0)
PY
```
