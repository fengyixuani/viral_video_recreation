# 端到端测试记录

每轮记录：用的参考片、用的用户素材、这轮改了什么、`review_case.py` 的结论。
判定用同一把尺子：`python3 review_case.py <task_id>`，「问题」为空才算通过。

## 案例 A：眼贴（task 20260813-204617-a354）

- 参考片：`videos/others/15_眼部护理_参考_胶原乳液.mp4`（37.1s，21 镜，有口播无 BGM）
- 用户素材：`materials/15/` 全部 17 条（切出 57 个可召回片段）
- 商品图：无（由素材抽帧 + 白底图补齐）
- 参数：strict 复刻、copy_bgm=True、copy_subtitles=True、use_user_materials=True

### 第 1 轮（01:13 → 01:26）

改动：段内混合出片（`_seg_blocks`/`_cut_piece`/`_gen_piece`）、「素材编辑」前提不成立时
降级为直接裁剪（`_demote_edit_rows`）、补片失败回退裁素材。

结果：素材利用率 12/21 = 0.571，成片 36.9s（计划 36.4s），3 段全 `mix_user_ai`。
问题 2 项：配音被截断 3 处（段2块1 1.3s、段2块2 0.6s、段3块3 0.8s）；字幕越过片尾 1 条。

### 第 2 轮（01:36 → 01:50）

改动：先合成念白再裁画面（`_dub_wav`/`_dub_scale`/`_cut_piece(scale=)`/`_paste_dub`，
装不下先放长画面≤1.5 倍再加速念白≤1.4 倍）；`charstream.clamp_to` 丢掉越过片尾的字幕。

结果：利用率仍 0.571，成片 38.7s。画面放长生效：段2块1 1.50×、段2块2 1.25×、段3块3 1.08×。
问题 2 项：片 2#5（镜11 有台词）几乎没声——seedance 返回静音轨；配音仍截断 1 处 0.8s
（镜3 台词 25 字塞进 1.8s，加速+放长的额度用尽）。字幕重复/交叠/越尾全部清零。

### 第 3 轮（02:01 → 02:17）

改动：根因回到剧本层——提示词按 `时长秒 × 6.5 字/秒` 限每镜台词字数，
`_fit_speech` 再做确定性重分配（总时长守恒）；`_fill_silent_dub` 给「有台词却没声」的补片
补克隆配音；`review_case` 只对该说话的片判静音。

结果：新剧本最密 6.7 字/秒（原来 13.9），无需重分配、无剧本告警；利用率升到 13/21 = 0.619。
但成片只有 21.9s——**段2 被静默丢掉了**。查到：`produce_video.concat` 把归一化失败的片段
悄悄跳过（日志只有一句「拼接 2/3 段完成」）；归一化失败的原因是段内流复制拼接后
声道数中途从 mono 变 stereo（克隆配音单声道 + seedance 补片立体声），
重编码时 AAC 编码器报 `Input contains (near) NaN/+-Inf`，且是随机复现。

### 第 4 轮（02:29 → 02:43，通过）

改动：`produce_video.aac_args()` 统一所有进拼接的音轨参数（aac / 44100 / 立体声，
`_normalize`/`cut_clip`/`_clip_audio`/`_paste_dub`/`_trim_to` 全部走它），
`concat` 遇到归一化失败直接返回 error 不再静默丢段；
`ass_burn.build_ass(seq, duration)` 把末块的 TAIL 停留卡进片尾。

结果：**问题 = []**，通过。

- 素材利用率 13/21 = 0.619（用户素材镜 1-5、8-10、12-15、21）
- 成片 36.75s vs 计划 36.4s（偏差 1%），3/3 段拼接，段1 全裁素材、段2/段3 段内混合
- 配音无截断、无需放长画面，8 个片音量 −14.6 ~ −20.9 dB（该说话的片都有声）
- 字幕风格克隆 24 块 / 18 句，无重复、无交叠、不越片尾
- 抽帧目检：用户素材片是本人贴眼贴的真画面，AI 补片是真包装盒特写，
  字幕白字 + 关键词黄字与参考片一致

## 案例 B：格力锅铲 × 眼睛参考片（task 20260812-182504-a149）

- 参考片：`videos/others/14_眼睛_参考_眼睛.mp4`（9.9s，5 镜，无台词）
- 用户素材：2 条（`materials` 外的旧上传：飞机上拍的透明盒子/粉色小花特写），与商品完全无关
- 结论（03:0x）：**问题 = []**。素材利用率 0.0，但 `可出镜素材镜 = 0`——3 个切片画面全都对不上，
  这是正确结果而不是缺陷（review 会区分「没有能用的素材」和「有能用的素材却被淘汰」）。
  成片 10.06s vs 计划 9.9s，全片 AI 生成，1 片音量 −12.0 dB。

## 案例 C：格力锅铲 × 眼部护理参考片（task 20260812-194734-b044）

- 参考片：`videos/others/15_眼部护理_参考_胶原乳液.mp4`（37.1s，21 镜）
- 用户素材：同上 2 条无关素材
- 结论（03:09）：**问题 = []**。素材利用率 0.0（`可出镜素材镜 = 0`，同样属预期），
  成片 36.29s vs 计划 36.2s，字幕风格克隆 14 块 / 11 句，3 片音量 −15 ~ −18 dB。

## 案例 D：眼贴（无商品图变体，task 20260812-234647-bb63）

- 参考片：`videos/others/15_眼部护理_参考_胶原乳液.mp4`
- 用户素材：`materials/15/` 全部 17 条（切出 52 个片段）
- 参数差异：`copy_subtitles=False`（这条变体不复刻字幕）

### 第 1 轮（03:10 → 03:19）

结果：成片 36.25s 正常出片，但素材利用率 0.0。查到**规则表兜底行的问题**：
这批素材标注缺 `真人出镜口播` / `有BGM`（None），而「用户切片处理」表只覆盖 True/False，
12 条 0.75~0.85 匹配度、画面已过门槛的切片全部掉进兜底行「不使用」——
一条标注缺失就把整条真素材丢掉，与「画面照用、声音重配」的表设计自相矛盾。

### 第 2 轮（03:20 → 03:35）

改动：`rules.CLIP_RULES` 兜底行从「不使用」改成「静音后使用 + 需要配音」——
画面过了门槛就一定要用上，音轨维度判不出来时按最安全的「静音 + 重配」处理；
画面对不上仍由门槛拦成「不使用」。

结果：素材利用率 0 → 14/21 = 0.667。但所有裁出来的片都「跳过：没有可克隆的基准原声」——
这批素材索引连 `真人出镜口播` 也全是 None，`_voice_source` 找不到基准音，
于是有台词的片全是静音。

### 第 3 轮（03:36 → 03:51）

改动：`_voice_by_loudness` —— 没有任何切片标了真人口播时，用 `声音台词` 标注排掉明确
说没人声的片段，再按时长从长到短实测平均音量（>−40 dB）取第一条当克隆基准。
实测挑中 `2025-04-23 225510#0005`（17s，−18 dB），标注里正是一整段女声口播，选对了。

结果：配音回来了，但 2 个纯文生视频块被 API 拒：
`41000000 reference_audio cannot be the only reference input`——
这条变体没有商品图，那两块既没有参考图也没有参考视频，参考音成了唯一的参考输入。

### 第 4 轮（03:55 → 04:10，通过）

改动：`_gen_piece` 在发请求前判断——没有参考图/参考视频时不送参考音色（并记日志）；
`_audio_rejected` 同时认这条报错，运行期再被拒也会丢掉参考音重试。

结果：**问题 = []**，通过。

- 素材利用率 14/21 = 0.667（`可出镜素材镜 = 16`）
- 成片 36.9s vs 计划 36.8s，3/3 段拼接，10 个片全部有声（−23 ~ −28 dB）
- 无配音截断、无画面放长；本条变体 `copy_subtitles=False` 故无字幕
- 已知局限：这条任务的商品事实卡是 08-12 跑的，没有商品图（当时还没有「从素材抽帧补齐商品图」），
  所以 AI 补片里的包装是模型编的。现在的代码会自动补齐商品图，重跑 `product` 步即可。

## 案例 E/F/G：videos × materials 配对多轮测试（04:11 起）

同一编号的参考片与素材是同一款商品的真实配对，三轮串行跑（TTS 只能串行）：

- **E**：`videos/others/01_耳塞_参考_博士耳塞.mp4` × `materials/01/` 17 条，商品「耳塞」
  → task `20260814-041137-22d4`
- **F**：`videos/others/05_复古跑鞋_参考_美津浓爆款复古跑鞋.mp4` × `materials/05/` 21 条，
  商品「复古跑鞋」→ task `20260814-041137-c900`
- **G**：`videos/others/12_牛肉饼_参考_牛肉饼.mp4` × `materials/12/` 6 条（小笼包，跨商品配对，
  故意测不完全匹配的情况），商品「小笼包」→ task `20260814-041137-2005`

结果：

- **E 耳塞（04:11 → 04:42）**：**问题 = []**（第一次跑就过）。素材利用率 **18/23 = 0.783**，
  成片 47.32s vs 计划 47.2s，10 个片全部有声（−20 ~ −27 dB），字幕风格克隆 37 块 / 22 句。
  首轮 review 报过一条「字幕重复：马卡龙小花耳塞」，查证是**文案本身在重复**
  （镜1 的长句里有一次、镜4 整句就是商品名），1.25s 和 8.00s 两处相隔很远——
  于是把自检口径改成「同句间隔 < 2.5s 才算烧重了」，隔得远的重复属正常口播。
- **G 小笼包 ×牛肉饼参考片（05:01 → 05:10）**：**问题 = []**。素材利用率 **8/11 = 0.727**，
  成片 15.32s vs 计划 15.1s。参考片只有 BGM 没有口播，整轨复用、剧本无台词故无字幕，
  5 个片音量 −28 ~ −29 dB（BGM 垫底）。跨商品配对（小笼包素材 × 牛肉饼参考片）也能出片。
- **F 复古跑鞋（04:42 → 04:57，第 1 轮不合格 → 改链路后重跑）**：
  第 1 轮素材利用率 0.0，成片 24.22s。根因是参考片本身是**一镜到底 POV 口播**，
  拆解出来就 1 个镜（23.9s）——而下游全部按镜工作：素材匹配拿一整镜去找素材，
  用户 21 条几秒的片段一条都对不上；字幕也摊不开。
  改动（通用）：`write_script._cut_long_shots` 把超过 `SHOT_MAX`(6s) 的镜切成连续子镜，
  画面/提示词照抄（本来就是同镜续拍），台词按句分给子镜、句号切不够份数时连逗号一起切，
  时长按分到的字数比例给且**总时长守恒**；没台词的长镜按时长均分。
  实测这条 23.9s 单镜切成 4 镜（7.9/5.3/3.6/7.1s，各 6.3~6.4 字/秒），正常剧本（案例 A 的 21 镜）
  完全不受影响。

  重跑第 2 轮（05:22 → 05:35）：素材利用率 **0.0 → 1.0**（4 镜全用用户素材、0 镜 AI），
  但报「配音被截断 段1 2.0s」——段1 的镜1 只有 4.0s 素材，`cut_clip` 放慢到 1.6 倍也只有 6.4s，
  整块比计划短 2s，念白加速到上限仍塞不下；而同一块里的镜2 有 9.5s 素材只用了 6.2s。
  改动（通用）：`_block_needs` 在块内做缺口重分配（缺的那几秒挪给还有余料的镜，
  块总长对齐计划即可），并在裁完后按**实际**长度再校正一次放长倍数。

  重跑第 3 轮（05:44 → 05:53）：**问题 = []**，通过。素材利用率 **1.0**，
  成片 24.73s vs 计划 24.0s，段1 14.62s、段2 10.06s（放长 1.07×），无截断，
  字幕风格克隆 12 块 / 4 句。

### 七个案例的最终状态（全部通过）

| 任务 | 参考片 | 素材 | 利用率 | 成片 vs 计划 |
| --- | --- | --- | --- | --- |
| a354 | 15_眼部护理 | materials/15 ×17 | 0.619 | 36.75 / 36.4 |
| a149 | 14_眼睛 | 无关素材 ×2 | 0.0（预期） | 10.06 / 9.9 |
| b044 | 15_眼部护理 | 无关素材 ×2 | 0.0（预期） | 36.29 / 36.2 |
| bb63 | 15_眼部护理 | materials/15 ×17 | 0.667 | 36.90 / 36.8 |
| 22d4 | 01_耳塞 | materials/01 ×17 | 0.783 | 47.32 / 47.2 |
| c900 | 05_复古跑鞋 | materials/05 ×21 | 1.000 | 24.73 / 24.0 |
| 2005 | 12_牛肉饼 | materials/12 ×6 | 0.727 | 15.32 / 15.1 |

回归复跑（05:55 → 06:10）：改完全部链路后把案例 A 从 `generate` 原样重跑一遍，
结果与之前完全一致（利用率 0.619、成片 36.75s、问题 = []），说明改动没有把已经过的案例改坏，
同一条 case 重复跑也稳定。

## 2026-08-14 第X轮：口播定义收紧 + 音色提纯 + BGM 残留修复

**触发**：用户指出 4 点——①「真人出镜口播」应定义为**人物出镜且张嘴说话**；②无人出镜或
出镜没张嘴的片段直接静音后使用；③「用户视频-原声」应指提取素材**纯净人声**（剥离 BGM）再当
克隆/参考基准；④素材无人声时应从**第一个出声的 AI 段**提取干净人声当基准；⑤c900 成片里
在配音之外还能听到一路口播，怀疑「静音后使用」没生效。

**诊断（⑤）**：c900 的段级产物证明用户切片静音是生效的（两条 clip 音轨=静音、配音整轨替换）。
漏进来的那路口播是**参考片的**：参考片「有 BGM + 有口播」走「分离伴奏」，分离器是
librosa REPET-SIM，实测它分出的伴奏里残留人声 **-17.1 dB**（demucs 提取量化），以 35% 音量
垫底后清晰可闻——听感上像"原视频没静音"。

**改动（全部通用方案）**：
- `analyze_materials.py`：「真人出镜口播」标注口径改为**张嘴说话**（嘴部开合、声音是他发出的）；
  出镜不张嘴/旁白配画面 → false → 表一第一行「静音后使用+重新配音」。规则表本身不用改。
- `pipeline.separate_bgm`：优先 demucs（htdemucs，`liveclip_vocal` env 子进程，CPU，权重已缓存），
  回落 REPET-SIM / 声道抵消时返回值带「残留风险」标记，`_bgm_separated` 落进产物、混音日志留痕。
- `_voice_plan` → 新增 `_purify_voice_base`：基准音来源切片带 BGM（或标注缺失）就分离纯人声再当
  基准；分离失败/分离后近乎无声则保留原声并写明原因。
- `_voice_source` 候选加一层「有人声」（旁白/画外音也可借音色），排在真人口播之后、响度兜底之前。
- `step_generate`：基准音为空时先**串行**跑生成块，第一个出声的 AI 段用 `_voice_from_piece`
  提纯人声当全片基准（写 voice_plan + 上传 URL），其余块再并发——落地表二「AI视频片段」语义。

**验证**：
- 残留量化：旧 REPET 伴奏残留人声 -17.1 dB → 新 demucs 伴奏 **-46.3 dB**（低于 SILENT_DB=-45，
  等于消失）；demucs 分离 24s 音频 CPU 仅 8s。
- c900 用 demucs 重分离伴奏后 reset_from(compose) 重跑：completed，24.73s，
  字幕风格克隆 12 块/4 句，`review_case` **问题 = []**，素材利用率 1.0 不变。
  旧产物备份在 `audio/bgm_repet_backup.m4a` / `render/final_repet_backup.mp4`，可对听。

**已知边界**：
- 旧任务的素材标注仍是旧口径（无跨任务缓存，materials 步重跑即按新定义标注）。
- 「AI 首段提基准」逻辑没有端到端跑过（需要一个素材全静音+有 AI 补片的任务），已过语法与
  代码审查，待下一轮 videos×materials 测试覆盖。
- agent 沙箱内无 GPU：qwen3-asr 强制 CUDA，逐字时间戳退化为按 plan 窗口铺字；
  字幕字体 Noto Sans CJK SC 缺失回落 DroidSansFallback。两者在用户终端（有卡/装字体）不受影响。

## 第七轮：videos×materials 同编号配对全覆盖（2026-08-14）

**范围**：先复核 output/tasks 已有 7 任务（a149/b044/bb63/a354/2005/22d4/c900，`review_case`
全部问题 0 项），再补跑剩余 6 组同编号配对（并发 2，批次总耗时 76 分钟）：

| 任务 | 配对 | 素材 | 利用率 | 音色链路 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 4972 | 干发喷雾×02 | 17条 | 1.0 | 素材口播克隆（-24.9dB） | 0 问题（重跑后） |
| 38f1 | 羽衣甘蓝×03 | 34条 | 0.778 | 素材口播克隆（-21.6dB） | 0 问题 |
| 4496 | 项链×04 | 4条 | 0.0 | 无台词，无需配音 | 0 问题（重跑后） |
| 21d0 | 饺子包×06 | 42条 | 1.0 | 素材人声克隆（-25.9dB） | 0 问题 |
| 3810 | 泥膜棒×13 | 36条 | 1.0 | 保留原声切片（-19.1dB） | 0 问题 |
| f25f | 猫眼墨镜×14 | 7条 | 1.0 | 素材口播克隆（-17.3dB） | 0 问题 |

至此 videos/others 全部 10 条参考片的同编号配对均已跑通（01/05/12/15 见前几轮）。

**本轮暴露并修复的两个通用缺陷**：

1. **同片段多镜头复用画面重复**（用户在 4972 发现：seg02_01 是 seg01_01 的前 3.2s）。
   根因：多个分镜命中同一素材片段时 `_cut_piece` 每刀都从片段窗口起点裁。
   修复：新增任务级 `_ClipWindows` 分配器——同一片段每裁一刀游标前移，下一刀取新鲜画面；
   同镜头因配音放长重裁复用原起点只向后延；新鲜画面耗尽才回卷并显式标记「复用回卷」。
   明细新增「源窗口」；`review_case` 新增窗口重叠检测与回卷计数。
   验证：4972 重跑后两刀取 [0,6.2]/[6.2,9.4] 错开、0 问题；3810（新代码首跑）自然复用场景
   实证 [0,1.7]/[1.7,4.6] 错开。
2. **克隆基准音无响度门槛 + 判定与提取口径矛盾**（4496 暴露：标注「有人声」的片段实测
   -42.6 dB 底噪被当克隆参考；且规则判定说「AI视频片段」、实际却取自素材）。
   修复：新增 `VOICE_MIN_DB=-38`，`_voice_plan` 改为候选生成器逐个实测（弃用记录进
   voice_plan「候选弃用」）；「可用来源」事实把素材里口播/有人声片段（只借音色）算进
   「用户视频-原声」，与提取器口径对齐；候选全弃时按修正事实重新判定；
   AI 段提纯基准同用此门槛。
   验证：4496 重跑当场弃用假人声→改判 AI 引导→唯一生成块提纯人声过弱如实放弃→
   该片本无台词，最终无配音、参考片 BGM 垫底，0 问题，判定痕迹完整。

**本轮顺带确认**：
- 「素材里有真人口播」比预扫描更普遍（02/03/06/14 都有），
  「AI 首段提基准」主链路在 4496 走通了引导与如实放弃的分支。
- 4496 利用率 0 属正确判定：参考片是真人佩戴项链实拍，素材全是手链静物，匹配层如实给 0 分。
- 旧代码进程跑出的 38f1/21d0 无片段复用，无需重跑；其 voice_plan 的「来源」标签为旧口径，
  仅观测字段不影响成片。

## 第八轮：BGM 口径两连改 + 剧情参考片 5 组补跑（2026-08-14）

**范围**：补跑 videos/drama/ref_videos 的 5 组配对（materials/07-11 为单张商品图，全 AI 生成），
至此 15 组参考片×素材配对全部跑通；BGM 复刻口径按用户反馈两次收敛。

| 任务 | 配对 | 成片 | 结果 |
| --- | --- | --- | --- |
| 38d6 | 耐克双肩包×07军营 | 44.4s | 0 问题（首跑剧本 JSON 解析瞬态失败，重跑过） |
| 54f7 | 西瓜茉莉果汁茶×08宫斗 | 15.1s | 0 问题（BGM 非音乐翻转不复用） |
| 345f | 公牛插排×09家庭纷争 | — | 0 问题（参考片无 BGM） |
| 089a | 格力锅铲×10武侠 | — | 0 问题 |
| e7e5 | 百雀羚面霜×11穿越 | 76.5s | 0 问题（首跑 kwarg 报错瞬态失败，重跑过） |

**BGM 口径演进（用户两次反馈）**：
1. 「口播时 BGM 偏小」→ 查根因：amix 默认 normalize 把每路再除以 2（名义 35% 实际 17.5%），
   且 demucs 分离出的伴奏本身比原片响度低（bb63 实测 -31.9 dB），固定增益双重衰减。
   先做响度自适应（垫到人声下 11 dB），随后用户定稿口径：**BGM 原声直取、不压低**
   （normalize=0 保两路原响度 + alimiter 防爆音）。9 个 mix_bgm 任务全部重合成，复核 9/9 通过。
2. 「参考片背景音不是音乐就不要复用」→ 新增 `_bgm_is_music` 判定（纯 numpy/ffmpeg，
   不依赖 librosa——本机 numba 缓存崩）：有声占比≥0.5、最长连续有声≥5s、谱平坦度≤0.35
   全过才算音乐；表四新增字段「BGM是音乐」，False → 不使用参考片音频，None（判定器挂）
   按通配放行维持旧行为。回填全部 15 任务：14 个真音乐维持复用，**54f7 宫斗茶命中翻转**
   （占比 0.43、最长连续 3.0s，零星音效），重合成后保留生成音，复核通过。

**其他修复**：
- review_case「字幕重复」误报：38d6 军营口号连喊两镜（剧本设计）被 2.5s 窗口规则误伤。
  修法：归一化后对照剧本台词出现次数，字幕出现不多于剧本的不算烧重。修后 38d6 0 问题，
  bb63/3810 回归无误报。
- 38d6/e7e5 首跑失败均为进程级瞬态（LLM 返回 JSON 缺分隔符 / build() kwarg 异常），
  重跑未复现；kwarg 异常疑似 pyc 缓存边界，留观。

**结论**：15/15 配对 review_case 问题清零；BGM 复刻现行口径 = 参考片背景音是真音乐才带走、
带走就原声直取不压低。

## 案例 H：人物参考图换主角 ×16_蔡徐坤 MV（task 20260814-135417-38c2，2026-08-14）

**目标**：验证「上传一张人物图，把参考片主角替换成这个人」在通用链路上能不能跑通、
成片里这个人**可不可以被识别出来**。参考片 `videos/others/16_蔡徐坤.mp4`（34.1s，2:1 横屏
舞蹈 MV，音轨是 Mr Polska《Jump》，无商品）；人物参考 `materials/16/image.png`（特朗普照片，
属公众人物，是对风控最严苛的用例）。

**新增通用能力（本轮实现）**：
- `pipeline` 新输入位 `person_images`（建任务/登记/入库校验/step_script 全链路，web 上传口
  经 KIND_EXTS 自动生效；老任务缺新输入位由 register_input setdefault 兜底）。
- `write_script`：`describe_persons` 把人物图读成外观描述，剧本 prompt 增加【人物参考】块——
  主角（依次往后）钉死为参考人物，外观按参考写、服装可按剧情重设；
  `gen_assets` 人物按「主角优先」分配，设定图改走「真人照片 → 图生图素描脸（保五官结构）→
  按剧本外观换装」（PERSON_SHEET_PROMPT），失败自动退回原文字线稿路径。
- 配置口径：任务级 `AIGC_ASPECT=16:9`（横屏参考片）；商品按片内道具「复古浅色西装」给，
  `copy_reference_audio=True`（MV 复刻要保住原曲）。

**三轮暴露并修复的通用缺陷**：
1. **无素材时人物设定图被整体丢掉**（第 1 轮：3 段全 t2v 无参考图）。根因：`step_generate`
   把「没有任何选中切片」记成 已有模特出镜=None（未判定），规则表四行全落空掉进兜底
   「AIGC直接生成」。修复：空列表是确定事实 → False，命中「直接生成线稿图」共用设定图。
2. **输出侧内容审核不在降级梯里**（第 1/2 轮各有 1-2 段 80000001
   「output video may be related to …」直接把段跑失败；两轮对比同段时过时不过，证明它随
   生成内容随机波动）。修复：`line_art.is_output_flagged` 单独分类，`gen_video_safe`
   对它「同模式重试一次再降级」，输入侧真人风控仍直接降级；线稿转换本身被拒也降级不再炸整块。
   段错误聚合截断 120→300 字符，别再把审核原因截没。
3. **归一化画布写死 9:16**（第 3 轮成片 16:9 内容被垫成竖屏黑边）。修复：
   `produce_video._canvas()` 从 `config.ASPECT` 推导画布（短边 720），默认竖屏行为不变。

**结果（第 3 轮 + compose 重跑）**：completed，`review_case` **问题 = []**。
成片 34.16s / 1280×720 / 3 段全拼接，音轨整体替换为参考片原声。三段把降级梯全走了一遍：
段2 ref2v（人物设定图直用）、段3 line_art、段1 t2v（重试仍被输出审核拒后如实降级）。
「AI 首段提基准音」主链路端到端走通（段1 提纯人声成基准；段3 用它被拒后自动丢音色重生成）。

**「可以识别出来么」的分层答案**（验证脚本 `output/verify_16_person.py`，
报告 `output/tasks/20260814-135417-38c2/verify_person/report.json`）：
- 拆解层：结构/歌词/运镜/情绪曲线拆得完整，VLM 不输出名人姓名（身份替换本来就由输入决定）。
- 输入风控层：特朗普照片在 seedream 图生图被 `input image may contain sensitive information`
  拒收——**审核识别出了公众人物**，素描脸保身份的路径对政治人物走不通，
  自动退回文字线稿（身份只剩「金发老年偏胖」级别的描述）。
- 成片层：8 帧判定 2 帧「同一个人」（95 分，判定语直接点名特朗普）+1 帧「高度模仿者」（85 分），
  其余帧漂移（白发/瘦削，主要来自无人物锚的 t2v 段1）。整体是「一眼像、细看不稳」的
  仿演者水平；对非公众人物，素描脸路径（同日 sketch_trial 97 分/6 帧全同人）可到强同人水平。

**已知边界**：公众人物照片过不了输入审核是平台约束，链路的正确行为就是当前的降级 + 留痕；
主角同人稳定性受限于「每段独立生成」，跨段人脸一致性没有硬锚点（后续可考虑段间桥接帧带脸）。

## 第九轮：实测判定收敛为「门禁范式」（gates.py，2026-08-14）

背景：第七、八轮陆续暴露的是同一类问题——标注/假设与实际不符（标注有人声实为底噪、
标注有BGM实为音效、分离伴奏残留人声），此前的修法是各处加 if + 各自阈值，属临时调整。
本轮把这类问题的方法收敛成范式并写进代码：

- 新增 `gates.py`：门禁声明表（指标函数 + 阈值 + 判不出策略 + 口径说明）、
  标准报告 schema、`check()/ok()/violations()` 三个入口、`python3 gates.py` 出清单自检。
- 迁移三处散点：克隆基准音响度门槛（_voice_plan / _voice_from_piece）、BGM 音乐性
  （step_audio，原 _bgm_is_music 整体挪入）、静音线常量（SILENT_DB / VOICE_MIN_DB
  唯一来源在 gates，pipeline 只留别名；review_case 裸写的 -45 一并接上）。
- 产物留痕升级：voice_plan.json 记「基准音门禁」（含 使用=true）、弃用候选记完整门禁报告；
  reference_audio.json 的「音乐性」改为标准门禁报告，贴走的 BGM 补 使用=true。
- review_case 新增门禁对账：扫产物里「通过=False 却 使用=True」的报告一律记问题，
  之后新加门禁自动被复核覆盖。

验证：
- 门禁单测：54f7 音效轨拦下（占比0.43）、c900 真音乐放行、4972 基准音 -24.9 dB 放行、
  缺失文件判不出 + 拦下策略 = 不放行、violations() 能抓到「未过却使用」的演练样本。
- 全量回归：19 个任务 review_case 全部通过（0 问题）。

结论：以后同类问题（某类资产用前该不该信）不再写散点 if，一律在 GATES 加声明 +
调用处一行 check()；口径改动只动 gates.py 一处，复核对账自动生效。

## 第十轮：仅遮眼参考图 × Seedance（2026-08-14）

**目标**：验证是否可以不做整脸去五官，只遮挡双眼后直接进入 Seedance。

- 输入：`output/face_lineart_seedance/model_frame2.jpg`
- 参考图：`output/face_lineart_seedance/eye_occlusion_trial_v1/sunglasses/eye_occluded_reference.jpg`
- 处理：按 InsightFace 68 点关键点自动定位双眼，保留其它像素，仅叠加眼部遮挡
- Seedance：单张 `reference_image`，`ref2v`，4 秒，480p，9:16
- 视频：`output/face_lineart_seedance/eye_occlusion_trial_v1/sunglasses/seedance_480p.mp4`

结果：**通过**。无需线稿、双参考图或本地转码；Seedance 直接接受仅遮眼参考图并生成视频。
视频实际时长 4.09s，分辨率 496×864，包含 AAC 音轨；抽帧 6/6 判定为同一人物，
平均人脸一致性 97.0 分。遮挡物在视频中保持稳定，鼻子、嘴巴、脸型、发型、耳饰和服装连续。

## 第十轮：BGM 音乐性门禁上线后全量重合成（2026-08-14）

用户口径确认：参考片背景音不是音乐（零星音效/环境声）就不复用爆款 BGM，且要求通用机制
（第九轮已落 gates.py「BGM音乐性」门禁 + 规则表新行）。本轮把 19 个任务全部用新链路
重判音轨决策并重合成成片（load → step_audio → step_compose → save，2 路并行，0 失败）。

决策结果：17 个不变，2 个翻转（都核实为更正确）：
- 345f 不使用→分离伴奏：上一轮音频模型补问波动漏判了 BGM；这轮实测占比 1.00、
  连续 45.2s、平坦度 0.00，真音乐找回，伴奏重新贴回成片。
- bb63 分离伴奏→不使用：旧 bgm.m4a 是 REPET 时代分离产物，人声残留把间隙填满，
  上轮回填才误判 True；demucs 干净重分离后伴奏真实占比 0.17、最长连续 1.3s，
  属零散非音乐，显式弃用（成片不再垫这条残留轨）。

试错记录（锚点 case 的价值）：曾尝试把噪声门改成随轨响度自适应 min(-40, 均值-15)，
以防"安静但连续的真 BGM 被冤枉"——复测立刻翻掉锚点 54f7（音效轨间隙是 -60dB 环境底噪
而非数字静音，门一放低占比 0.42→0.90 判成音乐）。回退为绝对 -40dB：真安静到 -40 以下的
BGM 原声直取贴回去也听不见，拦下才对。教训写进 silence_profile 文档字符串；
指标新增「噪声门dB」留痕。

验证：16 条候选轨复测 0 翻转（54f7/bb63 = False，其余 14 条 True）；
19 个任务 review_case 全部 0 问题（门禁对账已对新版报告全面生效，产物带 使用 标记）；
19 个 final.mp4 全部为本轮新合成。


## 第十一轮：文案不进画面 + 视觉锚点 + 状态忠实口径（2026-08-18）

case 17（任务 8d76）溯源：img3 屏幕实际显示的是深浅蓝色抽象弧形渐变壁纸（VLM 定向核验），
顶部营销小字「超拟人熊猫萌宠」被链路层层放大成画面事实——事实卡把文案推成卖点（这步本身合理），
剧本把卖点直接写成「屏幕显示超拟人熊猫萌宠壁纸」「熊猫跃出屏幕爆炸」，shot_refs 状态图按剧本
把熊猫 P 上屏幕（PICK_STATE 的编造判负只覆盖「看不见的结构」，屏幕显示内容漏网），验收通过后
生成端全程照抄。另一处连贯性缺陷：镜2 手里的发光软体是绿色、后段屏幕壁纸是蓝色——分段各自
独立生成，没有全局一致性约束。

改动（全部落在约束层，不是补个例）：
- write_script.WRITE_PROMPT 两条硬性要求：商品可见状态（屏幕/表盘/界面显示、指示灯、开合）
  只能写图片描述里实际看得见的，图里没有的显示内容用「屏幕亮起」中性写法；卖点文案宣称的
  功能画面（壁纸角色、界面动效）只能进台词与字幕，不许进「画面」「视频提示词」。
  新增「视觉锚点」输出字段：贯穿多镜的道具/光效/屏幕内容统一定形定色，分镜照抄。
- PRODUCT_IMG_PROMPT / FACT_PROMPT：图片描述与 appearance 只写画面可见；营销文案宣称的
  功能画面不当屏幕显示内容，卖点写成「支持××」宣称，不描述成画面里存在的形象。
- plan_segments + 新 _anchor_text：锚点渲染后随每个分段下发；produce_video._seg_brief 把锚点
  放进提示词重写输入并要求保形保色，重写失败的回退提示词同样带锚点。
- product_images.SHOT_REFS_PROMPT 状态忠实硬口径：屏幕/表盘/界面要显示的具体画面必须在某张
  可用图里真实存在才能锁，素材里没有就记「缺失」不许造；典型情形补「大量可读文字/参数界面」。
  PICK_STATE_PROMPT 编造判负扩展到「屏幕显示原图里不存在的具体画面」。
- pipeline.py 固化「剧本先行」原则注释（write_script 只看参考片与事实卡、不看素材库；素材靠
  裁剪/素材编辑/状态图向剧本靠；轻改剧本只在视频素材丰富时考虑，当前无此通路）。
  Planner 文档 Rule 11 扩展屏幕内容约束，新增 Rule 14「营销文案不是画面事实」。

验证：参考片/素材同 8d76（17_苹果笔记本参考 + 17_小米手机 5 图 1 视频），改后离线重写剧本
（strict）：分镜画面/提示词/动作 0 处熊猫（唯一提及是镜13特效字幕「超拟人萌宠互动」，属允许的
文字通道）；屏幕内容全部锚定 img3 真实的蓝色渐变壁纸；原绿色软体改为蓝色能量球与壁纸呼应；
视觉锚点 6 项、6/6 分段随段下发。锚点管线纯 Python 单测通过（注入/渲染/回退/无锚点不加字段）；
review_case 8d76 仍 0 问题；全部改动文件 py_compile 通过。整链重跑（含状态图与生成）留待下一次
全量任务验证。

## 第十二轮：约束层修复后全量复跑 17/18/19/24（2026-08-18）

第十一轮改动（文案不进画面、视觉锚点、状态忠实口径、剧本先行）落地后，四个用例各起全新任务复跑：

- 17 小米手机（b353）：completed，成片 71.6s，review_case 0 问题，素材利用率 13.6%。
  剧本+分段 0 处熊猫（字样仅存于「删除顶部『超拟人熊猫萌宠…』小字」的编辑指令里，属正确
  通道）；事实卡卖点不再含萌宠壁纸文案；所有屏幕画面锚定 img3 真实的蓝色壁纸；剧本想要的
  「副屏时钟界面」「壁纸动画」素材里没有 → 全部记缺失，0 张凭空状态图；锚点 3 项、6/6 段下发。
- 18 电动牙刷（2272）：首跑段1被 wenchain 输出侧版权风控拒（80000001），链路按「缺段显式失败」
  停下；断点续跑一次通过（随机性输出审核）。成片 31.4s，review_case 0 问题；锚点 1 项
  （含图里可见的绿色指示灯状态）、3/3 段；镜1 要「画面中央特定文字」，素材没有 → 按新加的
  大量文字口径记缺失不硬画。
- 19 女装（071a）：completed，成片 12.1s，review_case 0 问题（无用户视频，全 AI 属预期）；
  锚点抓住贯穿全片的固定花字与遮罩转场人影；换装 11 镜分镜级参考图 37→10→2/3 轮转无混搭；
  无屏幕类画面、无凭空状态图。
- 24 摩托车（73dd）：completed，成片 14.1s，review_case 0 问题；锚点 2 项含车侧真实印字
  （CFMOTO/450 SR/PLAY TO WIN）；appearance 只写画面可见；无状态图需求、缺失空。

遗留观察（未构成缺陷，先记录）：
- 17 镜9 想要「亮屏显示蓝色壁纸」，img3 本来就是该状态，shot_refs 却判成缺失——宁缺勿造的
  方向正确，但「已有图就是这个状态」的匹配还能更准；
- 18 的 80000001 版权拒目前靠断点续跑消化，若高频复现可把它纳入 _gen_piece 的可重试错误类。

### 第十二轮补记：任务目录改用可读命名（2026-08-18）

create_task 新增 slug 参数：CLI 跑用例时目录名 = 日期_用例_参考X生成Y（重名自动 _2/_3），
Web 端建任务仍用「时间戳-哈希」。本轮四个任务目录已改名并重写了产物内的路径引用
（review_case 复核 4/4 仍 0 问题）：
- 20260818-181655-b353 → 20260818_17_参考苹果笔记本生成小米手机
- 20260818-181658-2272 → 20260818_18_参考商业广告耳机生成电动牙刷
- 20260818-181700-071a → 20260818_19_参考卡点换装生成女装
- 20260818-181703-73dd → 20260818_24_参考汽车生成摩托车
历史任务目录保留原名（本文件旧轮次记录按旧 ID 引用）。

## 第十三轮：剧本层商品状态门禁（2026-08-18）

用户口径确认：所有修复必须是能沉淀在代码里的类级方案。自查发现「文案不进画面」在剧本层
只有 prompt 约束、没有验证兜底——模型无视指令时，视频提示词可以绕过状态图门禁直接让
生成端画素材里不存在的内容。补上最后一环：

- write_script 新增 _audit_product_states + SCRIPT_AUDIT_PROMPT：分镜定稿后审一遍
  「素材里看不见却写成画面」（屏幕/表盘显示内容、指示灯、开合、部件、印刷图案、
  文案功能画面），违规就地改写并把原文留痕在 script["商品状态审查"]；审查失败不阻断
  但显式记录。
- pipeline step_script 记日志；review_case 新增对账：未修复>0 或 审查失败 → 问题
  （老任务没有该字段，不误报）。

验证：人造熊猫违规（图片描述只有蓝壁纸、分镜写熊猫壁纸）两次运行均「违规 2 处、改写 2 处、
未修复 0 处」，改写正确落回真实蓝壁纸；真实剧本（b353）审出镜15 屏幕显示素材里没有的
风光照片、镜16 抹掉相机模组两类轻度同类违规并给出可用改写——证明这层验证有真实拦截价值
（这两处正是上一轮成片里的轻度走样）。模型输出格式波动（改写缺失/序号回字符串）已做
prompt 强制 + 类型兼容加固。

## 第十五轮：多款点名删除 + 参考图精简 + 剧本四维审查 + 片段验收 + 阶段并行（2026-08-18）

用户五条需求，全部落在通用层（prompt 规则 / 门禁 / 调度），不是补 case：

1. 多款式同图的编辑指令（product_images.EDIT_PROMPT_PROMPT）：同图商品数≥2 为硬规则，
   必须只留最能代表商品的一款，其余逐件用「方位+颜色或特征」点名删除；唯一例外是这张图
   被选中的用途就是展示多款/多配色（选图角色经 roles → _refine_one 作为用途提示传入）。
   PICK_REFINED_PROMPT 同步：按指令删掉的多余款式不算差异，但留下的那款必须与原图同款一致。
   冒烟（17 图4 左紫右绿双机）：普通用途 →「删除右边绿色手机」只留一台；用途=多配色全览 →
   两台都保留只删营销字。初版软措辞（"多件同框时…"）没触发删款，改成硬规则句式后通过。
2. 参考图精简：新增 rules.PIECE_REF_MAX=4，单个生成块的商品图（含状态图）截断到 4 张，
   无分镜计划的兜底路径同样截断；SHOT_REFS_PROMPT 加「同角度同款只带一张」。
3. 剧本审查从单维扩成四维（write_script.SCRIPT_AUDIT_PROMPT + _audit_product_states）：
   商品状态失实（含配色漂移）/ 商品漂移（品类品牌变了）/ 逻辑硬伤（动作状态自相矛盾）/
   复刻错位（与对应参考镜叙事功能不等价），输入新带参考片分镜简表；违规带类型就地改写。
   冒烟（合成剧本：熊猫屏 + 手机变笔记本 + 二次取出）：违规 6 处、改写 6 处、未修复 0，
   三类新违规全部命中且双字段（画面/视频提示词）都改。
4. 片段验收门禁（segment_build._vet_piece + PIECE_CHECK_PROMPT）：每个生成块下载后
   VLM 验片，只拦四类硬伤（商品走样 / 画面与分镜完全无关 / 大段乱码错字 / 严重崩坏），
   不合格把问题写进提示词重生一次，仍不过取较好一版并按门禁范式留痕
   （门禁=片段验收，通过=False 使用=True 会被 review_case 的 gates.violations 自动对账）。
   判不出/验收接口失败一律放行，不因验片器故障阻断产出。
   冒烟（24 摩托车已出货段1）：判不通过，理由"车身侧面明显乱码错字"——旧成片确实有此病，
   门禁抓的是真问题。
5. 阶段并行（pipeline.STAGES）：ingest → [reference ∥ materials] → [audio ∥ product] →
   script → match → generate → compose。首版把 audio 与 reference 并排，实跑立刻暴露
   audio 要读 reference/analysis.json，改为下一阶段与 product 并行。并行阶段共享同一个
   rec，task_store 新增 _LOCK（RLock）串行化 save/log 与步骤状态写入，防 json.dump
   迭代中 dict 变更。

验证：全模块编译+导入通过；纯逻辑断言（STAGES 与 STEPS 一致、PIECE_REF_MAX 截断、
prompt 占位符渲染干净）；三个 VLM 冒烟如上；用例 24 全流水线复跑验证端到端（含并行调度
与片段验收真实链路），结果见任务 20260818_24_参考汽车生成摩托车_2。

### 第十五轮补记：成片无口播时跳过音色提取（2026-08-18）

用户指出：既然参考片音轨判定已确定「无口播 → 直接复制爆款音轨」，后续就不该再提取音色。
溯源：step_audio 这层是对的（无口播时整轨复用、不做人声分离），问题在 step_generate ——
_voice_plan 无条件跑，会去素材里搜人声、抽 wav、过克隆基准门禁、上传；素材里没嗓子时还会
触发「先串行跑首个生成块提纯人声」的慢路径，把并发生成拖成串行。而音色基准的唯一消费方是
「需要配音的切片」和「有台词的 AI 补片」，全片没有这两样时提了也没人用。

修复（segment_build.step_generate）：新增全片级事实 need_voice =
any(切片需要配音) or any(AI 补片镜头有台词)；为假时跳过 _voice_plan，voice_plan.json 落
「策略=无需口播」留痕，串行提嗓子路径同样受 need_voice 门控。判定锚在下游消费方而不是
「参考片有无口播」——离线用真实任务验证：24 摩托车 / 19 女装（无口播无台词）→ False 跳过；
17 小米手机（参考片同样无口播，但剧本给补片镜头写了台词）→ True 保留音色链路，不误伤。

### 第十五轮补记（修正）：字幕策略改为跟随参考片（2026-08-18）

「默认开」上线后用户指出 creative 17 复刻的苹果广告本就是干净无字画面，成片不该有字幕。
定正确口径：字幕跟随参考片——参考片有字幕/花字才复刻，画面干净的成片保持干净；
用户显式指定时以指定为准。落地：copy_subtitles 改三值（True 强制烧 / False 强制不烧 /
None 默认跟随），rules「字幕复刻」表增加「字幕配置/参考片有字幕」两个事实，
compose 新增 _ref_has_captions 从参考片拆解的分镜特效判定（读不到时落到烧，宁多不静默丢）。
干测四组合全过。creative 17 已显式关字幕重出成片；17_2 与 25 保持用户显式要求的带字幕版，
花字机制保留（参考片有字幕而剧本无台词时的取词回退）。

### 第十五轮补记：字幕默认开 + 无口播片花字（2026-08-18）

用户问成片为什么没字幕。溯源两层：（1）copy_subtitles 默认 False，「字幕复刻」规则表
第一行直接判不烧——默认值改 True，CLI 改 --no-subtitles 反向关闭；（2）17/25 参考片都是
纯 BGM 无口播，strict 剧本台词全空，开了开关也无字可烧。通用修复：剧本分镜 schema 新增
「花字」字段（无口播片每个信息点镜必须给 14 字内文案，有台词的镜留空防双行），
compose 的台词时间轴在台词为空时回退花字。已完成任务用一次 LLM 调用补写花字后
从 compose 重跑：17 号烧入 32 块/16 句（自带字幕的片 5#1 时段自动跳过），25 号 8 块/4 句，
均走参考片风格克隆链路。词级 ASR 因本机无 GPU 失败但不阻断（对齐回退计划窗口）。

### 第十五轮补记：剧本 JSON 解析抖动重试（2026-08-18）

26 号 creative 首跑在「剧本与分段」炸在 json 解析（模型输出第 50 行缺冒号）。通用修复：
write_script 新增 _understand_json —— 问 LLM 要 json，解析失败带着报错原文重问，最多 2 次，
全失败才显式抛错；剧本主调用切换到该包装。离线桩测验证：第一次坏 json、第二次好 json，
重试路径与提示追加均正确。

## 第十四轮：主角商品配色锚定（2026-08-18）

用户反馈 17 成片"手拉蓝色的东西，后面出现绿色的手机，看不出关联"。溯源：
（1）蓝色软胶是参考片核心创意的复刻（软体→铺设备→点亮→捏出→碎片拼成部件），因果在
镜3-6 揭晓，段1 单看是悬念结构，属体裁正常；（2）真缺陷是主角机配色漂移——开场"银灰色"
直接继承自参考片的银灰笔记本（不是从素材配色里选的），结尾又写"森野绿手机"当同一台
主角机，且分镜参考图给的是多配色图（图4紫绿、图5四色），生成端自行选色。视觉锚点没拦住，
因为主角商品本身的配色不是必填锚点。

改动（约束层）：
- WRITE_PROMPT 新增硬性要求：主角商品必须从图片描述里实际存在的配色/款式中选定一款，
  写成第一条视觉锚点，全片统一；不许沿用参考片原产品的颜色；多配色只能作为明确的
  配色展示镜且只用真实存在的配色。
- SCRIPT_AUDIT_PROMPT 输入带上视觉锚点，违规类型新增"配色是图片里不存在的 / 主角商品
  跨镜配色漂移"。

验证（离线重写 17 剧本）：第一锚点 = 小米17 Pro Max（冷烟紫），配色取自素材真实紫色款；
审查门禁真实链路首秀：违规 12 处、改写 12 处、未修复 0 处；原"银灰主角机"消失，绿色只
出现在明确的绿→紫变色桥段。残余观察：镜16"背面恢复成无模组金属板"仍有部件抹除嫌疑，
属门禁概率性漏检（12拦1漏），同一把尺子下一轮继续压。已用新链路复跑 17 全流水线验证端到端。

### 第十三轮补记：违规成因定位（2026-08-18）

b353 剧本被门禁审出的三处走样全部溯源到参考片桥段搬运（strict 一镜对一镜）：
镜1「没有Logo的银灰色手机」抄自参考镜1「没有Logo的银灰色笔记本」；镜15「屏幕播放风光
摄影幻灯片」对应参考镜14「屏幕播放动作电影」（LLM 受徕卡卖点影响替换成摄影内容，但素材里
无此屏幕画面）；镜16「背面纯银平板无装饰」抄自参考镜15「合盖后极简金属平板」，把真实的
副屏+相机模组抹掉了。与熊猫（文案→画面）同类：商品状态没有以商品事实为准，只是来源换成了
参考片。上游已在 WRITE_PROMPT 补一条「参考片对原商品形态的描写不许照搬」，下游由剧本
门禁改写兜底。

### 第十四轮：口播/字幕链路系统性修正 + 重跑产物留档（2026-08-18）

**根因复盘（用户指出）**：正确口径本该由事实链闭环——参考片无人声口播 → 剧本无台词 →
无 TTS 配音 → 无字幕。此前的问题在于：`analyze_reference` 已正确产出事实「有人声口播:
false」，但整条链路无人消费它，规则表里也没有口播的规则；而我上一轮为"补字幕"给全部分镜
硬写花字，属绕开规则表的 bad case 补丁（错误方向），叠加 `_ref_has_captions` 因参考片
结尾标版一镜有字就整片判"有字幕"，导致 17 号成片烧进 18 镜文案。

**系统性修法**：
1. rules.py 新增「口播复刻」规则表：参考片有人声口播=false → 不写台词（纯画面+音效叙事）；
   true/判不出 → 照参考节奏写台词。write_script 消费该判定并落痕（剧本.口播判定），
   另加代码层兜底：判"不写台词"时无论模型输出什么，台词一律清空。
2. 花字改为参考镜驱动：只有参考镜「特效」出现字幕/花字/文案时对应镜才写花字（粒度到镜），
   参考片干净则全空；_ref_brief 的分镜保留字段补上「特效」让剧本模型能看到。
   回滚上一轮"无口播必须补花字"的硬性要求。
3. 重跑不覆盖：pipeline.reset_from 重置步骤时把该步及后续的产物目录（含 report.md）整体
   挪进 history/<时间戳>/ 留档，重跑写全新文件（STEP_DIRS 映射步骤→产物目录）。

**数据修复与验证**：清掉 17_2 / 25 / creative17 剧本里补写的花字（18/4/15 镜），三任务
copy_subtitles 回归 None（跟随参考片）；17_2、25 重出成片确认无字幕无口播，旧版本进
history/。全量审计 36 个任务的「参考口播 vs 剧本台词」全部合规（无口播参考的剧本台词
均为 0，26 号 Prada 参考有口播、正常写台词）。规则表三态自测：false→不写台词，
true/None→写台词。

### 第十五轮补记：字幕"该有却没有"的成因与对账层补齐（2026-08-19）

**用户问题**：`20260818_26_参考prada生成围巾_2`（现已改名 `..._creative`）为什么没有字幕。

**成因**：该任务的参考片 26_prada 事实是「有人声口播 = True」「24 镜里 11 镜特效含字幕花字」，
剧本也照口径写了 11 镜台词——事实链完全支持烧字幕。但任务创建于 CLI 语义从
`--subtitles`（默认关）切到 `--no-subtitles`（默认跟随参考片）之前的那个窗口，
`options.copy_subtitles` 被落盘成显式 `False`，compose 时命中「字幕复刻」表第 0 行
「用户明确关闭」，直接走 不烧字幕。属配置默认值切换留下的存量脏数据，不是链路逻辑错。

**为什么难查**：compose 的 不烧字幕 分支**一句日志都不打**，任务日志里只有音轨行没有字幕行，
事后无法从日志回答"为什么没字幕"。这正是"静默丢内容"的老毛病。

**三处沉淀（都是类级修法，不是补 bad case）**：
1. `compose_video.py`：字幕判定无论走哪个分支都先落一条 `字幕判定：<动作>（<说明>）` 日志，
   不烧也留痕。
2. `review_case.py` 新增字幕事实链对账（只看事实 vs 成片，故意不看配置——配置被显式关掉
   恰恰是要暴露的那类问题）：参考片有字幕 + 剧本有 N 镜文字 + 成片未烧 → 报问题；
   剧本一个字都没有却烧了字幕 → 报问题。
3. 同上文件：ASS 只在 `subtitles.burned` 为真时按 `mode` 读对应那份。此前无条件读
   `capwork/captions_clone.ass`，会把上一轮关字幕重跑后留下的孤儿文件当本轮结果
   （实测 17_creative 因此被误报"剧本 0 字却烧了 26 条"）。

**数据修复**：26 creative 与 26 strict 的 `copy_subtitles` 回归 `None`，从 compose 重跑，
旧产物进 `history/<时间戳>/`。26 creative 结果：风格克隆烧入 17 块 / 11 句（40s），
成片 59.5s，review_case 字幕块 17、无字幕类问题。

**顺带**：两个 creative 任务补上后缀改名（`20260818_18_参考商业广告耳机生成电动牙刷_creative`、
`20260818_26_参考prada生成围巾_creative`），目录内 22 个文本产物里的旧路径一并替换。
全量重跑字幕审计（36 个已出片任务）：现行规则判定与成片实际烧制状态零不一致。

### 第十五轮补记二：字幕统一成普通样式（2026-08-19）

**用户口径**：字幕每个 case 都要统一风格，就是普通字幕，不要强调、不要高亮。

**原状**：`caption_clone` 的风格克隆会把参考片的差异化手段一起复刻——块内关键词按
`WHQ_CAPTION_HOT_SCALE`（默认 1.25）放大并换成参考片的校准高亮色、`role=emphasis/highlight`
的块带斜排/弹入/放大镜等特效。26 号成片的 profile 就是
`{"density": "full", "styles": ["narration", "emphasis", "highlight"]}`。
（字号跳档和整块彩色早前已按用户拍板去掉，剩下的是块内关键词与特效。）

**修法（口径进 rules，实现落工具的既有开关体系）**：
1. `rules.py` 新增 `SUBTITLE_PLAIN = True` 并写清口径：只复刻参考片的字体/字号/正文色，
   不复刻强调手段；想看原味花字改 False 即可（只影响观感，不动时间轴与文本）。
2. `Agent_tools/caption_clone/core/target_match.py` 新增 `plain_mode()`（`WHQ_CAPTION_PLAIN`，
   默认关，不改工具原行为）。开启时 `build_sequence` 里所有块 role 降级成 narration、
   effects 清空、`hot` 标记全部摘掉 —— 位置、斜排、特效、关键词变色放大一次性全关。
3. `compose_video._clone_ref_captions` 把 `rules.SUBTITLE_PLAIN` 翻成
   `WHQ_CAPTION_PLAIN=1 / HOT_SCALE=1.0 / POS=bottom / ANIM=off / REVEAL=block`。
4. 产物里记 `subtitles.普通字幕`：`profile.styles` 是**参考片**被读出来的样式档，
   不代表成片烧了几种样式，显式记一笔免得看报告误会。

**验证**：合成数据单测（emphasis + highlight 两块，各带 keyword 与特效）——
plain=0 时 role=emphasis/highlight、eff=斜排,弹入/放大镜、hot=2；plain=1 时全部
role=narration、eff 空、hot=0，位置都是底部居中。两个 26 号任务从 compose 重跑复核成片。
