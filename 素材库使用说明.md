# 素材库使用说明

素材库根目录是 `output/library/`。将 `素材库清单.example.json` 复制为
`output/library/manifest.json`，再把清单中的 `file` 路径改成实际文件即可。

## 爆款视频库

爆款视频在页面中以扁平列表展示，不显示物理文件夹。每一项用 `title` 提供明确的中文名称，
用 `file` 指向视频文件。视频可直接放在 `output/library/` 根目录。

```json
{
  "title": "XIAOMI 17 Ultra · 夜景样张广告",
  "file": "xiaomi-17-ultra-night.mp4"
}
```

## 暂时下架某个素材

不想让某条素材出现在页面上、但文件还要留着（历史任务按路径引用着它）时，把它写进
`hidden`，不用从 `hot_videos` / `reference_groups` 里删也不用挪文件：

```json
{
  "hidden": ["hot-videos/others/25_色环变装.mp4"]
}
```

路径相对 `output/library/`，也可以写绝对路径。`hidden` 同时按路径和文件 MD5 过滤，所以
爆款库、参考素材包、文件夹扫描、历史任务补进来的那份都不会再出现。想放回来就删掉这一行。

## 参考素材库

参考素材按商品或主题组成一个素材包。每个素材包具有中文 `name` 与可选 `description`；
其中的每个素材都有中文 `title` 和明确的输入类型 `kind`。

页面中点击“一键导入本组素材”后，会将组内素材分别登记到商品图、自有素材、人物图或背景音乐
对应的素材位，不复制文件，也不会修改原素材。

可用的 `kind`：

- `product_images`：商品图
- `user_videos`：自有素材视频
- `person_images`：人物图
- `bgm`：背景音乐
