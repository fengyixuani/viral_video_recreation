# 通用爆款复刻 Agent：规则与链路详解

> 本文回答两个问题：**复刻过程中有哪些判定规则**、**这些规则串在哪条链路的哪一步**。
> 与既有文档的分工：`README.md` 是入口与分层，`通用爆款复刻Agent设计文档.md` 是设计蓝图，
> `系统设计链路详解.md` 是早期链路记录，本文是**以规则为主轴**的横切说明，行号对齐当前代码。
> 规则表与门禁清单可自动渲染核对：`python3 rules.py` → `output/rules.md`，`python3 gates.py` → `output/gates.md`。

---

## 零、一页速览

### 两条链路

| | 主链路 `pipeline.py` | 纯重剪链路 `recut.py` |
|---|---|---|
| 定位 | 换题材复刻：拆爆款 → 改剧本 → 素材+AIGC 出片 | 还原剪辑：爆款本就是从这份素材里剪出来的 |
| 步数 | 九步（ingest…compose） | 九阶段（box…concat） |
| 模型调用 | 重度（VLM 拆解、LLM 写剧本、生图、生视频、TTS） | **零调用**（VLM/AIGC 是可选兜底层，默认关） |
| 成立前提 | 有爆款 + 商品信息 | 两边画面是同一批帧 |
| 定位手段 | LLM 语义打分 | 感知哈希 + 词级 ASR 锚点（像素级事实） |
| 入口 | Web `./start_web.sh` / `python3 pipeline.py <编号>` | `python3 recut.py <爆款> <素材...>` |
| 产物根 | `output/tasks/{task_id}/` | `output/recut/<爆款名>/` |

两条链路互不 import，是并列关系；`recut.py` 只复用 `produce_video` 的 ffmpeg 工具函数。

### 判定分三层，层层不同性质

| 层 | 载体 | 性质 | 判不出来怎么办 |
|---|---|---|---|
| **规则层** | `rules.py` 六张表 + 音色优先级 | 标注驱动的决策：自上而下首条命中，末行兜底 | `None` 只能被通配吃掉，绝不等值命中 |
| **门禁层** | `gates.py` 三个门禁 | 资产进成片前的 ffmpeg 实测 | 按门禁自己声明的「放行/拦下」策略折算 |
| **审查层** | `write_script.py` 两道剧本审查 | LLM 自查 + 代码二次卡关 | 审查失败只记录，**不拦** |

一句话概括三层的哲学：**标注是假设（规则层用它做决策，但允许缺失）→ 用前实测（门禁层验证）→ 结果对账（`review_case.py` 复核「没过门禁却被用了」）**。

---

## 一、主链路：pipeline.py 九步

### 1.1 步骤表与并行阶段

`STEPS`（`pipeline.py:189-191`）定义顺序，`STAGES`（`pipeline.py:197-198`）定义并行分组：

| 阶段 | 步骤 | 中文名 | 落地模块 | 主产物 |
|---|---|---|---|---|
| 1 | ingest | 素材入库校验 | `pipeline.step_ingest` | `assets/registry.json` |
| 2 | reference ∥ materials | 爆款参考片拆解 ∥ 用户素材切片 | `analyze_reference.py` / `analyze_materials.py` | `reference/analysis.json` / `assets/material_index.json` |
| 3 | audio ∥ product | 参考片音频判定 ∥ 商品事实卡 | `ref_audio.py` / `product_facts.py` | `audio/reference_audio.json` / `product/fact_card.json` |
| 4 | script | 剧本与分段 | `write_script.py` | `script/script.json` + 设定图 |
| 5 | match | 分镜素材匹配 | `shot_match.py` | `edit/asset_matches.json` |
| 6 | generate | 分段出片 | `segment_build.py` + `voice_dub.py` | `generated/segments.json` |
| 7 | compose | 拼接与音轨字幕 | `compose_video.py` | `render/final.mp4` |

两处顺序是刻意安排的，改动前必须理解：

1. **materials 排在 product 前**（`pipeline.py:184`）：用户没传商品图时，商品事实卡要靠素材切片的结论去抽商品帧。
2. **剧本先行**（`pipeline.py:185-188`）：`write_script` 只看参考片拆解与商品事实卡，**不看素材库**。先定剧本，再让素材向剧本靠（match 裁剪/编辑、shot_refs 状态图改造）；素材做不到的记「缺失」靠提示词还原，**不反过来迁就素材改剧本**。注释里明确写了：只有素材极丰富时才值得考虑反向调整，目前没有这条通路。

并行的依赖约束：`audio` 要读 `reference/analysis.json`，`product` 要吃 `materials` 的切片结论，所以它们排在下一阶段。并行阶段共享同一个 `rec`，写状态/日志/落盘由 `task_store._LOCK`（`RLock`）串行化。

### 1.2 幂等、断点续跑、留档

- 步骤状态在 `task.json.steps[name]`，`status == "done"` 的步骤重跑时直接打「跳过…复用产物」日志（`pipeline.py:202-205`）。
- `reset_from(task_id, step)`（`pipeline.py:312-338`）清掉该步及其后续状态，旧产物按 `STEP_DIRS` 映射 `os.rename` 进 `history/{YYYYmmdd-HHMMSS}/`，旧 `report.md` 一起留档。**用户要求「重新生成不覆盖之前的结果」**。
- `STEP_DIRS`（`pipeline.py:306-309`）刻意**不含 `assets/uploads/`**：那是任务的输入素材，挪走后 `inputs` 里的路径全指向 `history/`，任务再也读不到自己的素材。
- 每步边界先查一次终止标记（`pipeline.py:206-208`），避免白跑一步、白花模型额度。终止是协作式的：`_CANCELLED` 集合 + 任何一次 `save()` 抛 `TaskCancelled`（Python 线程杀不掉）。
- 无论成败都出 `report.md`（`pipeline._build_report`，`pipeline.py:235-245`）：报告只读已落盘的产物，跑了一半的任务正好靠它定位；报告失败绝不拖垮流水线。

### 1.3 ① ingest 素材入库校验

两条硬校验（`pipeline.py:77-80`）：必须有爆款参考视频；商品名/商品图/用户素材至少有一样。
然后 `_probe` 探测每个素材的时长、是否有音轨、大小，落 `assets/registry.json`；`agent_tools.probe()` 探活三个外挂能力并打一行日志（声音克隆 / 字幕风格克隆 / 词级 ASR）。**探活失败不阻断 ingest**——动机是任务开始时就探一次，别等到最后一步才发现后端没配。

### 1.4 ② reference 爆款参考片拆解

一次 VLM 视频理解拆出 `分镜[]` + `整体{}`（`analyze_reference.py:274`）。
- 分镜 15 个固定字段（`SHOT_FIELDS`）：序号、开始/结束时间、时长秒、景别、运镜、画面、人物、动作、场景、特效、转场、台词、音效音乐、叙事功能。
- 整体 12 字段 + 2 个**判定布尔**（`JUDGE_FIELDS`）：`有背景音乐`、`有人声口播`。这两个布尔是下游两张规则表的事实来源，判不出来必须写 `null`。
- 时间轴修正：起止缺失按时长累加补齐；各镜时长合计与真实时长偏差 > **5%** 才等比缩放，每镜下限 0.1s。
- `转场` 字段是 `write_script._is_hard_cut` 断段的唯一依据，所以别名收敛只收「转入」侧，不收 `转出方式`，否则整份转场错位一镜。转场缺失时单独补问一次 VLM。

### 1.5 ③ audio 参考片音轨判定

先看有没有音频流（读 registry 的 `has_audio`，读不到现场探）——没有就直接把两个布尔置 False，避免 `extract_audio` 抛异常打断整个第三阶段。
有音轨则提整轨 `audio/reference.m4a`（aac 160k），事实优先直接读拆解结果，缺字段才补问音频模型（Gemini 支持音频输入），模型也不可用才退到「按剧本推断」（有台词=有口播，`整体.音乐` 非空=有 BGM，置信度固定 0.5）。
最后走 `rules.decide("参考片音轨")` 并执行动作，结论 + 判定痕迹落 `audio/reference_audio.json`。

### 1.6 ④ materials 用户素材切片

一次 VLM 把每条素材切成语义片段，17 个字段 + **5 个判定布尔**（`真人出镜`、`真人出镜口播`、`有BGM`、`有人声`、`模特人脸出镜`）。
- **传了素材就一定切片，没有开关**（`pipeline.py:118-119`）：素材是用户自己的实拍，比生成的可信；而且没有商品图时商品事实卡还要靠这些片段抽商品帧。
- 「召回文本」`_retrieval_text`：取固定 10 个字段（片段类型/画面/主体/动作/场景/情绪氛围/视觉标签/内容标签/可用场景/叙事功能）按序拼成 `字段：值；…`，供未来接向量检索用；**不含**台词、限制条件、时间字段与布尔。稳定 ID = `<视频名>#<四位序号>`，`召回版本 = 1`。
- 切片粒度是提示词层软约束（按内容完整性切、首尾相接覆盖全片、一个片段一个完整动作）；代码层只校验时间轴可信度：起止必须递增、与上段重叠容差 0.05s、末段不超真实时长 5%，任一不满足就整份按时长重排并把 `时间轴来源` 写成留痕。
- 时间轴为什么必须准：它是下游抽帧点、`shot_match` 的「开始秒」、`cut_clip` 窗口的唯一依据，整体前移就等于抽错帧、裁错画面。
- 多素材并发 `min(3, N)`。

### 1.7 ⑤ product 商品事实卡与商品图链路

事实卡 `product/fact_card.json` 是下游**唯一商品口径**：name / category / selling_points / copy / audience / appearance / usage_scene / images / image_urls，外加商品图的索引、明细、选图结论。用户填的字段永远覆盖模型给的。

商品图链路一共五步，但**分布在两个 pipeline 步骤里**（写文档最容易搞错的点）：

| 步 | 函数 | 在哪一步跑 | 做什么 |
|---|---|---|---|
| 1 | `_index_product_images` | ⑤ product | 每张图 12 字段结构化理解 → `product/image_index.json`，并发 4；单张失败标 `error` 不丢图 |
| 2 | `_select_refs` | ⑤ product | **只读文字不看图**排序选基准图：第 1 张必须是完整全貌且同图只有 1 台商品；同视角只留信息最全一张；多款多配色各留一张；干扰不是落选理由；最多 9 张（`PRODUCT_REF_MAX`） |
| 3 | `_refine_user_images` | ⑤ product | 体检与改造：结构化理解已判定干净（无干扰、占比不小）就**跳过 VLM** 直接用；否则 VLM 写改写指令 → 逐轮生成 → 逐张与原图比对，最多 3 轮（`REFINE_ROUNDS`），每轮 1 张 |
| 4 | `_plan_shot_refs` | ⑧ generate | 按分镜分配参考图 + 判断中间状态图 + 识别装配序列 → `generated/shot_refs.json`（**文件存在即复用**） |
| 5 | `_make_state_images` | ⑧ generate | 生成/查找中间状态图，并发 6 |

关键判定口径：

- **@图片1 必须是完整全貌**：顺序固定（理解→选图→体检）就是为此，否则生成端会照抄局部图的干扰。若首图同框 ≥2 台商品，代码会主动与后面的单款图**换位**并记 `主锚点换位`；同图商品数判不出来按 1 算。
- **体检只能让参考图变好，不能把图弄丢**：判定失败保留原图、无改写指令套通用模板并标 `指令兜底`、3 轮不过保留原图。`可直接用` 判 `None` 也放行。
- **中间状态图不许凭空编造**：状态需要原图看不见的结构，或要求屏幕显示原图不存在的具体画面 → 一律判 `状态达成=false` 并说明是凭空编造。拿不准的填 `状态候选编号`，交给 `_verify_pending_states` **真的把图打开看**（这是整条链路上唯一有资格回答「素材里到底有没有这个状态」的地方），核验失败按未命中处理、本镜退回普通商品图。
- **部件到位清单（manifest）**：`状态达成` = 应有的都在 + 应无的都不在 + 没有清单外部件多出来。应无部件出现（哪怕很淡很小只露一角）就判 false——这是最常见失败，模型爱自动补全。
- **装配序列校验极严，一条不过整条丢**：基准编号必须存在、有效镜头 ≥2、每镜「应有」非空、不能所有镜「应无」都为空、相邻镜「应有」集合必须**单调嵌套**（全程装配或全程拆解，来回变就丢）。宁可少一层创意，不放开一致性门禁。
- **选中即体检**（`_vet_picked`）：分镜级选中但未体检的图要补跑体检，否则那些实拍图常带真人脸与营销贴片，直接下发会被视频模型风控拒、整段掉到纯文生视频。
- **块内只留首尾**（`_collapse_states`）：装配序列整条落在同一生成块时只留起始与结束两张，因为 `PIECE_REF_MAX = 4`，4 步以上会把商品原图全挤掉，而且每张都声明「必须照它画」等于给一次调用四条互斥指令。

无商品图时走 `harvest_product_images`：LLM 挑商品片段（失败兜底取前 3 个）→ 抽帧（`抽帧秒数` 非法就取片段中点，**刻意不落 0.0**，首帧多半是黑场/淡入）→ 判帧清晰度（必须 `as_bool(...) is True`）→ 生白底图（比对失败按 `unknown_ok` 视为过关，因为源帧质量本就一般，判过严会把整条补齐链路清空）。

### 1.8 ⑥ script 剧本与分段

三步：仿写剧本 → 人物/场景设定图 → 动态规划分段。

**rewrite_mode 的差别只有一处**：注入提示词的 `MODE_RULES` 文本块。代码里没有任何按 mode 分支的逻辑（时长校正、两道审查、分段全部与 mode 无关）。

| | `strict` | `creative` |
|---|---|---|
| 定位 | 元素替换任务，不是创作任务 | 复刻的是传播机制，不是画面 |
| 分镜数 | 必须与参考片**完全相同**，一镜对一镜不得增删合并 | 参考片 **±20%** 内浮动 |
| 每镜属性 | 时长/景别/运镜/转场/叙事功能必须与对应参考镜一致 | 不约束 |
| 剧情 | 走向、冲突、反转位置、台词句数与语气节奏都照参考片 | 允许重写人物关系、剧情、台词、场景 |
| 必须保留 | — | 钩子类型、结构段落、节奏快慢分布、情绪曲线走向、产品植入位置 |
| 只许替换 | 人物身份外观、商品道具、场景与时代风格、台词里商品信息 | — |

默认值有一处不一致，容易踩：`DEFAULT_OPTIONS["rewrite_mode"] = "creative"`（Web 默认），但 **CLI 总是显式传 `strict`**，要创意模式得加 `--creative`。未知 mode 值回落 creative。

**设定图的三条人物路径**：有真人参考照 → `to_line_art` 转素描脸再当参考出图（失败退回文字线稿并记 `人物参考回退`）；带商品参考图 → 出图后核验商品一致性；纯文字线稿。场景图提示词强制曝光下限，避免暗场景生成全黑图。
商品一致性核验（`with_check`）：判不一致就把问题写进 avoid hint 重出一次，**仍不过就退回不带商品参考图的 plain 版本**并记 `商品参考图退回`；核验调用失败按通过处理。印刷文字镜像/缺字/乱码，或同画面出现多配色多款式，一律判不一致。

**确定性时长校正链**（在两道审查之前跑）：
1. `_match_total`：总时长与参考片偏差 > **10%** 才等比缩放，每镜下限 0.3s。
2. `_cut_long_shots`：单镜 > `SHOT_MAX = 6.0s` 切成连续子镜。台词先按句号切（停顿符 `。！？；…!?;`），不够份数再连逗号切，仍不够就补空白纯画面子镜；子镜标 `同镜续拍 i/n`，`i>0` 的转场写 `同镜续拍`；序号全片重排并加 `剧本告警`。
3. `_fit_speech`：按 `SPEECH_RATE = 6.5 字/秒` 算每镜台词需要多久，时长不够的镜从有余量的镜借时间（donor 每镜最多让出 50%、且不低于 0.4s），**总时长守恒**；仍差 > 0.3s 的写告警「出片时会靠加速念白补」。
   6.5 字/秒的来历：本地克隆音色实测约 5.2 字/秒，配 1.25 倍加速仍自然。

**分段动态规划**（`plan_segments`）——目标是把分镜打包成 seedance 一次能出的段：

- 硬约束：段时长 `> MAX_SEG = 15.0s` 的转移直接剪掉；切点只能落在镜头边界；单镜本身超 15s 才在镜内切开并标记需要桥接帧。
- 代价函数（越小越好）：
  ```
  cost = 1.0                              # 每段固定惩罚，压总段数
       + 3.0 * (4.0 - dur)   若 dur < 4.0  # 段过短（会被 clamp 到 4s）
       + 8.0                 若段首落在非硬切  # 别在叠化中间断开
       + 8.0                 若段尾落在非硬切
       + 0.3 * (15.0 - dur)                # 尽量填满段
  ```
  8.0 远大于段数惩罚 1.0，意思是「宁可多一段、宁可段短一点，也别在叠化中间断开」。首尾边界不罚。
- 软/硬转场判据：`SOFT_TRANSITIONS` = 叠化、溶解、运镜衔接、匹配剪辑、转场衔接、无缝、遮罩转场、划像、闪白、闪黑、同镜（命中任一子串即软）；`HARD_TRANSITIONS` = 硬切、切换、直切、跳切、无（或转场为空）。
- 输出每段 `生成时长秒` clamp 到 `[4, 15]` 整数；`衔接` 四分支：开场 / 同镜续拍（需生成桥接首帧）/ 镜头切换（直接硬切）/ 连续画面（被迫断开，建议桥接帧）。
- 视觉锚点按段过滤：只带「出现镜头」与本段有交集的锚点。理由：多配色商品各自成锚点，全塞进每段等于要求同时画两种配色。

### 1.9 ⑦ match 分镜素材匹配

**一次 LLM 调用给所有分镜×所有片段打分**（不是逐镜调用），程序只做收敛与钳制（非数字算 0，clamp 到 `[0,1]`）。

提示词把分档口径直接写进去，与 `rules.py` 的阈值保持一致：≥0.8 可直接用 / 0.55–0.8 需改造 / <0.55 不该用，宁可重生成 / 没有合适片段写空候选。匹配规则按重要性排序：能表达动作与叙事功能 > 商品清晰可见 > 景别机位氛围接近 > 时长够用（可裁剪、可轻微放慢，**不能硬凑**）> 画面清晰稳定。每镜最多列 3 个候选，降序。

两个布尔由 LLM 一起给：
- `画面匹配`（**从宽判**）：主体/商品对得上且不与台词冲突就 true；只有明显是别的商品/场景、或画面与台词自相矛盾才 false。
- `口播内容匹配`：语义一致即可；**只决定声音怎么处理，不影响画面是否采用**。

**「一个片段只服务一个分镜」的分配算法是全局贪心，不是匈牙利**（`_assign_segments`）：
1. 候选字典的键用 enumerate 下标而不是「序号」——模型可能给出两个相同序号，按序号做键会让两镜塌缩成一条、拿到同一片段。
2. 把所有 `(分镜, 候选)` 对展平，只保留 `匹配度 >= 0.55` 的，排序键 `(-匹配度, 分镜序号, 候选排名)`。
3. 线性扫一遍占位：匹配度高的分镜先占，让位的分镜退到自己的次优候选，并写 `分配说明`。
4. 有过门槛候选但全被抢走的分镜，写「候选片段都已分给其它分镜，改走重新生成」。

不做全局最优二分图匹配，是为了消灭「同画面播两遍」——片段往往只有几秒，允许跨镜复用时第二镜裁到的还是同一段画面。

一个易错的实现细节：**先把匹配度 `round(..., 2)` 再判策略**，否则会出现「产物写 0.80 却走重新生成」的自相矛盾。

### 1.10 ⑧ generate 分段出片

**段内按镜头混合**是这一步的核心。`_seg_blocks` 把一段切成连续「块」：

- 每镜先判 `kind`：能裁素材的是 `cut` 块，其余是 `gen` 块。
- **gen 块必须合并连续镜头**：seedance 最短出 4s，一镜一块会把 0.4s 的镜头撑成 4s，费额度还打乱节奏。
- **cut 块再按 `需要配音` 分一层**：配音是整块贴的（`-map 0:v:0 -map [a]` 换整轨），块内混着「原声直接使用」的镜头会把那一镜的人声与画面内音效一起丢掉。

能不能裁的判定集中在 `shot_match.py` 三个函数（全链路唯一定义）：
- `_can_cut(row)` = 策略是「直接裁剪」+ 有源文件 + 切片动作 ∈ {原声直接使用, 分离BGM保留人声, 静音后使用}
- `_cuttable(row)` = 有源文件 + 切片动作在白名单 + 可用秒 > 0（**不看策略**，语义是「画面已过门槛、素材在手」，专供退回判断）

裁剪的时长处理：`take = max(0.4, min(可用秒, 需要秒))`；仅当 `需要 > take*1.02` 才放慢，`MAX_SLOWDOWN = 1.6` 是上限（视频 `setpts`，音频 `atempo` 下限夹 0.5），再不够就整块偏短。块内缺口按各镜余量比例挪移。

**新鲜画面优先**（`_ClipWindows`）：同一片段被多次取料时，剩余新鲜画面不足 `min(需要, 1.0)` 秒才回卷复用起点，并显式记 `复用回卷` + 打日志（`review_case` 会把它算成问题）。

生成路径（`_gen_piece`）：挑到合格参考片段 → 素材编辑（r2v）；挑不到 → 参考图生视频 / 纯文生视频。参考片段的两道门：素材里有真人就跳过；`可用秒 * 1.6 < REF_CLIP_MIN(5.0s)` 就跳过。为什么要 5s：seedance 硬限只要 1.8s，但短于 5s 的参考段控不住整段画面。

并发：块级 `seg_workers` 默认 **5**；但 TTS 有全局锁 `_TTS_LOCK` 一次只跑一条（无 GPU，VoxCPM2 单条吃满约 30 核，实测 4s 音频 131s，三条并发会一起撞 600s 超时）。

**缺段是硬失败**：任一段出不来，`step_generate` 直接 raise——缺段的成片会静默少内容。

### 1.11 ⑨ compose 拼接与音轨字幕

**归一化拼接**（`produce_video.concat` → `_normalize`）：
- 画布 `TARGET_W × TARGET_H` 由 `_canvas()` 算出：**短边 720**，跟随 `config.ASPECT`（默认 9:16 → 720×1280），横屏参考片时按比例反向算，宽高强制取偶。刻意不写死 9:16，否则横屏会被垫黑边。
- 帧率 30、采样率 44100、双声道。mono↔stereo 中途切换会让 AAC 编码器吐 `NaN/+-Inf` 直接失败，所以统一。
- 滤镜 `scale=...:force_original_aspect_ratio=decrease, pad=居中, fps=30`；编码 `libx264 -preset medium -crf 20 -pix_fmt yuv420p -movflags +faststart`。
- 无音轨的片补 `anullsrc` 静音轨——每片都有音轨，拼接才能 `-c copy`。
- 每片进拼接前做**静态增益对齐**：目标 `-20.0 dB`，单片增益上限 ±6 dB，差值 < 0.8 dB 不动；只用 `volume` + `alimiter=0.95`，刻意不用 loudnorm/compand。静音片（低于 -45 dB）不许抬。
- 任一片归一化失败 → 整体中止，**不出少一段的成片**。
- 拼接本体 `-f concat -safe 0 -c copy`。

**BGM 怎么铺**（`_apply_audio`）——用户拍板的口径是「不压低、原声直取、无淡入淡出」：
```
[1:a]aloop=loop=-1:size=2e9[bg];
[0:a][bg]amix=inputs=2:duration=first:dropout_transition=0:normalize=0, alimiter=limit=0.95[a]
```
- 循环：`aloop=loop=-1` 无限循环；截断：`duration=first` 按成片音轨长度收。
- `normalize=0` 显式关掉 amix 的自动各减半，人声与 BGM 都保持原响度；**全链路没有 `afade`**，只有 `alimiter` 防叠加爆音。
- `copy_bgm=False` 关不掉用户显式上传的 BGM（策略为「用户上传音乐」时照铺）。
- 绝不整轨照搬参考片原声：成片人声一律是自己 TTS 克隆出来的，整轨搬会盖掉克隆口播和画面内音效。

**字幕**（判定见 §2.7，样式参数见下）：
- 一律**自己写 ASS**，不烧 SRT。原因：SRT 没有分辨率信息，libass 按默认 384×288 解释字号再拉伸，字会放大 4 倍多冲出画面。
- 样式：字号 42、左右边距各 56、距底 72、一屏最多 2 行；主色白 `&H00FFFFFF`、描边/背景 `&H80000000` 半透明黑、BorderStyle=1、Outline=3、Shadow=0、**Alignment=2 底部居中**。
- 折行自己算：CJK（`ord > 0x2E7F`）算 2 半宽；行宽达到 `per_line * 0.7` 且遇标点即提前断；英文回退到最近空格。libass 只在空格断行，中文不会折。
- 字体解析用 `fc-query` 取**族名**而不是文件名 stem，.ttc 多 face 时优先挑简体 face。踩过的坑：写 stem 会让 libass 匹配失败、一行中文由多种 fallback 字形拼出甚至烧成方块，而产物仍记 `burned=True`。
- 找不到 CJK 字体就只产 SRT、`burned=False`。

**成片查画面残字**（`detect_burned_text`）：素材自带字幕的片不该再叠一层。
- 粒度是「片」不是「段」：段内混合出片后一段里既有用户素材也有 AI 补片，按段判会因为一块自带字幕就让整段都不叠字幕。
- 缓存 `render/subtitle_check.json`，命中条件是 `检测版本 == SUBCHECK_VERSION(2)` **且** `片段指纹（整文件 MD5）` 都对上。只按 key 缓存会拿旧结论判新画面。
- 判定口径写在提示词里：画面下方/中下方跟台词出现的成句文字、明显花字贴纸水印算字幕；**商品本身/包装/吊牌/衣服/招牌/书本/手机电脑屏幕里的文字不算**。
- 检测失败按「没有字幕」处理——宁可我们贴字幕，也别整片没字幕。并发上限 3。
- 这条链路按 `SUBCHECK_ENGINE`（默认 qwen）**直调**，没有 gemini→qwen 回落。

---

## 二、规则体系 rules.py：规则是数据

### 2.1 引擎语义（所有表共用，由 `decide()` 实现）

1. **自上而下取第一条命中**，每张表最后一行必须是无条件兜底行（没有兜底就 `RuntimeError`）。
2. 规则里不写某字段 = **通配**。
3. **事实缺失（`None`）只能被通配吃掉，不会等值命中**——漏采集的维度不会静默走进某条规则。
4. 数值字段写 `(下限, 上限)`，含义 `下限 <= 值 < 上限`。
5. 「动作」只是动作名，实现由调用方的动作表映射到函数；换实现不用动表。
6. 前置门槛**只认明确的 False**，`None` 照旧往下走规则并在痕迹里记 `门槛未判定`。
7. 命中结果带完整判定痕迹（`规则表` / `命中行` / `事实`），调用方原样写进产物 JSON——前端和排查时能直接看出「这一条为什么这么处理」。
8. 表名拼错直接 `KeyError`，不静默兜底。
9. 事实收敛统一走 `as_bool`：`true/1/是/有/yes/y` → True，`false/0/否/无/没有/not/no/n` → False，**其余一律 None**。

自检工具 `coverage_holes()`：穷举各字段取值组合，报出「只能落到兜底行」的组合供人工确认。枚举取值域时不一律按布尔穷举——`字幕配置` 的取值是 关/开/跟随参考片，按布尔穷举会报一堆现实里不存在的假空洞。

### 2.2 表一：用户切片处理（每条命中素材怎么用）

前置门槛 `画面匹配`，不过 → **不使用**。判定字段：`真人出镜口播`、`口播内容匹配`、`有BGM`。

| # | 真人出镜口播 | 口播内容匹配 | 有BGM | 动作 | 需要配音 |
|---|---|---|---|---|---|
| 0 | 否 | 任意 | 任意 | 静音后使用 | 是 |
| 1 | 是 | 否 | 任意 | 静音后使用 | 是 |
| 2 | 是 | 是 | 否 | 原声直接使用 | 否 |
| 3 | 是 | 是 | 是 | 分离BGM保留人声 | 否 |
| 4 | 兜底 | | | 静音后使用 | 是 |

两处设计是**事故驱动**的，改动前务必读注释：
- **口播内容对不上不再一票否决**：台词是新写的，用户素材里说的几乎永远是别的话，卡这一条会把所有真素材淘汰掉。改成画面照用、声音重配。
- **兜底必须是「用」而不是「不用」**：画面过了门槛就一定要用上，音轨维度判不出来时按最安全的「静音 + 重新配音」处理。实测有一轮 12 镜全被兜底判成不使用，**成片 0 帧用户素材**。
- 「真人出镜口播」的口径：画面里的人张嘴在说话（看得到嘴动、声音是他发出的）才算 True。人物出镜但没张嘴、只有旁白/画外音 → False。

### 2.3 表二：音色优先级（有序，取第一个拿得到的）

| # | 来源 | 策略 | 说明 |
|---|---|---|---|
| 0 | 用户视频-原声 | 铆钉基准 | 提取素材里的纯净人声（带 BGM 先分离）当整片音色基准 |
| 1 | AI视频片段 | 参考原声 | 素材没人声：第一个出声的 AI 段提纯人声当基准 |
| 2 | 用户视频-静音 | 克隆原声 | 克隆基准音色后给静音切片配音 |
| -1 | 兜底 | 全部AI配音 | 没有任何用户原声可参考 |

这张表是**判定与实测分离**的典型：`pick_voice` 只按可用来源选策略，真文件要靠 `_voice_candidates` 逐个抽取 + 过门禁。四层候选：keep 里可用秒最大的那条 → 池里 `真人出镜口播=True` 的按时长降序取前 3 → 池里 `有人声=True` 的前 3 → 按响度兜底探前 8 条取第一条响度过线的。全军覆没时把「用户视频-原声」从可用列表里移除、**重新 `pick_voice`**，一般落到「AI视频片段」。

抽取参数：`take = min(12.0, max(2.0, 可用秒))`，单声道 44100 pcm。下限 2.0s 是 seedance 参考音的硬要求（更短整条请求被拒）。

### 2.4 表三：切片缺失补片时模特怎么出镜

判定字段：`需要模特出镜`、`已有模特出镜`、`多段需要模特`。

| # | 需要模特 | 已有模特 | 多段需要 | 动作 | 依赖 |
|---|---|---|---|---|---|
| 0 | 否 | 任意 | 任意 | AIGC直接生成 | 商品图 |
| 1 | 是 | 是 | 任意 | 提取模特改线稿图 | 商品图、视频模特帧 |
| 2 | 是 | 否 | 是 | 直接生成线稿图（各段复用同一张保一致） | 商品图、视频模特帧 |
| 3 | 是 | 否 | 否 | 文字描述生成（不做人物参考图） | 商品图 |
| 4 | 兜底 | | | AIGC直接生成 | 商品图 |

一个易错点：`已有模特出镜` 在无选中切片时要写 **False 而不是 None**——记 None 会让四行规则全落空、掉进兜底。

### 2.5 表四：素材匹配策略

| # | 匹配度 | 动作 |
|---|---|---|
| 0 | `0.80 ≤ x < 1.01` | 直接裁剪（裁剪对齐分镜时长） |
| 1 | `0.55 ≤ x < 0.80` | 素材编辑（拿素材当参考视频编辑） |
| 2 | 兜底 | 重新生成（宁可重生成也不用低分素材） |

配套常量：`DIRECT_USE_SCORE = 0.80`、`EDIT_SCORE = 0.55`、`SEGMENT_EXCLUSIVE = True`（文档性开关，独占行为硬编码在 `_assign_segments`）、`PRODUCT_REF_MAX = 9`、`PIECE_REF_MAX = 4`。

### 2.6 表五：参考片音轨怎么复刻

判定字段：`用户上传BGM`、`参考片有BGM`、`参考片有口播`、`BGM是音乐`。**调用点在步骤③ `ref_audio.py`，不在 compose**；compose 只读它落盘的结论。

| # | 用户上传BGM | 参考片有BGM | BGM是音乐 | 参考片有口播 | 动作 |
|---|---|---|---|---|---|
| 0 | 是 | 任意 | 任意 | 任意 | 用户上传音乐 |
| 1 | 否 | 否 | 任意 | 任意 | 不使用参考片音频 |
| 2 | 否 | 是 | 否 | 任意 | 不使用参考片音频 |
| 3 | 否 | 是 | 任意 | 否 | 整轨复用 |
| 4 | 否 | 是 | 任意 | 是 | 分离伴奏 |
| 5 | 兜底 | | | | 不使用参考片音频 |

`BGM是音乐` 由 `gates.check("BGM音乐性")` **实测**给出：False = 背景音只是零星音效或环境底噪，复刻它没意义还污染成片；**None = 判定器判不出，按通配放行走原有逻辑，不因判定器挂掉丢 BGM**。

实测触发条件（三者同时成立）：用户没上传可用 BGM + `有背景音乐 is True` + 口播维度已判定（否则不知道该测整轨还是测分离伴奏）。测的对象就是「将要贴的那条轨」：无口播测整轨，有口播先分离伴奏再测它。真的贴走时给门禁报告补 `使用: True` 供对账。

### 2.7 表六：字幕怎么烧

判定字段：`字幕配置`、`参考片有字幕`、`有参考片`、`风格克隆可用`。

| # | 字幕配置 | 参考片有字幕 | 有参考片 | 风格克隆可用 | 动作 | 回退 |
|---|---|---|---|---|---|---|
| 0 | 关 | 任意 | 任意 | 任意 | 不烧字幕 | — |
| 1 | 跟随参考片 | 否 | 任意 | 任意 | 不烧字幕 | — |
| 2 | 任意 | 任意 | 是 | 是 | 风格克隆 | 基础排版 |
| 3 | 兜底 | | | | 基础排版 | — |

三个要点：

- **`copy_subtitles` 是三值不是布尔**：`None`（默认）= 跟随参考片 / `False` = 完全不烧 / `"force"`（或老任务落盘的 `True`）= 强制烧。折算顺序是先判 `is False` 再判 `in ("force", True)`，**顺序不能换**，否则老任务的 `True` 会被混成默认值、显式打开的字幕一条都不烧。
- **「跟随参考片」是复刻口径的默认**：参考片有字幕/花字才烧，参考片画面干净的成片也保持干净（17 号苹果广告复刻实测教训）。`参考片有字幕` 的判据不是单纯关键词：某镜 `特效` 命中 字幕/花字/文案/标题/贴纸 **且该镜台词非空** 才算 True——那条 case 里 23 镜只有片尾一镜写 "MacBook Neo" 且台词为「无」，只按关键词会把整片判成有字幕。读不到拆解结果返回 `None`，靠通配落到「烧」，宁可多字不静默丢信息。
- **一道无条件覆盖**：动作不是「不烧」但剧本所有分镜台词都为空 → 强制改成不烧。实测教训：无口播 case 里字幕工具把 BGM 英文歌词识别成念白，烧出一屏重叠乱码。

`SUBTITLE_PLAIN = True`：用户拍板「每个 case 统一风格，就是普通字幕，不要强调、不要高亮」。所以风格克隆**只复刻字体/字号/颜色底色**，不复刻差异化手段——块内关键词不变色不放大、不斜排、不加入场特效、位置一律底部居中。落地方式是 compose 调 caption_clone 前把这条口径翻成 `WHQ_CAPTION_*` 环境变量（`PLAIN=1`、`HOT_SCALE=1.0`、`POS=bottom`、`ANIM=off`、`REVEAL=block`），并无条件设 `GAPFILL=0` 关掉补漏（补漏会把 ASR 里未被 plan 覆盖的部分当念白补进来，无口播时就是 BGM 歌词）。

### 2.8 表七：口播复刻（成片要不要台词）

| # | 参考片有人声口播 | 动作 | 说明 |
|---|---|---|---|
| 0 | 否 | 不写台词 | 参考片纯画面+音效叙事，所有镜台词为空 |
| 1 | 兜底 | 写台词 | 有口播（或判不出来按有处理）：照参考节奏写台词 |

这是整条链的**源头开关**：参考片纯 BGM/音效叙事的，剧本一句台词都不许写——没有台词自然没有 TTS 配音、没有字幕，整条链自动闭环。命中「不写台词」时除了在提示词里加约束，**代码层还会兜底把所有台词清空**（模型可能无视指令）。相应地，叙事修补那一层也不许再把台词加回来，否则等于绕过规则表。

---

## 三、门禁体系 gates.py：标注是假设，用前实测

### 3.1 五条范式

1. **标注是假设，不是事实**。VLM/LLM 说「有人声」「有 BGM」只配当候选依据；资产被用进成片前，必须对「将要使用的那个文件」实测。两个锚点 case：`4496` 标注有人声、实测 -42.6 dB 底噪；`54f7` 标注有 BGM、实测是零星音效不是音乐。
2. **判定进声明表**。每个门禁 = 指标函数 + 阈值 + 判不出策略 + 口径说明，全部集中在 `GATES`；调用方只喊 `check(门禁名, 文件)`，不自带阈值。
3. **报告是标准件**：`{门禁, 通过, 指标, 判据, 原因}` 原样写进产物 JSON；谁真用了这份资产就在报告上加 `"使用": True`。
4. **判不出 ≠ 不通过**。走门禁声明的策略：「放行」= 宁可维持旧行为也不静默丢内容；「拦下」= 来路不明的资产不许上。调用方统一用 `ok(report)` 拿结论，不自己解释 `None`。
5. **复核对账**。`review_case` 扫全部产物里的门禁报告，「通过=False 且 使用=True」一律记问题——新加的门禁自动被覆盖，不用改复核器。

加一个新门禁 = `GATES` 里加一条声明（必要时补一个指标函数）+ 调用方一行 `check()`。

### 3.2 三个门禁

| 门禁 | 对象 | 判定 | 判不出 | 说明 |
|---|---|---|---|---|
| **有声内容** | 任何「应当有声」的音视频（AI 段音轨、分离出的人声） | 平均响度 > `SILENT_DB = -45.0 dB` | **拦下** | 低于静音线就当没有声音（seedance 偶尔返回静音轨） |
| **克隆基准音** | TTS 克隆 / seedance 参考音用的基准人声 | 平均响度 > `VOICE_MIN_DB = -38.0 dB` | **拦下** | 弱参考克出噪音嗓，量不出的更不许当基准 |
| **BGM音乐性** | 将要贴进成片的 BGM 轨（整轨或分离伴奏） | 三条同时满足（见下） | **放行** | 零星音效/环境声不复用；判定器挂了不因它丢 BGM |

BGM 音乐性的三条判据（音乐 vs 音效的可测差别）：
- `有声占比 >= MUSIC_MIN_COVER = 0.5`（音乐基本铺满，零星音效大段留白）
- `最长连续有声秒 >= min(MUSIC_MIN_RUN = 5.0, 时长 × 0.6)`（音乐成段，音效是短促脉冲）
- `谱平坦度中位数 <= MUSIC_MAX_FLATNESS = 0.35`（音乐有音高/和声结构，环境底噪接近白噪）

### 3.3 指标原语的实现细节

- `mean_db`：ffmpeg `volumedetect` 抓 `mean_volume`，量不出返回 **-99.0**（按静音处理）。
- `silence_profile`：`silencedetect=noise=-40dB:d=0.4`。**噪声门必须是绝对阈值，不能随轨响度自适应**——零星音效轨的间隙不是数字静音而是 -60 dB 上下的环境底噪，门一放低整条轨都算「有声」，锚点 case 会翻判。音轨 ≤ 0.5s 直接抛「判不出」。
- `spectral_flatness_median`：纯 numpy STFT（`win=2048, hop=512`，前 90 秒，单声道 22050 Hz），有声帧判据 `rms > max(peak_rms × 0.05, eps)`。刻意不用 librosa——它会拖起 numba 缓存，本机崩。

`SILENT_DB` 是跨模块共享的单一来源：`produce_video._audio_gain`（静音片不许抬增益）、`review_case`（该有口播的片是否静音）、`gates` 自己三处都引用它。

---

## 四、两道剧本审查：唯一会拦停任务的地方

两道都在步骤⑥ script 里跑，**跑在任何生成动作之前**——设定图是第一次真的花钱出图，分段之后就是 seedance 出片。

### 4.1 商品状态审查（不拦，只改写）

可改字段只有三个：`画面`、`视频提示词`、`视觉锚点`。查四类违规：

1. **商品状态失实**：屏幕/表盘/界面显示了商品图描述里不存在的具体内容（壁纸角色、界面、图案、成段文字）；指示灯、开合形态、部件、印刷图案与图片描述冲突或凭空新增；配色/款式不存在或**在分镜之间漂移**；把卖点文案宣称的功能画面写成画面元素。
2. **商品漂移**：某镜主体商品变成别的品类/品牌/型号。
3. **逻辑硬伤**：前后镜动作/位置/道具状态自相矛盾。
4. **复刻错位**：与「对应参考镜」的叙事功能明显不等价，破坏钩子或节奏。

不算违规：创意特效（光效/粒子/转场）、人物表演、环境氛围、字幕花字、「屏幕亮起」这类中性写法。

落地细节：锚点按**名称**定位（不按序号——锚点无序号，一错就全片错），分镜按 `int(序号)` 定位（模型偶爾回字符串）。落不下去的进 `未修复`。状态文案是 `通过` 或 `违规 N 处，改写 M 处，未修复 K 处`。**审查失败不阻断**，但 `review_case` 会把「未修复 > 0」和「审查失败」都记成问题。

### 4.2 叙事连贯审查（会拦）

三段式：`_narrative_check` → `_narrative_repair` → 复审。走纯文本 qwen 链路（输入全是文字，不占 gemini 视频额度）。

**审查的核心方法是「盲测」**：先只看画面与台词、**不看剧本意图**，用一句话说出片子在讲什么；第二步再对比意图判断是否兑现。四类问题：
- `理解断点`：看到这一镜不知道发生了什么、或与前面情境接不上
- `孤立镜`：整段删掉对理解整片无影响
- `缺失交代`：相邻两镜之间少了必要过渡镜
- `结构未兑现`：某段没做到它自己声明的「作用」

快切、无台词、抽象视觉本身不算问题，只有**导致看不懂**才算。

**状态判定**：`0 <= 可理解性 < NARRATIVE_MIN_SCORE(60)` **或** `意图兑现 == "未兑现"` → `可理解性不足`；否则有问题条目 → `有 N 处可优化`；否则 `通过`。分数解析失败记 -1，因为条件是 `0 <= score`，-1 不会被当成低分误报。

**修补的硬约束**：不许增删合并拆分重排任何分镜、序号与时长一个都不能改；「缺失交代」不能靠加镜（改写相邻两镜把那一环演出来）；「孤立镜」不能删（改成与主线相关、时长不变）。
可改字段动态开放：基础是 `画面` + `视频提示词`；`台词` 只在剧本本来有台词且口播判定不是「不写台词」时开放；`花字` 只在剧本本来就有花字时开放。
**代码二次卡关**：字段不在白名单 → 拒；文本空 → 拒；台词字数 > `时长 × 6.5 + 1` → 拒；花字 > 14 字 → 拒。拒掉的进 `未采纳` 并记原因。

**复审取舍**：只有复审分 ≥ 首轮分才采纳复审结论；退步则最终结论按首轮报并写 `复审说明`。首轮全量结论始终保留。

### 4.3 拦下条件与恢复

唯一触发条件：`narrative_gate` 为真（默认 True）**且** 叙事审查最终状态恰为 `可理解性不足`。
「有 N 处可优化」、「审查失败」、商品状态审查的任何结果**都不拦**。

拦下时的表现：剧本本体照旧落盘（`script.json` / `script.md` / `report.md` 全有审查痕迹），但 `素材` 为空、`分段` 为空——**一张设定图都没出、一段都没生成**。`pipeline.step_script` 把审查结论与告警打进日志后 raise，任务停在 script 步。

三条恢复路径：改商品信息或换参考片后「从 script 重跑」；或把 `options.narrative_gate` 关掉强行往下走（审查照跑、告警照写，只是不停）。

---

## 五、五条降级链

「显式失败，不静默丢内容」的具体落地。每条链都有明确的触发条件和留痕字段。

### 5.1 真人人脸风控（line_art.py）

seedance 拒收含真人人脸的参考图。**判据是错误文案不是错误码**——`40000002` 是通用「参数非法」码，风控拒绝与参数错误共用它，按码判会把「参考视频短于 1.8s」误判成风控、白跑三次降级还把真因埋掉。文案关键词：`real person`、`OutputImageSensitiveContentDetected`、`InputImageSensitiveContentDetected`、`sensitive`、`风控`、`审核`。

降级链 `ref2v → line_art → t2v`，**只在确认是风控拒时才下降**（非风控错误直接抛）。旧实现无条件往下掉的后果：参数类错误被当成风控，悄悄掉到纯文生视频、商品信息全丢，日志上只看到「降级成功」。

line_art 档的处理是分类的：商品图（`keep_refs`）走 `to_face_safe` **只换脸**，保住商品/姿态/背景的原像素；其它图走 `to_line_art` 整张重画。这个区分很关键——服装/配饰类商品图本身就是「模特穿着实拍」，整张重画会把它变成一张人物图、商品信息全丢。

替身图的三段提示词，每段都有实测支撑：
- **VLM 读外观**（≤80 字）：只写性别年龄、发型发色、上衣下装鞋子款式颜色、体型、随身道具；**明确排除五官、表情、背景、光线**。排除五官是核心——保住「看起来是同一个人」的信息同时不复现人脸。
- **写实底座**：具体相机光学参数 + 介质声明（数码单反、85mm 定焦、f/2.8、ISO 200、影棚柔光箱三点布光、「实体照片扫描件」、无手绘或渲染成分）。实测：只写「4K写实摄影」约 2/3 出插画感，换成这套后 12/12 判为实拍。
- **面部线稿**（材质拼接写法）：面部纯白底黑色细线矢量轮廓、无肤色无阴影；**头发按发型发色写实渲染、不做线稿化**；发际线与颈部作为明确材质分割带。两条教训：只说「脸是线稿、身体写实」会让线稿扩散到全身衣服；线稿范围含头发会画成白色线条头发、发色信息丢失、成片里人物顶一头白发。
- **生成视频时前置 `REAL_ACTOR_HINT`**：把线稿脸显式定义成「需要被替换的占位符」，要求保持参考图的发型发色服装体型不变、线稿元素完全消失仅作结构引导。必须放在 prompt 最前面，否则 seedance 会把线稿脸照抄进成片。

多段一致性靠五条机制叠加：文字锚点复用、多段共享同一张线稿图（表三第 2 行）、`model_ref.json` 缓存整任务只做一次、`to_face_safe` 按 `(src, size)` 缓存（size 必须进 key，否则同图不同尺寸会拿错结果）、`REAL_ACTOR_HINT` 约束外观不变。

### 5.2 音轨分离三级（ref_audio.separate_bgm）

| 级 | 手段 | 触发 | 留痕 |
|---|---|---|---|
| 1 | **demucs**（htdemucs，`--two-stems vocals`，固定 CPU，超时 900s） | 首选 | `分离方法 = demucs_htdemucs_<want>` |
| 2 | **librosa REPET-SIM 软掩蔽**（`nn_filter` + `softmask`） | demucs 环境不存在或失败 | `残留风险 = True`、`分离回落原因` |
| 3 | **ffmpeg 声道抵消**（取人声用中置求和，取伴奏用中置抵消） | 前两级都失败 | 同上 |

三级全挂才抛异常。第 2、3 级都会打 `伴奏残留人声风险: True` 写进产物——弱分离器的结果被用了要留痕。
产物文件：`want="vocal"` 取 `vocals.wav`，否则取 `no_vocals.wav`（伴奏），统一编码成 aac 160k。

### 5.3 出片失败退素材（segment_build）

**核心原则：素材编辑走不通，先退回裁素材，最后才退纯生成。** 旧实现直接掉纯生成，实测有一轮 5 镜全丢真素材。

| 触发 | 降级 |
|---|---|
| 策略是「素材编辑」但素材有真人 / 可用秒×1.6 < 5s | → 直接裁剪，记 `策略降级` |
| 挑不到合格参考片段（有真人 / 太短 / 裁出来不够 5s） | → 参考图生视频 |
| 参考音被拒（`audio format` / `reference_audio cannot be the only`） | → 丢参考音重试（另有前置丢弃：有参考音但既无参考图也无参考视频时提前置空） |
| 参考视频被真人风控拒 | → 能裁素材就抛给调用方裁素材；否则丢参考视频 + 摘掉编辑提示重试 |
| `_gen_piece` 任何异常且块内全部可裁 | → `cut("补片走不通（原因）改用素材")` |
| 仍失败 | → 该块记 `error`，该段变 error 段，`step_generate` 最终 raise |

主生成有 3 次重试，但**每次只丢一样输入**（不是无脑重试）。验收（`_vet_piece`）不通过会带反馈重生一次，采纳新版的条件是「新版没被判失败」或「新版商品一致而旧版不一致」；验收模型挂掉按放行处理。

### 5.4 配音时长对齐三级（voice_dub）

顺序上先合成后裁画面——先知道念白多长才能决定画面放不放长。

1. **先放长画面**：假定念白可加速 `DUB_MAX_TEMPO = 1.4`×，仍塞不进才放长画面，上限 `DUB_MAX_STRETCH = 1.5`×。
2. **贴回时以实际画面时长为准**：念白偏长 → 加速到 1.4× 上限，仍超出 0.3s 就**截断**并记 `配音截断秒`；念白偏短 → 放慢到 `DUB_MIN_TEMPO = 0.85` 下限，剩余空档 > `DUB_GAP_MAX = 0.25s` 才**补静音**并记 `尾部补静音秒`。
3. 滤镜链必须自己 `atrim` 音轨长度：`apad + -shortest` 配 `-c:v copy` 会让 ffmpeg 挂死（实测跑 5 分钟不结束）。

旧实现直接 apad 的后果：实测两处各留 0.8~0.9s 空档，而 BGM 只有 -32 dB 撑不住这段空白。

### 5.5 模型链路回落（aigc.understand）

**全项目只有 `understand()` 一处实现 gemini → wenchain 回落**。触发条件是 `vision_gemini` 抛 `RuntimeError/OSError`，而那要求**所有网关 × 所有重试都失败**（9 个内网网关 × 3 轮 = 最多 27 次 HTTP，退避 2s→4s 封顶 8s）。回落后有 media 走 `vision`，纯文本走 `chat`。

三个容易搞错的点：
- **`json_mode` 只对回落链路生效**：gemini 原生协议没有 `response_format`，要 JSON 必须在 prompt 里写清楚。
- **回落前要补上传**：gemini 支持本地路径（base64 内联），一旦回落，那些路径会被原样塞进 `image_url` 发给 wenchain，网关拉不到。而回落是静默降级，比对会全返回「判不出」，白底图那条 `unknown_ok=True` 的链路还会把「判不出」当成通过。
- **成功判据是 `status.code == 0` 而非只看 HTTP 200**：HTTP 200 不等于拿到文本（内容被过滤、只回 reasoning、回 tool_calls 时 content 为 null）。不收口 `None` 会一路流到 `.strip()` 抛 `AttributeError`，而下游降级链只 catch `RuntimeError`，异常会穿透整条流水线。同理 `_post` 把状态码错误与非 JSON 响应都翻成 `RuntimeError`。

### 5.6 外挂工具探活失败

| 工具 | 消费点 | 失败行为 |
|---|---|---|
| `tts_clone` | `voice_dub._dub_wav` | 返回「跳过：TTS 后端未就绪」，该片无配音继续出片，**不抛异常** |
| `caption_clone` | `compose` 的字幕规则表 | 作为事实 `风格克隆可用=False` 进表 → 命中兜底行「基础排版」，走自研 SRT+ASS |
| `caption_asr`（词级 ASR） | caption_clone 内部 | **不参与任何判定**，只影响逐字时间精度，工具内部自兜 |

探活只是纯文件存在性检查（解释器路径 + 脚本路径），不启子进程。

---

## 六、纯重剪链路：recut.py

### 6.1 为什么零模型调用

成立前提：**爆款本身就是从这份用户素材里剪出来的**（短剧投放素材的常见形态）。此时「找素材」根本不需要语义理解——两边画面是同一批帧，用感知哈希做帧级检索比让 VLM 各自描述再配对又快又准。

三条理由：
- 重剪要的事实只有两个（爆款的镜头切点、每个镜头在素材里的位置），都是**像素级事实**；VLM 给的是语义描述，反而要靠时间码对齐，绕远且贵。
- `analyze_materials` 走 VLM 要把整条素材 base64 内联上传，59 分钟原片**实测跑 4.6 分钟未返回**。
- 显式失败：指纹匹配不上的镜头记「未命中」写进 EDL，不拿相近画面糊过去。

### 6.2 九个阶段

**0) box 几何归一化**——横屏原片塞进竖画布的必需步骤。
取 12 帧铺满全片，`scale=64:64:flags=area` 灰度，沿帧轴求平均得一张平均灰度图。指标是**行/列平均亮度剖面**，阈值 `峰值 × 0.18`，把过阈的连续段全部枚举后**取最长的一段**。这一步同时干掉信箱黑边、标题带、落款带：标题带只占一两行且与内容区隔着纯黑行，长度竞争不过内容区。
内容区内再切边：底部 22%（烧字幕）、左右各 8%（竖排引流水印）、顶部 6%（角标台标贴纸）。最后按长宽比统一——对爆款与每条素材各算一次 AR，取**最窄的那个**当目标。

**1) shots 镜头切分**
dHash 9×8 网格，同行相邻列比较得 64 bit。相邻解码帧（爆款 10 fps，即 0.1s）汉明距离 > `CUT_DIST = 20` 判硬切。短于 `MIN_SHOT_SEC = 0.4s` 的镜并进前一镜。

**2) index 素材指纹索引**
同样方式解码，素材 4 fps（爆款 10 fps）。数据结构是全内存 `(times[n], hashes[n,8])`，59 分钟 @4fps ≈ 1MB。落盘缓存 npz，key 由（文件大小+mtime、fps、几何裁剪参数）决定。

**3a) asr 台词锚点**——把每一镜的搜索范围从整条原片缩到锚点附近几十秒。
台词的价值：**不随改分辨率/加字幕/贴纸/变速而变**。
引擎是 Qwen3-ASR-0.6B（+ 可选 ForcedAligner），独立解释器子进程、CPU float32、超时 180s。整片按 30s 分块转写（对抗 `max_new_tokens=512` 截断），爆款整片 + 每条素材整片各做一遍。
锚点表 `anchor_map`：素材侧建 5-gram 倒排；爆款侧逐 5-gram 查表，命中数 > 12 的（满片都有的台词）跳过；投票落 **0.5s 网格**的桶（0.1s 网格会让票数永远 <2、锚点表恒空）；票数 ≥2 的桶保留；最后对素材时间做**最长非降子序列**得到单调锚点链。
窗口外推：取镜头前后最近的锚点线性外推，**基础 ±6s，每远离锚点 1s 再放宽 0.25s**（固定 ±6 或 ±12 两头都不对）。

**3b) hash 滑窗序列对齐**
取样帧数 `min(12, max(3, 时长 × 4))`；取样区间内缩 12%——**这就是「避开转场帧」**，叠化/淡入的混合帧都在镜头两端。
距离函数：`Σ 各取样帧的 64bit 汉明距离 / 取样帧数`，值域 0-64。这是**序列对齐**，镜内相对时序当硬约束；旧实现（每帧独立最近邻）在静止镜头上大量误判。
命中判据 `平均距离 <= MEAN_MATCH_DIST = 12.0`（平均每帧 64 bit 里错 12 bit 以内）。
变速搜索：12 个倍率候选 `1.0, 0.95, 1.05, 0.9, 1.1, 0.85, 1.15, 0.8, 1.25, 0.75, 1.5, 2.0`（1.0 在最前，打平留 1.0）。**只在 1.0 未命中且镜长 ≥1.5s 才搜**，且非 1.0 解必须比 1.0 好 `SPEED_MARGIN = 1.5` 分才采纳——曾有一条片 29 镜被误标 1.5x/2.0x 导致节奏全乱。
**什么条件才推翻锚点约束**：锚点窗内平均距离 > `ANCHOR_TRUST = 6.0` 才多搜一遍全片，且全片解必须比锚点解好 ≥ `ANCHOR_MARGIN = 4.0` 分才采纳。实测依据：某镜全片 4.8 分那一解在时间线上往回跳，锚点 6.1 分才接得上前后镜。

**叠化补刀**（只在该镜定位失败后才调）：镜长 ≥2.5s 时算**跨 4 帧**的距离曲线（逐帧变化太小抓不到叠化），峰值须同时满足 `>= 20` 且 `>= 窗内中位数 + 8`；再检查高原宽度（向左右扩到峰值 70% 为止，宽度 > 12 帧则判为持续运动，否决）；切点取跨窗峰值的中点，两侧都必须 ≥0.4s。全片软切总预算 = 原硬切镜数（每条最多再补一刀）。

**3b') ASR 直接定位**（rescue 第一层）：对爆款该镜区间转写，取第一个非空 token 的起点当镜内念白偏移，在素材窗内用 4-gram 投票（按 0.12s/字 回推到 query 开头）。窗宽 > `ASR_MAX_WIN = 90s` 直接放弃。要求台词 ≥ `ASR_MIN_CHARS = 4` 字。

**3c) vlm 校验**（rescue 第二层，**只校验候选、不让它满片检索**）：抽爆款 3 帧 + 素材 3 帧共 6 张（360px），问一行 JSON `{"same":bool,"src_start":秒}`，并要求「拿不准就 same=false」。校验链四道：能抠出 JSON → `same` 为真 → `src_start` 可解析 → 落在 `[lo-1, hi+1]` 内。全片预算 `VLM_MAX_SHOTS = 8` 次。

**3d) aigc 补片**（默认关，`--aigc` 才开，预算 6 镜）：必须 hash + ASR + VLM **三层全失败**才触发。抽 3 帧让模型写提示词（截 300 字）→ 参考图生视频（被风控拒就退纯文生）→ 下载（不能让 ffmpeg 直接吃签名 URL）→ 裁回原时长、**一律静音**。

**4) cut 裁剪**：`take = 目标时长 × 倍率` 取料，`setpts=PTS/speed` 压回原时长，音频 `atempo` 钳在 `[0.5, 2.0]`。画布统一 = 等比缩放不超框 + 居中黑边 + 帧率归一。静音片补 `anullsrc` 静音轨。编码 `libx264 -preset veryfast -crf 21`。

**5) concat 拼接**：`-f concat -safe 0 -c copy -movflags +faststart`。流复制可行的前提是所有片都出自同参数的 `cut_piece`。清单必须写绝对路径（写相对路径会被 concat demuxer 解成 `pieces/pieces/xxx.mp4`）。

### 6.3 单镜决策序（主循环）

```
place(shot)                        # 锚点窗 → hash 序列对齐（必要时搜全片）
  ├─ 命中 → 裁剪
  └─ 未命中 且 还有软切预算 → soft_cut_at 拆两段压回队列头，重新来
       └─ 仍未命中 → rescue(shot)
            ├─ ASR 直接定位（台词 ≥4 字）
            └─ VLM 校验（预算 8 次）
                 └─ 仍未命中 且 --aigc 且 预算未尽 → gen_piece
                      └─ 都不成 → 产物为空，显式记「未命中」
```

### 6.4 「未命中」的显式失败表现

- 控制台该行打 `镜NN X.XXs → 未命中（平均距离 X.X）`。
- `edl.json` 该行 `命中: false`、`产物: ""`，但**保留 `源起点` / `平均距离` / `变速` / `定位` / `源文件`**——把「最接近的落选解」和它的分数留下来。
- `report.md` 表格该行素材位置写 `未命中`、产物写 `—`。
- `pieces/` 里没有对应 `sNNN.mp4`（编号有缺口），`final.mp4` 因此短于爆款，`覆盖时长比` 反映缺口。
- **全片零命中时仍然先写 edl.json 和 report.md 再抛异常**：几何没对齐时距离会整体停在 20 上下，与真换了素材是两种分布，靠这份逐镜距离才能区分。

### 6.5 CLI 与产物

```bash
python3 recut.py <爆款片> <用户素材...> [--outdir DIR] [--mute]
                 [--cache .recut_cache] [--no-asr] [--no-vlm] [--aigc]
```

| flag | 默认 | 说明 |
|---|---|---|
| `--outdir` | `output/recut/<爆款名>` | 产物目录 |
| `--mute` | 关 | 成片静音（默认带素材原声） |
| `--cache` | `.recut_cache` | 指纹缓存目录，传空串关闭 |
| `--no-asr` / `--no-vlm` | 都开 | 关掉对应兜底层 |
| `--aigc` | **关** | 三层都失败才生成补片 |

产物：`final.mp4`、`edl.json`（逐镜源位置与命中距离 + 命中率/覆盖时长比/变速镜数/各定位来源计数）、`report.md`（一张逐镜表：镜/爆款区间/时长/素材位置/变速/定位/平均汉明距离/产物）、`pieces/`。

### 6.6 与主链路的复用关系

`recut.py` **不 import pipeline，pipeline 也不 import recut**。只复用 `produce_video` 的 4 个函数（`_ffmpeg` / `_run` / `_duration` / `aac_args`）和 4 个常量（`TARGET_W/H/FPS/AR`）。`aigc`、`storage`、`Agent_tools.caption_clone.core.asr_tokens` 都是**延迟 import**，失败即降级不阻断链路。

注意「零模型调用」是运行时事实，不是依赖树事实——`import produce_video` 会传递性拉进 `aigc / config / line_art / storage / write_script`。

---

## 七、质检与对账：review_case.py

`python3 review_case.py <task_id>`，**只读产物 + 量音量，不调任何模型**。18 项检查，重要的判定标准：

| 检查项 | 判定标准 |
|---|---|
| 素材利用率 | `用户素材镜数 / 总镜数`，只报数不设阈值 |
| **0 镜用户素材是否属缺陷** | 用户素材 0 镜 **且** 有能直接出镜的可裁镜 → 报问题；两者都空 → 记「属预期」不算问题 |
| 素材窗口重叠 | 同片段相邻窗口 `b.start < a.end - 0.5`（0.5s 容差）→ 同画面重复出现 |
| 窗口回卷复用 | `复用回卷` 计数 > 0 即报（口径：片段应当一镜独占） |
| 成片时长偏差 | 与剧本计划秒偏差 > **25%** |
| 该有口播的片是否静音 | 逐片量 `mean_volume < -45 dB` **且**该片任一镜有台词 → 报。没台词的静音是分镜设计，不报 |
| 配音截断 | 任一片有 `配音截断秒` → 报 |
| 字幕重复 | 同文本两条 cue 间隔 < 2.5s **且**不是剧本本身重复的台词 |
| 字幕交叠 / 越尾 | 相邻 cue 重叠 > 50ms / cue 超出成片 > 150ms |
| **字幕事实链** | `参考片有字幕` 为真 + 剧本有台词或花字 + 成片没烧 → 报。**只看事实 vs 成片，不看配置**（配置被显式关掉恰恰是要暴露的） |
| 反向检查 | 剧本无任何文字却烧了字幕 → 报 |
| 剧本两道审查 | 商品状态审查 `未修复` 非空、或任一审查「审查失败」、或叙事审查「可理解性不足」→ 报 |
| **门禁对账** | 递归扫 `voice_plan.json` / `reference_audio.json` / `segments.json`，找「`通过=False` 且 `使用=True`」的门禁报告 |

两项**只记录不算问题**：`放长倍数`、`降级`——它们是正常的兜底动作留痕。

对账机制的关键设计：`gates.violations()` 递归找违规，所以**新加的门禁自动被对账覆盖，不用改复核器**。

---

## 八、常量速查

### 8.1 判定阈值

| 常量 | 值 | 位置 | 含义 |
|---|---|---|---|
| `DIRECT_USE_SCORE` | 0.80 | rules | ≥ 直接裁剪素材 |
| `EDIT_SCORE` | 0.55 | rules | ≥ 素材编辑，低于此重新生成 |
| `PRODUCT_REF_MAX` | 9 | rules | 一次生成最多带几张商品参考图 |
| `PIECE_REF_MAX` | 4 | rules | 单个生成块最多带几张（多了模型混搭） |
| `SILENT_DB` | -45.0 | gates | 静音线 |
| `VOICE_MIN_DB` | -38.0 | gates | 克隆基准音响度门槛 |
| `MUSIC_MIN_COVER / MIN_RUN / MAX_FLATNESS` | 0.5 / 5.0s / 0.35 | gates | BGM 音乐性三判据 |
| `NARRATIVE_MIN_SCORE` | 60 | write_script | 可理解性阈值 = 拦下门槛 |

### 8.2 时长与节奏

| 常量 | 值 | 含义 |
|---|---|---|
| `MAX_SEG` / `MIN_SEG` | 15.0s / 4.0s | seedance 单次上下限，分段 DP 的硬约束与 clamp |
| `SHOT_MAX` | 6.0s | 单镜上限，超了切成同镜续拍子镜 |
| `SPEECH_RATE` | 6.5 字/秒 | 台词容量（实测 5.2 字/秒 + 1.25 倍加速仍自然） |
| `MAX_SLOWDOWN` | 1.6 | 素材放慢补时长上限 |
| `REF_CLIP_MIN` | 5.0s | 编辑用参考片段下限（硬限只要 1.8s，但 <5s 控不住画面） |
| `DUB_MAX_TEMPO / MAX_STRETCH / MIN_TEMPO / GAP_MAX` | 1.4 / 1.5 / 0.85 / 0.25s | 配音加速 / 画面放长 / 配音放慢 / 允许空档 |
| 花字上限 | 14 字 | 提示词与代码双重卡 |
| 总时长偏差容忍 | ±10% | 触发全片等比缩放 |

### 8.3 视频规格与模型

| 项 | 值 |
|---|---|
| 画布 | 短边 720，跟随 `ASPECT`（默认 9:16 → 720×1280），宽高取偶 |
| 帧率 / 采样率 / 声道 | 30 / 44100 / 2 |
| 视频编码 | `libx264 -preset medium -crf 20 -pix_fmt yuv420p`（recut 用 `veryfast -crf 21`） |
| 音频码率 | 128k（归一化）/ 160k（贴 BGM、分离产物） |
| 增益对齐 | 目标 -20.0 dB，单片上限 ±6 dB，差值 <0.8 dB 不动 |
| 字幕 | 字号 42、边距 56/56/72、最多 2 行、底部居中、描边 3px |
| LLM / VLM | `ali-qwen3.7-max` / `ali-qwen3.7-plus`（wenchain） |
| 理解主链路 | `gemini-3.1-pro-preview`（9 个内网网关，3 轮重试，超时 300s） |
| 生图 / 生视频 | `doubao-seedream-5-0-260128`（1664×2368，超时 300s）/ `doubao-seedance-2-0`（超时 900s） |
| 生视频参考上限 | 图 ≤9、视频 ≤3、音频 ≤3（音频不能单独用），时长 clamp `[4,15]` |
| TTS | VoxCPM2 zero-shot，输出 44100/单声道/16bit，归一到 -16 LUFS / 峰值 -1.5 dB，超时 600s |
| 并发 | 素材切片 3、商品图理解 4、设定图 4、状态图 6、分段出片 5、残字检测 3、TTS **1（全局锁）** |

---

## 九、复刻参数（options）与任务目录

### 9.1 options 全集

| 参数 | 默认 | 含义 |
|---|---|---|
| `copy_bgm` | `True` | 把参考片音乐铺到成片下（用户上传独立 BGM 时优先用用户的；关不掉显式上传的 BGM） |
| `copy_subtitles` | `None` | **三值**：`None` 跟随参考片 / `False` 完全不烧 / `"force"` 强制烧 |
| `rewrite_mode` | `"creative"`（Web）／CLI 显式传 `strict` | 复刻强度 |
| `narrative_gate` | `True` | 叙事审查不通过就停在 script 步，不往下生成 |
| `seg_workers` | `5` | 分段出片并发 |

`options` 与 `product` 更新都走**白名单过滤**，未知键静默丢弃。

### 9.2 任务目录

```
output/tasks/{task_id}/
├── task.json               # 唯一状态源，原子写（.tmp + os.replace，前端在轮询）
├── report.md               # 溯源报告
├── assets/  registry.json / material_index.json / uploads/（输入素材，任何重跑都不许动）
├── reference/ analysis.json + .md
├── audio/   reference.m4a / bgm.m4a / reference_audio.json / voice_base.wav /
│            voice_plan.json / voice_probe.mp4 / dub_seg*.wav
├── product/ fact_card.json / image_index.json / frames/ / refined_* / state_* / whitebg_*
├── script/  script.json + .md + assets/（char_*.jpg、scene_*.jpg）
├── edit/    asset_matches.json
├── generated/ segments.json / shot_refs.json / model_ref.json /
│             segments/seg_NN.mp4 / clips/ / mixNN/
├── render/  stitched.mp4 → with_audio.mp4 → with_subs.mp4 或 with_capfx.mp4 → final.mp4
│            subtitles.srt/.ass / subtitle_check.json / capwork/
└── history/{时间戳}/       # reset_from 留档
```

`task.json` 顶层：`task_id / title / title_auto / created_at / updated_at / status / step / error / options / product / inputs / steps / logs（只留最后 400 条）/ result`。

### 9.3 素材库与复用

- 素材库 `output/library/` + `manifest.json`（`hot_videos` / `reference_groups` / `hidden`）。
- **全链路按 MD5 判重，不按路径**（4MB 分块，按大小+mtime 缓存，上限 2000 条）。`hidden` 的指纹被预置进去重集合，于是清单/历史任务/目录扫描三个来源统一被过滤。
- 复用是**硬链接**（失败退回复制），不是让 inputs 指向别的任务目录——源任务被删或 reset 后本任务会报「缺少爆款参考视频」而前端素材栏还显示着那个文件。
- 搜索范围限定 `LIBRARY_DIR` + 任务根的 `assets/uploads/`：整个任务目录都翻会命中别人的产物（`render/final.mp4`、`generated/seg_xx.mp4`），而这些正是 `reset_from` 会挪进 `history/` 的对象。
- `task_id` 唯一校验点是 `task_dir()`，正则 `^[\w-]+$`。踩过的漏洞：传 `".."` 时 realpath 会跳到任务根的父目录，`/files` 能列出跨全部任务的产物（实测 1169 个文件）。

---

## 十、设计原则与踩坑档案

### 10.1 四条原则（README 的展开）

1. **用户素材为主、AI 补片为辅**：段内可混合，只有素材覆盖不了的镜头才生成；素材利用率是成片主指标。
2. **判定口径单一来源**：决策进 `rules.py` 规则表、事实检查进 `gates.py` 门禁表，管线里不散写 if + 魔法数；改口径只动一处。
3. **显式失败，不静默丢内容**：工具不可用就明确跳过并记日志；门禁判不出走声明好的「放行/拦下」策略；失败原因写进 `task.json`。
4. **一切可追溯**：每步产物落盘留痕，`review_case.py` 用同一把尺子复核每轮成片。

### 10.2 高频踩坑清单（改代码前先看这里）

| 坑 | 后果 | 现在的做法 |
|---|---|---|
| 兜底行写「不使用」 | 12 镜全被判不使用，**成片 0 帧用户素材** | 表一兜底必须是「静音后使用 + 重新配音」 |
| 门槛把 `None` 当 `False` | 缺一条标注的 0.9 分素材被整条丢掉 | 门槛只认明确的 False，`None` 记「门槛未判定」继续 |
| 按错误码 `40000002` 判风控 | 参数错误被当风控，白跑三次降级、真因被埋 | 按错误文案关键词判 |
| 无条件三级降级 | 悄悄掉到纯文生视频，商品信息全丢，日志只显示「降级成功」 | 只在确认风控拒时才下降 |
| 允许片段跨分镜复用 | 成片同画面播两遍（片段往往只有几秒） | 全局贪心独占分配 + 新鲜画面窗口 |
| 匹配度先判策略后取整 | 产物写 0.80 却走「重新生成」 | 先 round 再判 |
| 候选字典按「序号」做键 | 模型给重复序号时两镜塌缩、拿到同一片段 | 用 enumerate 下标做键 |
| 线稿范围含头发 | 成片里人物顶一头白发 | 线稿严格封闭在面部五官，头发写实渲染 |
| `REAL_ACTOR_HINT` 不前置 | seedance 把线稿脸照抄进成片 | 必须放 prompt 最前面 |
| 只写「4K写实摄影」 | 约 2/3 出插画感 | 换成相机光学参数 + 扫描件介质声明，12/12 判实拍 |
| 烧 SRT 而不是自写 ASS | libass 按 384×288 解释字号，字放大 4 倍冲出画面 | 自己写 ASS，带 PlayRes |
| 字体写文件名 stem | 一行中文由多种 fallback 字形拼出甚至烧成方块，产物仍记 `burned=True` | `fc-query` 取族名，.ttc 优先简体 face |
| 只按关键词判「参考片有字幕」 | 23 镜里只有片尾一镜写型号名，整片被判成有字幕 | 特效命中关键词**且**该镜台词非空 |
| 字幕检测按「段」缓存 | 拿旧结论判新画面 | 按「片」+ 整文件 MD5 指纹 |
| 关掉字幕后 `capwork/*.ass` 残留 | 拿它对账会误判 | 只在 `burned=True` 时读回 ASS |
| 配音直接 `apad` | 两处各留 0.8~0.9s 空档，BGM 只有 -32dB 撑不住 | 加速/放慢 + 阈值内才补静音 |
| `apad + -shortest + -c:v copy` | ffmpeg 挂死（实测跑 5 分钟不结束） | 自己 `atrim` 音轨长度 |
| silencedetect 噪声门自适应 | 零星音效轨的 -60dB 底噪被算成「有声」，锚点 case 翻判 | 固定绝对阈值 -40dB |
| 提纯判定用 `SILENT_DB` 而非 `VOICE_MIN_DB` | 响度落在 (-45,-38] 时覆盖掉可用的混音基准，且不可回退 | 用 `VOICE_MIN_DB` 判「提纯白干」 |
| 锚点投票用 0.1s 网格 | 票数永远 <2、锚点表恒空 | 0.5s 网格 |
| 变速搜索无 margin | 29 镜被误标 1.5x/2.0x，节奏全乱 | 非 1.0 解必须好 ≥1.5 分 |
| 窄窗尾部不留余量 | 查询序列尾巴被切掉，真解 1.8 分被压成 5.5 分 | 尾部多放一个镜头长度 × 最大倍率 |
| concat 清单写相对路径 | 被解成 `pieces/pieces/xxx.mp4` | 写绝对路径 + 单引号转义 |
| SAR ≠ 1 直接 `-c copy` | 拼流后画面按错比例拉伸 | 裁剪时统一 `setsar=1` |
| mono↔stereo 中途切换 | AAC 编码器吐 `NaN/+-Inf` 直接失败 | 全链路统一 44100/2 |
| `reset_from` 挪走 `assets/uploads/` | inputs 全指向 history/，任务读不到自己的素材 | `STEP_DIRS` 刻意不含它 |
| 并发登记素材不加锁 | 前端拖多个文件时各自读到同一份 rec、后写覆盖先写 | `task_store._LOCK` 串行化 |
| `task_id` 不校验 | 传 `".."` 可跨任务列出 1169 个文件 | `task_dir()` 正则 `^[\w-]+$` 唯一校验 |
| `chat` 不收口空 `choices` | `None` 流到 `.strip()` 抛 `AttributeError`，穿透只 catch `RuntimeError` 的降级链 | HTTP 错误与空响应统一翻成 `RuntimeError` |

---

## 附：自动生成的规则通览

```bash
python3 rules.py     # → output/rules.md，六张表全量渲染 + 覆盖自检（报出只能落兜底行的组合）
python3 gates.py     # → output/gates.md，三个门禁清单 + 阈值
python3 review_case.py <task_id>   # 成片自检 + 门禁对账
```
